# 작업 전에 — 구글 시트부터 확인한다

> **한 줄 규칙:** 코드를 건드리기 전에 **구글 시트를 먼저 받아 견주고, 시트에서 손본 값을 가져온 다음** 시작한다.

## 왜 이 문서가 있나

표(`items` · `monsters` · `drops` · `quests` · `skills` · `classes` · `traits` · `stats` · `config`)는
**두 곳에서 고쳐진다.**

| 어디서 | 누가 | 무엇을 |
|---|---|---|
| 구글 시트 | **사람** | 값을 눈으로 보며 곧장 — 드랍 확률, 퀘스트 보상, 아이템 수치 |
| 저장소 `sheets/*.csv` | **작업** | 새 아이템·새 칸·리밸런싱처럼 코드와 함께 가야 하는 것 |

작업을 하면서 `sheets/*.csv` 를 그냥 덮어쓰면, 그 사이 시트에서 손봐 둔 값이
**소리 없이 사라진다.** 실제로 한 번 그럴 뻔했다 — 시트에는 슬라임이 에메랄드를 1% 로
떨구게 해 두었는데, 저장소에는 그 줄이 아예 없었다.

그래서 **순서를 규칙으로 못 박는다.** 시트가 먼저다.

---

## 절차

### 1. 시트를 받는다

**작업하는 컴퓨터·서버에서** (시트가 "링크가 있는 모든 사용자 — 뷰어" 로 열려 있어야 한다):

```bash
node tools/sheets.js pull "https://docs.google.com/spreadsheets/d/<문서아이디>/edit"
node tools/sheets.js pull            # 두 번째부터는 주소가 필요 없다
```

주소는 한 번만 적으면 `sheets/SOURCE.txt` 에 남습니다. 찾아가는 것은 문서 **이름**이
아니라 **아이디**라서, 시트 이름에 붙은 판 번호(`…0_42_0`)가 바뀌어도 그대로 읽힙니다.

> **다음 판도 같은 문서에 덮어써 주세요.** 새 문서를 만들면 아이디가 바뀌고,
> 그때부터 서버와 이 도구가 옛 문서를 읽습니다. 드라이브에 옛 문서가 남아 있을 때
> 실제로 그 일이 있었습니다(아래 0.41→0.42 참고).

이건 시트를 `sheets/*.csv` 에 **곧장 덮어씁니다.** 이번 판에서 새로 넣은 줄이 있으면
아래 2번 방법(견주고 고르기)을 쓰세요.

**바깥으로 못 나가는 자리에서 작업할 때**(예: 이 대화의 작업 환경은
`docs.google.com` 에 붙지 못합니다)는 드라이브에서 통합문서를 내려받아
`sheets/inbox/` 에 두고 2번으로 갑니다.

### 2. 견주고, 시트 값을 고른다

```bash
node tools/sheets-diff.js                    # sheets/inbox/ 의 최신 통합문서와 견준다
node tools/sheets-diff.js --take             # 전부 시트 값으로
node tools/sheets-diff.js --take drops,quests  # 그 탭만
node tools/sheets-diff.js /어디/책.xlsx       # 다른 자리에 있으면 짚어 준다
```

**파일 이름은 안 봅니다.** 폴더를 주거나 아무것도 안 주면 그 안에서 가장 최근에 받은
`.xlsx` 를 집습니다 — `…-0.42.0.xlsx` 가 `…-0.43.0.xlsx` 로 바뀌어도 그대로 돌아갑니다.
(이름을 손으로 적게 하면, 언젠가 옛 파일을 견주고도 "같습니다" 를 보게 됩니다.)

`--take` 가 가르는 규칙:

| 줄 | 어떻게 |
|---|---|
| 양쪽에 있다 | **시트 값을 쓴다** — 사람이 고친 것이 이긴다 |
| 시트에만 있다 | 가져온다 — 사람이 새로 넣은 것 |
| 저장소에만 있다 | **드랍표는** 시트가 그 아이템을 아예 모를 때만 남긴다(예: 홈 뚫는 송곳). 아니면 사람이 지운 것으로 본다 |
| 시트에 없는 탭·칸 | 건드리지 않는다 — 통합문서가 옛것이라는 뜻 |

### 3. 어긋난 것을 가른다

시트가 저장소보다 **옛 판**일 때가 많다. 그러면 차이가 두 종류로 섞여 나온다.

- **사람이 시트에서 고친 값** → 가져온다.
- **지난 판에서 요청받아 바꾼 값** → 저장소가 맞다. 가져오면 그 요청을 되돌리는 셈이다.

가르는 기준은 하나다 — **그 값을 바꾼 이유가 지난 대화에 적혀 있는가.**
적혀 있으면 저장소가 맞고, 없으면 사람이 고친 것으로 본다.
어느 쪽인지 끝내 모르겠으면 **가져오고, 무엇을 가져왔는지 말한다.**
말 없이 되돌리는 것이 가장 나쁘다.

### 4. 확인하고 반영한다

```bash
node tools/sheets.js check     # 표가 말이 되는지
node tools/sheets.js import    # sheets/*.csv → src/data/*.json
node tools/balance.js          # 값이 바뀌었으면 세기도 다시 잰다
```

> ### ⚠ `export` 는 sheets/ 를 **덮어씁니다** — import 를 먼저 하세요
>
> `node tools/sheets.js export` 는 `src/data/*.json` 을 읽어 `sheets/*.csv` 를 새로 씁니다.
> 그래서 **csv 를 고쳐 놓고 import 하기 전에 export 를 돌리면 그 손질이 통째로 날아갑니다.**
>
> 0.42 작업 중에 실제로 그랬습니다 — 보석 개수와 아이템 이름을 csv 에서 고쳐 두고,
> 다른 일(시트 칸 이름 바꾸기) 때문에 export 를 돌렸다가 둘 다 잃었습니다.
> 조용히 사라지므로 눈치채기 어렵습니다. **순서는 언제나 고치기 → check → import → (필요하면) export** 입니다.

### 5. 그 다음에 코드를 만진다

---

## 칸이 늘어난 판을 냈을 때

새 칸을 넣으면(예: 0.33 의 `monsters` 탭 **분노**) **시트도 다시 올려야 한다.**
서버는 칸을 **자리로** 읽으므로 칸이 하나 없으면 뒤가 통째로 밀린다.

밀린 채로 들어가지는 않는다 — 0.33 부터 당기기가 그 자리에서 멈추고 이렇게 말한다:

```
monsters 시트의 16번째 칸 제목이 다릅니다. '분노' 이어야 하는데 '필드그림' 입니다.
표를 새로 뽑아(npm run sheets) 올려 주세요.
```

그럴 때 순서:

1. `node tools/sheets-diff.js <통합문서> --take <칸이 안 바뀐 탭들>` — 먼저 사람 손을 건진다
2. `npm run sheets` — 새 칸이 들어간 통합문서를 굽는다
3. 드라이브에 올려 시트를 갈아 끼운다(탭 이름은 그대로 둔다 — 서버가 이름으로 찾아간다)

---

## 지금까지 이렇게 갈랐다 (0.33 → 0.34)

받은 시트: `dragonfieldtables` (0.31 판 + 사람이 손본 것)

**가져온 것 — 사람이 시트에서 고친 값**

| 탭 | 무엇 |
|---|---|
| drops | 슬라임·늑대·정예들에게 보석 1% 드랍 추가, 정예 재료 개수 상향, 발가르 장신구 0.45 → 0.2 · 용의 인장 2% 추가, 감옥의 주인 희귀약 8~15 → 18~35, 잡템 몇 줄 삭제 |
| quests | 첫 무장 보상을 나무검 1 → 회복약 15 · EXP 80 → 100, 밤의 이빨 EXP 120 → 150, 골짜기의 울음 8 → 12마리, 대장간의 부탁 2 → 3개 |
| items | 낡은 몽둥이 공격 5 → 7, 수호의 목걸이 속도 +3, 룬 목걸이 치명 +3% · 치명피해 +30% |
| stats | 민첩의 2회 공격 확률 0.4% → **0.6%** |

**저장소를 지킨 것 — 지난 판에서 요청받아 바꾼 값**

| 탭 | 무엇 | 언제 요청받았나 |
|---|---|---|
| stats | 힘 공격 +3 · 방어 +4, 지능 체력 +5 | "힘/민첩/지능 모두 공격력을 1 증가", "힘의 방어도는 4", "지능당 체력이 5씩" |
| skills | 광폭화(받는 피해 감소) · 생명 전환(최대 HP) | "스킬에 피흡은 다 없애고" |
| classes | 마법사 광역 마법 설명 | 0.30 광역 70% |
| items | 오닉스 설명(무기·허리띠 홈만) | "장비에서도 피흡은 무기 및 벨트에서만" |
| items · drops | 홈 뚫는 송곳과 그 드랍 3줄 | 0.33 "소켓을 뚫게 해주는 새로운 아이템" |
| monsters | **분노** 칸 | 0.33 "고룡은 한 턴 지날 때마다 공격력을 10%씩" |

> 민첩의 2회 공격을 0.6% 로 올리면 **사냥꾼이 더 세집니다.** 지금도 사냥꾼이
> 다른 두 직업보다 크게 앞서 있으므로(15단계 61% 대 16%/15%), 이 값은
> 밸런스를 다시 잴 때 가장 먼저 보게 될 자리입니다. 그래도 요청대로 가져왔습니다 —
> 말 없이 되돌리지 않는 것이 규칙입니다.

---

## 0.34 → 0.35 에서 저장소가 시트를 앞섭니다 — **시트를 다시 올려야 합니다**

이번 판은 `monsters` 와 `items` 두 탭을 저장소 쪽에서 크게 고쳤습니다.
**시트를 그대로 두면, 서버가 켜질 때 시트가 이 값들을 도로 덮어씁니다.**

| 탭 | 무엇을 | 왜 |
|---|---|---|
| `monsters` | **레벨 사다리 전면** — 슬라임 1 · 박쥐 3 · 늑대 6 · 버섯 9 · 꼬마 악마 13 · 해골 17 · 악마 병사 21 | "쫄몹 레벨 갭이 너무 크다 · 6단계가 5단계보다 낮다" |
| `monsters` | `dungeon_lord` hp 25,720 → **41,705** | 지하감옥 배율을 다시 잡은 뒤 목표 승률 30% 를 되찾기 위해 |
| `items` | **새 줄 둘** — `dragon_helm`(투구 · 희귀 · 20,000 · 방어 16 · HP 70), `dragon_pauldron`(어깨 · 희귀 · 20,000 · 방어 15 · HP 65) | "용린투구·어깨갑옷을 만들고 2만 원 내외로" |
| `items` | `dragon_mail` 등급 → **희귀**, 값 → **20,000** | "용린 세트템은 희귀 등급으로" |

올리는 순서:

```bash
node tools/sheets-diff.js /tmp/user.xlsx        # 사람이 손본 것이 남아 있는지 먼저 본다
node tools/sheets-diff.js /tmp/user.xlsx --take drops,quests,classes,traits,stats,config,skills
#   ⚠ monsters 와 items 는 --take 하지 마세요. 이번 판에서 요청받아 바꾼 값이 들어 있습니다.
node tools/sheets.js check && node tools/sheets.js import
npm run sheets                                  # 구글 시트에 올릴 xlsx 를 새로 굽는다
```

굽힌 xlsx 를 드라이브에 **덮어쓰기로 가져오기** 하면 끝입니다(탭 이름은 그대로 두세요).

> `items` 탭에 줄이 늘었으므로 시트를 안 올리면 **용린 투구와 어깨갑옷이 사라집니다** —
> 상점 진열에서도, 이미 입고 있던 사람의 몸에서도. 이번 판에서 가장 조심할 곳입니다.

**여전히 남아 있는 자리** — `node tools/balance.js --trash` 가 14단계에서
직업 격차 36%p 를 알려 줍니다(사냥꾼 90% · 마법사 54%). 위에서 적어 둔 민첩 0.6% 가
그대로 살아 있고, 3구간 전체에서 사냥꾼이 앞섭니다. 다음 판에서 볼 자리입니다.


---

## 0.35 → 0.36 — 이번에도 **시트를 다시 올려야 합니다**

받은 시트: `dragonfieldtables0_35_0` (0.35 판 xlsx 를 그대로 올린 것)

**가져올 것 없음** — `node tools/sheets-diff.js` 가 "다른 곳이 없습니다" 로 나왔습니다.
사람이 손본 값이 아직 없어서, 이번에는 가릴 것이 없었습니다.

**저장소가 앞선 것 — 이번 판에서 요청받아 바꾼 값**

| 탭 | 무엇을 | 왜 |
|---|---|---|
| `items` | **새 줄 둘** — `useless_sword`(쓸모없는 검 · 희귀 · 100,000 · 공격력 1), `dragon_knight_sword`(용린 기사검 · 희귀 · atk 56 · def 8 · hp 120 · crit 6%) | "성안에서 쓸모없는검을 10만원에 추가 … 용린기사검으로 바뀌고 세트아이템이 완성" |
| `skills` | **한 줄 삭제** — `power_strike`(강타) | "기사스킬 강타는 없애줘" |
| `stats` | 민첩 2회 공격 확률 0.006 → **0.005** | "민첩 2회공격을 0.5%로 낮춰줘" |
| `quests` | `reach5`(무너진 초소까지) 보상 `iron_sword` → **`dragon_pauldron`** | "퀘스트 무너진 초소까지 에서 보상을 용린 어깨를 줘" |
| `monsters` | 슬라임 26/7/3 → **155/19/6**, 박쥐 34/12/4 → **115/23/6**, 늑대 88/26/13 → **163/37/16** | "1~4단계는 저렙구간이라 약간의 고비가 있어야해" |
| `monsters` | 보스 체력 — 악마 정찰대장 103 · 발가르 232 · 강화된 정찰대장 4,996 · 강화된 발가르 7,421 · 감옥의 주인 43,960 | 위의 값들이 바뀌어 목표 승률을 다시 맞춤 |

올리는 순서:

```bash
node tools/sheets-diff.js /tmp/user.xlsx        # 사람이 손본 것이 있는지 먼저 본다
#   ⚠ items · skills · stats · quests · monsters 는 --take 하지 마세요.
#     이번 판에서 요청받아 바꾼 값이 들어 있습니다.
node tools/sheets.js check && node tools/sheets.js import
npm run sheets                                  # 올릴 xlsx 를 새로 굽는다
```

> ### ⚠ 시트를 안 올리면 **시트 반영이 통째로 멈춥니다**
>
> 조용히 덮어쓰는 것이 아니라, 서버가 당기기를 **거절합니다.**
>
> ```
> npcs.json: 'gate_merchant' 의 판매 목록에 없는 아이템 'useless_sword'
> ```
>
> 새 코드의 상점 목록이 새 아이템을 가리키는데 시트에는 그 줄이 없으니, 검사가
> 걸러 냅니다. 게임이 깨지지는 않습니다 — 서버는 이전 표를 그대로 쓰고 돌아갑니다.
> 다만 **그때부터 시트에서 무엇을 고쳐도 반영되지 않습니다.**
> 시트를 다시 올리면 그 자리에서 풀립니다.
>
> 조용히 어긋나는 것보다 낫습니다. 값 하나가 밀린 채로 들어가 몇 판 뒤에야
> "왜 이 몬스터가 이렇게 세지" 로 발견되는 것이 제일 나쁩니다.

> **⚠ 서버가 보고 있는 문서가 새 문서인지 확인하세요.**
> 드라이브에 `dragonfieldtables`(옛것)와 `dragonfieldtables0_35_0`(새것)가 **둘 다** 있습니다.
> Render 의 `SHEET_ID` 가 아직 옛 문서를 가리키고 있으면, 새 문서에 아무리 올려도
> 서버는 옛 표를 읽어 갑니다. 한 문서만 남기거나 `SHEET_ID` 를 새 문서로 바꾸세요.

**여전히 남아 있는 자리** — 직업 격차가 큽니다.
14단계에서 사냥꾼 94% 대 마법사 38%(56%p), 15단계 보스에서 용사 14% 대 사냥꾼 51%(37%p).
민첩을 0.5% 로 낮춘 것만으로는 좁혀지지 않았습니다. 3구간(11~15단계) 전체에서
사냥꾼이 앞서고 마법사가 처집니다 — 다음 판에서 가장 먼저 볼 자리입니다.


---

## 0.36 → 0.37 — **칸이 둘 늘었습니다. 시트를 꼭 다시 올리세요**

받은 시트: `dragonfieldtables0_35_0` (아직 0.35 판 그대로)

**가져올 것 없음** — `node tools/sheets-diff.js` 로 견준 차이가 **전부 0.36 에서
요청받아 바꾼 값**이었습니다(슬라임·박쥐·늑대 능력치, 보스 체력, 강타 삭제,
민첩 0.5%, 무너진 초소 보상, 새 검 두 줄). 사람이 시트에서 손본 값은 없었습니다.

**저장소가 앞선 것 — 이번 판에서 요청받아 바꾼 값**

| 탭 | 무엇을 | 왜 |
|---|---|---|
| `quests` | **새 칸 `열림조건`** + 새 줄 `dragon_scrap`(쓸모를 잃은 무기 · 10만 골드) | "고룡을 한번이라도 만났으면 특별 퀘스트 … 10만골드를 보상으로" |
| `classes` | **새 칸 `마법 전환 배율`** · 마법사 `1.1` | "마법사는 모든 데미지를 마법데미지로 최종으로 바꾸고 1.1배" |
| `items` | 새 줄 셋 — `magic_helm`(매직 투구), `dragon_knight_bow`(용린 기사궁), `dragon_knight_staff`(용린 기사장) | "매직 투구를 하나 상점에서" · "직업에 맞는 용린 기사검으로 전직업이 사용가능하게" |
| `items` | 새 줄 `dragon_gate_key`(용의 열쇠) | "10개의 용의 인장을 모아서 마녀에게서 열쇠를 구매하고" |
| `monsters` | 새 줄 `elder_dragon`(잠겨 있던 용 아그라모스 · hp 4,937,000 · 공격 7,406 · 방어 2,962 · 골드 15,000,000) | "두번째 고룡 … 지하감옥5층보스 보다 두배정도 세게" |
| `drops` | `elder_dragon` 10줄 — 보석 일곱 가지 2~4개씩, 마력석·악마의 핵 40~80, 약초 20~40 | "각종 보석을 랜덤으로 20개, 각종 재료 100~200개" |

올리는 순서:

```bash
node tools/sheets-diff.js /tmp/user.xlsx        # 사람이 손본 것이 있는지 먼저 본다
#   ⚠ items · monsters · drops · quests · classes 는 --take 하지 마세요.
node tools/sheets.js check && node tools/sheets.js import
npm run sheets                                  # 올릴 xlsx 를 새로 굽는다
```

> ### ⚠ 이번 판은 **칸이 늘어서** 시트가 안 맞으면 당기기가 멈춥니다
>
> `quests` 탭에 `열림조건`, `classes` 탭에 `마법 전환 배율` 이 늘었습니다.
> 서버는 칸을 **자리로** 읽으므로, 옛 시트를 그대로 두면 당기기가 그 자리에서 멈추고
> 어느 칸이 다른지 알려 줍니다. 게임은 이전 표로 정상 동작하지만,
> **그때부터 시트에서 무엇을 고쳐도 반영되지 않습니다.**

> **⚠ 서버가 보고 있는 문서를 확인하세요.**
> 드라이브에 `dragonfieldtables`(옛것)와 `dragonfieldtables0_35_0`(새것)가 아직 둘 다 있습니다.
> `SHEET_ID` 가 옛 문서를 가리키면 새 문서에 올려도 서버는 옛 표를 읽습니다.

**여전히 남아 있는 자리** — 마법사는 이번 판으로 크게 나아졌습니다
(20단계 보스 16% → 23%, 지하감옥 주인 34% → 45%). 다만 **3구간(11~15단계)에서는
아직 사냥꾼이 앞섭니다** — 14단계에서 사냥꾼 94% 대 마법사 44%. 다음 판에서 볼 자리입니다.


---

## 0.37 → 0.38 — 표는 그대로입니다

받은 시트: `dragonfieldtables0_35_0` (아직 0.35 판)

**가져올 것 없음** — 견준 차이가 전부 0.36·0.37 에서 요청받아 바꾼 값이었습니다.
사람이 시트에서 손본 값은 이번에도 없었습니다.

**이번 판은 시트 밖만 바뀌었습니다.**

| 어디 | 무엇을 |
|---|---|
| `src/data/affixes.json` | `각인` 표가 새로 생김(등급별 3~5 / 5~10 / 10~15 / 20~30, 초월확률 0.1) · 용린 4세트에 아그라모스 줄 추가(`특성배율`) |
| `src/data/npcs.json` | 촌장 `lines2`(연달아 두 번 말 걸면 나오는 이야기) |
| `tools/gen-maps.js` | 촌장 자리 y 20 → 22 |
| 코드 | 소지품 목록에서 착용 장비 숨김 · 장비 칸 클릭 = 정보 · 장비 비교 ▲▼ |

`affixes.json` 과 `npcs.json` 은 **시트 탭이 아닙니다.** 그래서 이번 판은
**시트를 다시 올리지 않아도 됩니다** — 0.37 시트를 이미 올렸다면 그대로 두면 됩니다.

> 아직 0.37 시트를 안 올렸다면 그건 그대로 남아 있는 일입니다.
> `quests` 의 `열림조건`, `classes` 의 `마법 전환 배율` 두 칸이 없으면 당기기가 멈춥니다.

**여전히 남아 있는 자리**

- 3구간(11~15단계)에서 사냥꾼이 앞섭니다 — 14단계 사냥꾼 94% 대 마법사 44%.
- 15단계 보스에서 용사가 처집니다(용사 15% 대 사냥꾼 50%).


---

## 0.38 → 0.39 — **탭이 하나 늘었습니다(`engrave`). 시트를 다시 올리세요**

받은 시트: `dragonfieldtables0_35_0` (드라이브에서 받아 견줌 · 아직 0.35 판)

**가져올 것 없음** — `node tools/sheets-diff.js /tmp/user.xlsx` 로 견준 차이가
**전부 0.36·0.37 에서 요청받아 바꾼 값**이었습니다(슬라임·박쥐·늑대 능력치, 보스 체력,
강타 삭제, 민첩 0.5%, 무너진 초소 보상, 새 아이템 여섯 줄, 아그라모스와 그 드랍,
`quests` 의 `열림조건` 칸). 사람이 시트에서 손본 값은 이번에도 없었습니다.

**저장소가 앞선 것 — 이번 판에서 새로 생긴 것**

| 탭 | 무엇을 | 왜 |
|---|---|---|
| **`engrave` (새 탭)** | 등급 다섯 줄 — `등급 · 이름 · 최소 · 최대 · 붙을 확률 % · 초월 확률 %` | "각인 등급 표를 시트로 빼면 직접 수치를 만질 수 있다" |

`affixes.json` 의 `각인` 표가 이 탭으로 들어옵니다. 등급 줄이 `[최소, 최대]` 에서
`[최소, 최대, 붙을확률, 초월확률]` 네 칸으로 늘었고, **뒤 둘은 없어도 됩니다**
(없으면 표 전체의 `확률` · `초월확률` 을 씁니다 — 지금까지와 같은 값입니다).

올리는 순서:

```bash
node tools/sheets-diff.js /tmp/user.xlsx        # 사람이 손본 것이 있는지 먼저 본다
#   ⚠ items · monsters · drops · quests · classes · skills · stats 는 --take 하지 마세요.
node tools/sheets.js check && node tools/sheets.js import
npm run sheets                                  # 탭 열 개짜리 xlsx 를 새로 굽는다
```

> ### ⚠ 탭이 없으면 그 탭에서 당기기가 멈춥니다
>
> 서버는 `server/sheets.js` 의 등록표대로 탭을 하나씩 찾아갑니다. `engrave` 탭이
> 없으면 그 자리에서 "'engrave' 탭을 받지 못했습니다" 로 멈춥니다. 게임은 이전 표로
> 그대로 돌지만, **그때부터 시트에서 무엇을 고쳐도 반영되지 않습니다.**

> **`affixes.json` 의 서식이 바뀌었습니다.** 이 파일은 사람이 칸을 맞춰 둔 표라
> `JSON.stringify` 로 다시 쓰면 표가 아니게 됩니다(드랍표·퀘스트와 같은 이유).
> 그래서 전용 서식기(`formatAffixes`)를 붙였고, 그 서식기가 한 번 다시 찍었습니다 —
> **값은 하나도 안 바뀌었고** 칸 맞춤과 `0.10 → 0.1` 같은 표기만 달라졌습니다.

**여전히 남아 있는 자리** — 지난 판과 같습니다.

- 3구간(11~15단계)에서 사냥꾼이 앞섭니다 — 14단계 사냥꾼 94% 대 마법사 44%.
- 15단계 보스에서 용사가 처집니다(용사 15% 대 사냥꾼 50%).


---

## 0.39 → 0.40 — **표에 줄이 늘었습니다. 시트를 다시 올리세요**

받은 시트: `dragonfieldtables0_35_0` (드라이브에서 확인 · 마지막 수정 09:20, 앞 판에서 견준
그 판 그대로였습니다)

**가져올 것 없음** — 사람이 시트에서 손본 값은 이번에도 없었습니다.
차이는 전부 0.36~0.39 에서 요청받아 바꾼 값이었습니다.

**저장소가 앞선 것 — 이번 판에서 요청받아 바꾼 값**

| 탭 | 무엇을 | 왜 |
|---|---|---|
| `monsters` | **새 줄** `dungeon_general`(봉인된 장군 발가르 · hp 14,000 · 공격 480 · 방어 320) | "지하 4단 보스가 약해, 4단 쫄보다 공격력을 강하게" |
| `monsters` | `dungeon_lord` 공격 208 → **350**, 체력 43,960 → **10,000** | "5단계 보스도 약한 느낌이야, 보스답게 공격력을 올려줘" |
| `monsters` | `great_dragon` 공격 1,050 → **7,000** | "고룡의 공격력은 너무 약해서 데미지가 400정도 밖에 안 들어와" |
| `monsters` | `elder_dragon` 공격 7,406 → **9,500** | 고룡이 세지면 그 위가 뒤집히므로 함께 올림 |
| `drops` | **새 줄 아홉** — `dungeon_general` 의 전리품 | 새 몬스터가 아무것도 안 떨구면 4층이 도리어 손해가 된다 |
| `drops` | 1~5단계 여섯 몬스터에 루비·에메랄드·오닉스 (2~15%) | "1~5단계에서 루비 에메랄드 오닉스 확률을 2배" — 없던 줄은 새로 넣었습니다(0을 두 배 해도 0) |
| `drops` | 꼬마 악마·정찰대장의 `demon_core` 확률·개수 세 배 | "악마의 핵재료는 3배정도 많이 나오게" |

**표 밖에서 바뀐 것** (시트와 무관)

| 어디 | 무엇을 |
|---|---|
| `src/data/formulas.js` | 초월 강화 규칙 교체 — `TRANSCEND_CHANCE` · `TRANSCEND_GEM` |
| `src/data/npcs.json` | `royal_smith`(왕실 대장장이 도르한) 추가 · 마녀의 초월 강화 제거 |
| `tools/gen-maps.js` · `gen-dungeon.js` | 성 안 대장간 타일 · 지하감옥 올라가는 계단 |
| 서버 | `/api/admin/season-reset` — 전체 유저 초기화 |

올리는 순서:

```bash
node tools/sheets-diff.js /tmp/user.xlsx        # 사람이 손본 것이 있는지 먼저 본다
#   ⚠ monsters · drops 는 --take 하지 마세요. 이번 판에서 요청받아 바꾼 값입니다.
node tools/sheets.js check && node tools/sheets.js import
npm run sheets                                  # 탭 열 개짜리 xlsx 를 새로 굽는다
```

> ### ⚠ 시트에 새 몬스터가 없으면 당기기가 멈춥니다
>
> `maps.json` 의 4층이 `dungeon_general` 을 가리키는데 시트에 그 줄이 없으면
> 검사가 "없는 몬스터" 로 걸러 냅니다. 게임은 이전 표로 그대로 돌지만,
> **그때부터 시트에서 무엇을 고쳐도 반영되지 않습니다.**

**여전히 남아 있는 자리**

- 3구간(11~15단계)에서 사냥꾼이 앞섭니다 — 14단계 사냥꾼 94% 대 마법사 44%.
- 15단계 보스에서 용사가 처집니다(용사 15% 대 사냥꾼 50%).
- 감옥의 주인은 체력이 4분의 1로 줄어 **싸움이 20턴 안에 끝납니다.** 경험치·골드는
  그대로라 시간당 보상이 올라갔습니다 — 되살아나는 시간(15초)이 상한을 잡고 있지만,
  다음 판에서 한 번 재 볼 자리입니다.


---

## 0.40 → 0.41 — **표에 줄이 바뀌었습니다. 시트를 다시 올리세요**

받은 시트: `dragonfieldtables0_35_0` (드라이브에서 확인 · 마지막 수정 09-02 09:20 —
앞 판에서 견준 그 판 그대로였습니다)

**가져올 것 없음** — 사람이 시트에서 손본 값은 이번에도 없었습니다.

**저장소가 앞선 것 — 이번 판에서 요청받아 바꾼 값**

| 탭 | 무엇을 | 왜 |
|---|---|---|
| `skills` | `volley`(연발 사격) **삭제**, `foresight`(예측 공격 · 선제 공격 위력 0.7 · 최대 3) **추가** | "사냥꾼 연발사격을 예측공격으로 바꾸고 선제공격시 데미지 70%" |
| `skills` | `deadeye`(죽음의 일격) 치명타 피해 0.3 → **0.2** | "죽음의 일격 치명타 피해도 30%를 20%로 낮춰줘" |
| `monsters` | `dungeon_lord` 경험치 89,680 → **134,520**, 골드 70,800 → **106,200** | "감옥주인 보상을 올려줘" |
| `drops` | `dungeon_lord` 보석 개수 **두 배**, `gem_emerald`·`gem_onyx` **새 줄 둘** | "보석을 두배 많이줘" — 에메랄드·오닉스는 초월 강화의 값인데 안 떨구고 있었습니다 |

**표 밖에서 바뀐 것**

| 어디 | 무엇을 |
|---|---|
| `src/data/skills.json` | 효과 이름 `openerPowerBonus`(선제 공격 위력) 추가 |
| `src/systems/CombatSystem.js` · `SkillSystem.js` | 그 보정을 `tr.openerPower` 에 얹는다 |
| `src/ui/AdminPanel.js` | `SEASON_GIFT` — 시즌 시작 선물 꾸러미와 단추 |
| `tools/balance.js` | 맵 이름을 그대로 넘기도록 고침(아래 참고) |

> ### 재는 자를 하나 고쳤습니다
>
> `tools/balance.js` 가 맵 배율을 **미리 숫자로 바꿔** `winRate` 에 넘기고 있었습니다.
> 그러면 그 함수가 "이 땅은 마법이 몇 할인가" 를 찾지 못해 **0 으로 잽니다.**
> 지하감옥은 절반이 마법이라(0.37 부터), 감옥의 주인이 16%(너무 어려움)로 보였습니다 —
> 맵 이름을 그대로 넘기니 **28%** 였습니다. 목표 30% 에 이미 맞아 있었던 것입니다.
>
> 값은 하나도 안 건드렸습니다. 이런 것은 **몬스터를 고치기 전에 자를 먼저 의심하는**
> 것으로만 잡힙니다 — 안 그랬으면 멀쩡한 보스를 약하게 만들 뻔했습니다.

올리는 순서:

```bash
node tools/sheets-diff.js /tmp/user.xlsx        # 사람이 손본 것이 있는지 먼저 본다
#   ⚠ skills · monsters · drops 는 --take 하지 마세요. 이번 판에서 요청받아 바꾼 값입니다.
node tools/sheets.js check && node tools/sheets.js import
npm run sheets
```

**여전히 남아 있는 자리**

- **15단계 보스에서 용사가 처집니다** — 용사 13% 대 사냥꾼 49%(36%p). 죽음의 일격을
  낮춘 뒤에도 그대로입니다. 사냥꾼이 센 것이 아니라 **용사가 그 구간에서 약한** 쪽에
  가깝습니다(10단계 50%, 20단계 19%). 다음 판에서 가장 먼저 볼 자리입니다.
- 나머지 네 자리(5·10·20단계, 지하감옥 5층)는 목표 안에 들어와 있습니다.


---

## 0.41 → 0.42 — 표가 바뀌었습니다(칸 이름 하나 포함)

받은 시트: 드라이브에 둘이 있습니다.

| 문서 | 언제 | 무엇 |
|---|---|---|
| `dragonfieldtables0_35_0` | 09-02 09:20 | 지금 쓰는 것. 앞 판에서 견준 그대로였습니다 |
| `dragonfieldtables` (dragonfield 폴더) | 09-03 02:13 에 **열림** | **0.31 판입니다** — 분노·열림조건 칸이 없고 engrave 탭도 없습니다 |

두 번째 문서가 어제 열린 흔적이 있어 받아서 견줬는데, **사람이 고친 것이 아니라
그냥 열어 본 것**이었습니다(고친 사람이 칸을 지우고 아이템 열 줄을 없앨 리는 없습니다).
가져올 값은 없었습니다.

> ⚠ **서버의 `SHEET_ID` 가 이 옛 문서를 가리키고 있지 않은지 확인하세요.**
> 가리키고 있으면 당기기가 "칸이 다르다"에서 멈춰, 그때부터 시트 반영이 통째로 서 버립니다.

**저장소가 앞선 것 — 이번 판에서 요청받아 바꾼 값**

| 탭 | 무엇을 | 왜 |
|---|---|---|
| `classes` | **칸 이름** `힘 성장주기` → **`힘 성장 차례`** (민첩·지능도) | 숫자의 뜻이 '몇 레벨마다' 에서 **차례(1등·2등·3등)** 로 바뀜 |
| `items` | `socket_drill` 이름 `홈 뚫는 송곳` → **`악마의 파편`**, 설명 첫 줄을 "장비에 보석 홈을 하나 더 만든다" 로 | "송곳의 이름을 악마의 파편으로 바꾸고 장비에 홈을 추가한다는 설명을" |
| `drops` | `dungeon_lord` 보석 개수 **원복**(2~4 → 1~2) | "지하감옥 보석 확률 두배는 다시 원복" |

**표 밖에서 바뀐 것**

| 어디 | 무엇을 |
|---|---|
| `src/systems/ProgressionSystem.js` | 스탯 성장 규칙을 다시 짬 — `growthShares` · `statsFromGrowth` |
| `src/systems/CombatSystem.js` · `ui/BattleView.js` | 선제 사격에 위력을 실어 보내고 화면이 `예측 +210%` 로 적음 |
| `src/ui/AdminPanel.js` · `main.js` | 초기화에 "선물도 함께" 체크 |
| `tools/stat-growth.js` | **새 도구** — 1레벨부터 만렙까지 키워 보고 3:2:1 이 지켜지는지 잰다 |

올리는 순서:

```bash
node tools/sheets-diff.js /tmp/user.xlsx        # 사람이 손본 것이 있는지 먼저 본다
#   ⚠ classes · items · drops 는 --take 하지 마세요. 이번 판에서 요청받아 바꾼 값입니다.
node tools/sheets.js check && node tools/sheets.js import
npm run sheets
```

> ### ⚠ `classes` 탭의 숫자 뜻이 바뀌었습니다
>
> 1·2·3 은 이제 **몇 레벨마다**가 아니라 **몇 등으로 잘 자라나** 입니다.
> 값 자체는 그대로 두면 되고(용사 1·2·3), 칸 이름만 바뀝니다.
> 다른 비율로 키우고 싶으면 차례를 바꾸면 됩니다 — 몫은 언제나 3:2:1 로 갈립니다.

**여전히 남아 있는 자리**

- **15단계 보스에서 용사가 처집니다** — 용사 13% 대 사냥꾼 49%. 지난 판 그대로입니다.
- 보스 리젠은 **1분이 규칙**이고 고룡 둘만 예외입니다(카르나크 30분 주기 · 아그라모스
  죽은 뒤 1시간). 이번 판에서 값을 바꾼 것이 아니라, 이미 그랬던 것을 문서로 못 박았습니다.


---

## 0.42 → 0.43 — 표는 한 줄만 바뀌었습니다

받은 시트: `0.42.0` 판 통합문서(사용자가 "시트 그대로 옮겨놨다" 고 한 그것).
`node tools/sheets-diff.js` 로 견주니 **다른 곳은 세 줄뿐**이었고, 그 셋이 곧
이번 판에서 바꾸기로 한 값이었습니다.

**가져올 것 없음** — 사람이 시트에서 손본 값은 이번에도 없었습니다.

**저장소가 앞선 것 — 이번 판에서 요청받아 바꾼 값**

| 탭 | 무엇을 | 왜 |
|---|---|---|
| `drops` | `dungeon_wraith` · `dungeon_golem` · `dungeon_lord` 의 `socket_drill` 확률 `0.00001` → **`0.01`** | "악마의 파편은 1%확률로 해줘" |

0.001% 는 사실상 안 나오는 값이었습니다(만 번 잡아야 한 번). 1% 면 백 번쯤에 한 번이라
지하감옥을 오래 도는 사람에게는 **찾아지는** 물건이 됩니다.

**표 밖에서 바뀐 것**

| 어디 | 무엇을 |
|---|---|
| `src/core/Sound.js` | **새 파일** — 소리를 전부 그 자리에서 만든다(음원 파일 0바이트) |
| `src/scenes/hitKind.js` | **새 파일** — "무엇으로 때렸나" 를 정하는 잣대 한 곳 |
| `src/scenes/BattleScene.js` | 공격 자세 갈아끼우기 · 맞는 자리 이펙트 |
| `src/core/Renderer.js` | `drawSprite` 에 `rotate` — 발밑을 축으로 기울인다 |
| `tools/art-fx.js` | **새 파일** — 이펙트 다섯 장과 공격 자세 만들기 |

### 시트를 **이름과 상관없이** 읽습니다 (이번 판의 요청)

두 자리를 고쳤습니다.

| 어디 | 전 | 후 |
|---|---|---|
| `tools/sheets.js pull` | 주소를 매번 적어야 했다 | 한 번 적으면 `sheets/SOURCE.txt` 에 남는다. 문서 **아이디**로 찾으므로 시트 이름이 바뀌어도 읽힌다 |
| `tools/sheets-diff.js` | 파일 이름을 매번 적어야 했다 | 폴더(기본 `sheets/inbox/`)에서 **가장 최근 .xlsx** 를 집는다 |

이제 판이 올라가도 절차가 그대로입니다:

```bash
# 드라이브에서 받은 통합문서를 sheets/inbox/ 에 둔다 (이름은 아무래도 좋다)
node tools/sheets-diff.js            # 무엇이 다른지
node tools/sheets-diff.js --take     # 사람이 고친 값 가져오기
node tools/sheets.js check && node tools/sheets.js import
npm run sheets                       # 올릴 xlsx 를 새로 굽는다
```

**여전히 남아 있는 자리**

- **15단계 보스에서 용사가 처집니다** — 용사 13% 대 사냥꾼 49%. 세 판째 그대로입니다.
- 이번 판은 표를 거의 안 건드렸으므로 밸런스는 0.42 와 같습니다.


---

## 0.43 → 0.44 — **사람이 시트에서 고쳤습니다. 처음으로 가져왔습니다**

받은 시트: `dragonfieldtables0_42_0` (드라이브에서 받아 `sheets/inbox/` 에 두고 견줌 ·
마지막 수정 09-03 04:53 — **0.43 을 내보낸 04:11 뒤**입니다).

여덟 판 만에 처음으로 **가져올 것이 있었습니다.**

### 가져온 것 — 사람이 시트에서 고친 값

| 탭 | 무엇을 | 전 → 후 |
|---|---|---|
| `skills` | 가시 갑옷 최대 랭크 | 5 → **4** |
| `skills` | 가시 갑옷 반사량 | 20% → **25%** |
| `skills` | 전투 본능 — 방어력 → 공격력 전환 | 6% → **10%** |
| `skills` | 철벽 최대 랭크 | 4 → **5** |
| `stats` | **힘의 공격력** | 3 → **1** |

넷은 전부 용사 스킬이고 마지막 하나는 힘입니다. 따로 보면 뿔뿔이 흩어진 값 같은데,
같이 놓고 보면 **용사를 다시 짠 것**입니다 — 힘으로 때리던 직업에서
**방어를 공격으로 바꾸는 직업**으로. 그래서 다섯 줄을 통째로 가져왔습니다.

**설명 글도 따라 고쳤습니다.** 값만 바뀌고 글이 그대로면 화면이 거짓말을 합니다.

| 어디 | 전 | 후 |
|---|---|---|
| 가시 갑옷 | "방어력의 20%를 되돌려 준다" | "방어력의 **25%**를…" |
| 전투 본능 | "방어력의 6%가 공격력에" | "방어력의 **10%**가…" |
| 힘 | "공격력 +3, 방어력 +4" | "공격력 **+1**, 방어력 +4" |

### 저장소를 지킨 것

| 탭 | 무엇을 | 왜 |
|---|---|---|
| `drops` | 악마의 파편 1% (`0.01`) | 0.43 에서 요청받아 바꾼 값입니다. 시트는 0.42 판이라 아직 `0.00001` 이었습니다 |

### ⚠ 이 다섯 줄이 밸런스를 뒤집었습니다

```
                            0.43                0.44
15단계 보스     용사 13% · 사냥꾼 49%   →   용사 51% · 사냥꾼 35%
20단계 보스     용사  3% · 사냥꾼 20%   →   용사 80% · 사냥꾼 23%
지하감옥 5층    용사  0% · 사냥꾼 14%   →   용사 76% · 사냥꾼 14%
```

세 판째 "용사가 처진다" 던 자리는 풀렸습니다. 대신 **용사가 혼자 앞서 나갑니다** —
직업 격차가 최대 62%p 입니다(30%p 를 넘으면 한 직업이 정답이 됩니다).

값은 **하나도 되돌리지 않았습니다.** 말 없이 되돌리지 않는 것이 이 문서의 규칙이고,
어느 쪽으로 맞출지는 고친 사람이 정할 일이기 때문입니다. 손볼 곳을 적어 둡니다:

- **전투 본능 10% → 7% 쯤.** 방어력이 그대로 공격력이 되는 값이라 룬 한 벌(+10)에서
  가장 크게 부풀어 오릅니다. 앞 구간은 거의 안 변하고 뒷 구간만 내려옵니다.
- **철벽 최대 5 → 4 로 되돌리기.** 철벽이 방어력을 올리고 전투 본능이 그 방어력을
  공격력으로 바꾸므로, 둘이 **곱해집니다**. 한쪽만 줄여도 충분합니다.
- 힘의 공격력은 그대로 두는 편이 낫습니다 — 세 직업이 다 쓰는 값이라 여기를 만지면
  사냥꾼·마법사까지 함께 움직입니다.

### 재는 자도 한 번 고쳤습니다

`tools/balance.js` 는 직업마다 **스킬 찍는 순서를 딱 하나** 적어 두고 그것으로 쟀습니다.
0.44 의 용사는 그 순서(공격 먼저)로 찍으면 15단계에서 **3%** 이고,
방어부터 찍으면 같은 표로 **51%** 입니다.

```
없는 문제를 고치러 갈 뻔했습니다 — 약한 것은 직업이 아니라 자에 적힌 순서였습니다.
```

이제 직업마다 순서를 **여럿** 적어 두고, 잴 때 그중 가장 잘 버티는 것으로 잽니다
(`PLANS`). 사람은 어차피 몇 판 해 보고 잘 되는 쪽으로 찍으므로, 그 사람을 재는 것이 맞습니다.
새 스킬을 넣거나 값을 크게 바꿨으면 **후보를 하나 더 적어** 두면 됩니다.

### 표 밖에서 바뀐 것

| 어디 | 무엇을 |
|---|---|
| `tools/art-monfx.js` | **새 파일** — 몬스터 열한 종의 덤벼드는 자세 |
| `src/core/Sound.js` | 곡 넷(마을·들판·지하감옥·고룡) · 효과음 열다섯 추가 |
| `src/core/hitLook.js` | `scenes/hitKind.js` 에서 옮겨 옴 + `hitWeight` 추가 |
| `src/data/maps.json` | 땅마다 `bgm` 칸 |
| `src/systems/SettingsSystem.js` | **배경 음악 기본값 꺼짐 → 켬** (0.43 의 곡을 아무도 못 듣고 있었습니다) |
| `styles/ui.css` | 치명타·큰 한 방 글씨 |

올리는 순서:

```bash
node tools/sheets-diff.js            # sheets/inbox/ 의 최신 통합문서와 견준다
#   ⚠ drops 는 --take 하지 마세요. 0.43 에서 요청받아 바꾼 값입니다.
node tools/sheets.js check && node tools/sheets.js import
npm run sheets
```

**여전히 남아 있는 자리**

- **직업 격차 62%p** — 위의 "이 다섯 줄이 밸런스를 뒤집었습니다" 를 보세요.
- 5단계 보스가 목표보다 어렵습니다(평균 -14%p). 힘의 공격력이 내려간 여파가
  장비가 얇은 초반에 가장 크게 옵니다.


---

## 0.44 → 0.45 — 표는 그대로입니다

받은 시트: `dragonfieldtables0_42_0` (드라이브에서 확인 · 마지막 수정 **09-03 04:53** —
0.44 에서 가져간 그 판 그대로였습니다. 새로 고친 값이 없어 내려받지 않았습니다).

**가져올 것 없음 · 저장소도 표를 안 건드렸습니다.** 이번 판은 운영자 창과 서버만
바뀌었습니다.

| 어디 | 무엇을 |
|---|---|
| `server/world.js` | 시즌 고정(`locked`) · `setSeason` · `clearAllMail` |
| `server/server.js` | `/api/admin/season-lock` · `/api/admin/season-set` · `/api/admin/mail-clear` |
| `src/ui/AdminPanel.js` | 시즌 상자(고정 · 1 시즌 되돌리기) · 우편 모두 지우기 |

**시즌 번호가 한 살씩 많던 것도 고쳤습니다** — 랭킹 창은 2 시즌인데 알림만 "3 시즌"
이라고 말하고 있었습니다. 세이브를 지우는 일과 랭킹을 넘기는 일의 순서가 바뀌면서
이미 올라간 번호에 +1 을 한 번 더 하고 있었습니다.

**여전히 남아 있는 자리** — 0.44 와 같습니다. 직업 격차 62%p 가 그대로입니다.

### 시트 주소를 서버도 저장소에서 찾습니다 (0.45)

운영자 창의 "시트 지금 읽기" 가 `SHEET_ID 가 설정되어 있지 않습니다` 에서 막히던 자리를
고쳤습니다. 이제 서버도 `sheets/SOURCE.txt` 를 봅니다.

| 차례 | 어디 |
|---|---|
| ① | 환경변수 `SHEET_ID` (있으면 이깁니다) |
| ② | 저장소 `sheets/SOURCE.txt` |

이 파일은 0.43 에서 `tools/sheets.js pull` 이 적어 두던 것인데, **도구만 보고 서버는
안 보고 있었습니다.** 같은 것을 두 곳이 따로 알고 있으면 그중 하나는 늘 모릅니다.


---

## 0.45 → 0.46 — ⚠ **적어 둔 문서가 사라졌습니다**

### 먼저 — 드라이브에 같은 이름이 둘입니다

`sheets/SOURCE.txt` 에 적어 두었던 `dragonfieldtables0_42_0` 이 **없어졌습니다**(지워짐).
대신 **`dragonfieldtables` 라는 이름의 문서가 둘** 생겼습니다.

| 문서 | 어디 | 언제 | 무엇 |
|---|---|---|---|
| `1sNjfEbe…` | dragonfield 폴더 | 09-03 07:04 수정 | **사람이 고친 값이 들어 있습니다** ← 이걸 씁니다 |
| `1jOJqc5w…` | 뿌리 폴더 | 09-03 07:11 만듦 | 0.45 통합문서를 그대로 올린 것. 고친 값 없음 |

`SOURCE.txt` 를 **앞의 것**으로 바꿔 적었습니다.

> **뒤의 것(뿌리 폴더)을 지우거나 이름을 바꿔 주세요.** 같은 이름이 둘이면 다음에도
> 어느 쪽인지 알 수 없고, 서버가 고친 값이 없는 쪽을 읽으면 **손질이 통째로 덮입니다.**
> 새 판을 올릴 때는 언제나 **같은 문서에 "덮어쓰기로 가져오기"** 하세요 — 새로 만들면
> 아이디가 바뀝니다. 0.42 에서도 같은 일로 헷갈렸고, 이번에는 실제로 사라졌습니다.

### 가져온 것 — 사람이 시트에서 고친 값

| 탭 | 무엇을 | 전 → 후 |
|---|---|---|
| `monsters` | 슬라임 hp | 155 → **30** |
| `monsters` | 박쥐 hp | 115 → **45** |
| `items` | 낡은 몽둥이 공격 | 7 → **5** |

셋 다 "1~4단계를 순하게" 라는 같은 뜻이었습니다. 그대로 가져온 뒤,
**이어서 1~4단계를 목표에 맞췄습니다**(아래).

### 이어서 맞춘 것 — 요청받은 작업

| 탭 | 무엇을 | 값 |
|---|---|---|
| `monsters` | 슬라임 | hp **44** · 공격 24 · 방어 7 |
| `monsters` | 박쥐 | hp **51** · 공격 25 · 방어 6 |
| `monsters` | 늑대 | hp **99** · 공격 29 · 방어 13 |
| `monsters` | 버섯 | hp **75** · 공격 17 · 방어 11 |
| `skills` | 전투 본능 | 10% → **8%** (설명 글도 함께) |

> 슬라임 hp 를 30 에서 **44** 로 올렸습니다. 30 이면 어느 레벨에서도 100% —
> 첫 땅에 아무 내용이 없어집니다. 44 면 Lv.1 에 85%, Lv.2 부터 100% 입니다.
> 고치신 방향(순하게)은 그대로 두고 첫 한 걸음만 남겼습니다.

**5단계는 요청대로 손대지 않았습니다** — 여전히 목표보다 어렵습니다(평균 -14%p).

### 재는 자를 또 한 번 고쳤습니다

1단계를 **Lv.3** 으로 재고 있었습니다. 슬라임은 **Lv.1 에** 만납니다.
그래서 "1단계 95% ✓" 를 띄워 놓고도 첫 싸움은 **33%** 였습니다.

```
목표는 맞았는데 사람은 첫 슬라임에게 세 번 중 두 번을 졌습니다.
```

이제 1~4단계를 **그 땅에 처음 발을 들이는 레벨**로 잽니다(Lv.1 · 3 · 5 · 8).
그러면 목표를 맞추는 것이 곧 "들어서자마자 해 볼 만하다" 가 됩니다.
4단계는 아예 재지 않고 있어서 표에 새로 넣었습니다.

### 표 밖에서 바뀐 것

| 어디 | 무엇을 |
|---|---|
| `server/store.js` | `del` · `delDoc` (두 저장소 모두) |
| `server/world.js` | `deleteAccount` — 계정 · 우편함 · 랭킹 기록까지 |
| `server/server.js` | `/api/admin/account-delete` |
| `src/ui/AdminPanel.js` | **계정** 단 |
| `src/core/Sound.js` | 보스 곡 |

**여전히 남아 있는 자리**

- 직업 격차 49%p — 전투 본능 8% 로 62 → 49 까지 내려왔습니다. 철벽 최대 5 → 4 가
  다음 손잡이입니다(철벽과 전투 본능은 **곱해집니다**).
- 마법사가 처집니다 — 10단계 15%, 20단계 23%.
- 1~4단계에서 **용사만 유독 낮습니다**(85 · 76 · 70 · 70%). 힘의 공격력 1 이
  스킬 없는 초반에 가장 크게 옵니다.


---

## 0.46 → 0.47 — 한 줄만 바뀌었습니다

받은 시트: `dragonfieldtables` (드라이브에 **하나만** 남아 있습니다 — 지난 판에 둘이던
것을 정리해 주셨습니다. 고맙습니다).

> ⚠ 다만 **또 새 문서였습니다.** 0.45 → 0.46 → 0.47 에서 세 판 연속으로 아이디가
> 바뀌어 `sheets/SOURCE.txt` 를 매번 고쳐 적고 있습니다.
> **같은 문서에 "덮어쓰기로 가져오기"** 를 쓰시면 이 줄을 다시는 안 고쳐도 됩니다.
> (새 문서를 만들면 서버의 `SHEET_ID`·`SOURCE.txt` 가 옛 문서를 계속 읽게 됩니다.)

### 가져온 것 — 사람이 시트에서 고친 값

| 탭 | 무엇을 | 전 → 후 |
|---|---|---|
| `skills` | 철벽 방어력 | 20% → **15%** |

설명 글(`방어력 +20%`)도 함께 고쳤습니다 — 값만 바뀌고 글이 그대로면 화면이 거짓말을 합니다.

### 그 한 줄이 한 일

```
                            0.46              0.47
15단계 보스     용사 43% · 사냥꾼 35%   →   용사 35% · 사냥꾼 35%   (나란해졌습니다)
20단계 보스     용사 65% · 사냥꾼 23%   →   용사 56% · 사냥꾼 23%
지하감옥 5층    용사 63% · 사냥꾼 14%   →   용사 51% · 사냥꾼 14%
직업 격차       최대 49%p               →   최대 37%p
```

두 판에 걸쳐 62 → 49 → **37%p** 로 내려왔습니다. 30%p 가 "한 직업이 정답이 되는" 선이니
이제 거의 다 왔습니다.

### 요청대로 손대지 않은 것

| 무엇 | 왜 |
|---|---|
| 용사 초반(힘의 공격력 1) | "초반 힘들게 유지" |
| 마법사 | "따로 손봐야 하니 아직은 유지" |
| 5~9단계 | "유지" |
| 지난 시즌 한 판만 보관 | "지난시즌만 보는게 좋아 유지" |

### 표 밖에서 바뀐 것

| 어디 | 무엇을 |
|---|---|
| `src/ui/BattleView.js` | 보스 등장 연출 |
| `src/core/Sound.js` | `bossroar` |
| `server/server.js` | 계정 목록에 `createdAt` · 만든 때순 정렬 |
| `src/ui/AdminPanel.js` | 목록에 만든 때 표시 |

**여전히 남아 있는 자리**

- 직업 격차 37%p — 마법사가 처지는 쪽이 이제 더 큽니다(10단계 15%, 20단계 23%).
  다음에 마법사를 보실 때 함께 재면 좋겠습니다.
- 5단계 보스가 목표보다 어렵습니다(-14%p) — 그대로 두기로 하신 자리입니다.


---

## 0.47 → 0.48 — 마법사, 그리고 **칸이 둘 늘었습니다**

받은 시트: `dragonfieldtables` (09-03 17:05 수정 · **뿌리 폴더**).

> ⚠ 또 새 문서였고, `dragonfield` 폴더에 같은 이름의 옛 문서가 하나 더 있습니다.
> 네 판 연속으로 `sheets/SOURCE.txt` 를 고쳐 적고 있습니다 —
> **같은 문서에 "덮어쓰기로 가져오기"** 를 쓰시면 이 일이 없어집니다.

### 가져온 것 — 사람이 시트에서 고친 값

| 탭 | 무엇을 | 전 → 후 |
|---|---|---|
| `skills` | 마력 증폭(`arcane_bolt`) 마법 피해 | 6% → **20%** |
| `skills` | 보호막 연장(`mana_shield`) | 최대 5 → **4** · 300 → **500** |
| `skills` | 메테오(`meteor`) | 최대 5 → **4** · 0.14 → **0.25** |

설명 글까지 함께 고쳐 두셔서 이번에는 손댈 것이 없었습니다.

### 그 셋이 한 일

```
                        0.47              0.48
5단계 보스     마법사 45%   →   60%   ✓
10단계 보스    마법사 15%   →   28%   ✓
15단계 보스    마법사 18%   →   30%   ✓
20단계 보스    마법사 23%   →   40%
지하감옥 5층   마법사 30%   →   43%   ✓
```

다섯 자리 중 **넷이 목표 안**에 들어왔습니다. 세 판에 걸쳐 용사를 내리고 마법사를 올려
직업 격차가 **62 → 49 → 37 → 33%p** 로 좁혀졌습니다.

이제 남은 쪽은 **사냥꾼**입니다 — 지하감옥 14%, 20단계 23%. 예전에 가장 세던 직업이
둘을 손보는 사이에 혼자 뒤에 남았습니다.

### ⚠ `monsters` 탭에 칸이 둘 늘었습니다

| 칸 | 무엇 |
|---|---|
| `등장대사` | 보스가 나타날 때 하는 말 |
| `쓰러질때` | 쓰러지며 하는 말 |

`전투그림` 뒤, `기타` 앞에 들어갑니다. **보스에게만 보여 줍니다** — 잡몹 줄은 비워 두세요.
비워 두면 대사만 안 붙고 연출은 그대로 돕니다.

여덟 보스에 미리 적어 두었습니다(정찰대장 · 발가르 · 강화된 둘 · 봉인된 장군 ·
감옥의 주인 · 고룡 둘). 마음에 안 드는 줄은 시트에서 고치시면 됩니다.

```bash
npm run sheets      # 칸 둘이 들어간 xlsx 를 새로 굽는다
# 드라이브의 **같은 문서**에 덮어쓰기로 가져오기
```

### 표 밖에서 바뀐 것

| 어디 | 무엇을 |
|---|---|
| `src/ui/BattleView.js` | 연출 셋을 한 틀로 — 등장 · 고룡 · 쓰러짐 |
| `src/core/Sound.js` | `dragonroar` · `bossfall` |
| `server/server.js` | `lastLoginAt` 적기와 내려 주기 |
| `server/sheets.js` | `monsters` 탭에 칸 둘 |

**여전히 남아 있는 자리**

- **사냥꾼이 처집니다** — 지하감옥 14%, 20단계 23%. 다음에 볼 자리입니다.
- 5단계 보스가 목표보다 어렵습니다(-14%p) — 그대로 두기로 하신 자리입니다.

---

## 0.48 → 0.49 — 시트에 고치신 것 없음

받은 시트: `dragonfieldtables` (09-03 17:06 수정 · **뿌리 폴더** · 아이디 `10Iy8m6…`).

이번에는 **`sheets/SOURCE.txt` 를 안 고쳤습니다** — 다섯 판 만에 처음입니다.
같은 문서에 그대로 두셨습니다.

```
node tools/sheets-diff.js
  monsters   ⚠ 제목 줄이 다릅니다 · 시트에 없는 칸: 등장대사, 쓰러질때
```

0.48 에서 늘린 두 칸이 아직 시트에 없다는 말이고, 나머지 아홉 탭은 저장소와
**한 줄도 다르지 않았습니다.** 그러니 이번 판에서 덮어써서 잃은 값은 없습니다.

> ⚠ 그래도 **이번 xlsx 는 꼭 올려 주세요.** 두 칸(`등장대사`·`쓰러질때`)이
> 없는 채로 서버가 당기기를 하면 그 자리에서 멈춥니다.

### 이번 판이 표에 한 일

| 탭 | 무엇을 | 전 → 후 |
|---|---|---|
| `monsters` | 악마 정찰대장(5단계 보스) 체력 | 103 → **150** |
| `monsters` | 악마 장군 발가르(10단계 보스) 체력 | 232 → **464** |
| `drops` | 동쪽 필드 1~20단계의 보석 줄 | **전부 뺐습니다** (늑대 사파이어 1% 만 남김) |

보스 둘은 **같은 땅의 잡몹보다 얇았습니다** — 발가르 232 vs 악마 병사 260,
정찰대장 103 vs 꼬마 악마 118. 맵 배율(×9.5)은 둘에게 똑같이 걸리므로
그 순서가 게임에도 그대로 남아 있었습니다. `node tools/boss-check.js` 가
이제 그것만 봅니다.

---

## 0.49 → 0.50 — 시트를 못 읽고 있었습니다 (시트 잘못이 아니었습니다)

받은 것: 운영자 창 화면 두 장. `표` 단에 붉은 글이 떠 있었습니다.

```
'classes' 탭 — classes 시트의 첫 줄이 제목 줄이 아닙니다.
첫 칸은 '직업' 이어야 합니다. 지금: '직업 warrior warrior warrior'
```

### 무엇이 일어난 것인가

시트의 첫 줄은 `직업 / 항목 / 값 / 설명` 로 **멀쩡합니다.** 틀린 것은 받아 오는 쪽입니다.

서버는 구글의 `gviz` 통로로 탭을 CSV 로 받습니다. 이 통로는 **제목 줄이 몇 줄인지
스스로 짐작합니다.** `classes` 탭은 첫 칸이 죽 글자입니다:

```
직업        ← 제목
warrior
warrior
warrior
```

구글은 이것을 "네 줄짜리 제목" 으로 보고 **한 줄로 이어 붙여** 돌려줬습니다.
그래서 첫 칸이 `직업 warrior warrior warrior` 가 된 것입니다.
다른 탭들은 첫 칸이 `id` 뒤에 `slime`·`bat` 처럼 제각각이라 이 일이 안 났습니다 —
**하필 `classes` 만** 걸린 이유입니다.

### 고친 것

주소에 `headers=1` 을 붙였습니다(`tools/sheets.js` · `server/sheetsync.js` 둘 다).
"제목은 언제나 한 줄" 이라고 못 박는 값입니다.

```
…/gviz/tq?tqx=out:csv&headers=1&sheet=classes
```

그리고 같은 일이 또 나면 오류 글이 짚어 주도록 했습니다 —
"제목과 아랫줄이 붙어 왔습니다. 구글이 제목 줄을 여러 줄로 잘못 읽은 것입니다."

> **다음에 시트를 못 읽는다고 하실 때 먼저 볼 것:** 오류에 적힌 **첫 칸의 값**입니다.
> 거기에 제목과 데이터가 붙어 있으면 시트가 아니라 통로 문제입니다.

---

## 0.50 → 0.51 — 문서가 또 바뀌었습니다 (여섯 번째)

`401` 이 떴습니다. 원인은 공개 설정이 아니라 **문서 자체가 없어진 것**이었습니다.

```
드라이브 확인 → 10Iy8m6…  "Requested entity was not found"
              → 19yz0jyl…  (09-04 02:06 만듦, anyone: reader) ← 새 문서
              → 1GYsl-Cc…  (09-04 01:21 만듦) ← 같은 이름의 문서가 또 하나
```

`sheets/SOURCE.txt` 를 `19yz0jyl…` 로 바꿨습니다. 내용은 0.50 통합문서 그대로라
**가져올 수정은 없었습니다**(`sheets-diff` — 다른 곳이 없습니다).

### 그런데 이번엔 파일만 고치고 끝내지 않았습니다

0.45 · 0.46 · 0.47 · 0.48 · 0.51 — **여섯 판 중 다섯 번**을 같은 이유로 고쳤습니다.
같은 손질을 여섯 번 하고 있다면 고칠 것은 값이 아니라 **길**입니다.

그래서 **운영자 창 표 단에서 시트 주소를 바꿀 수 있게** 했습니다(0.51).
다음에 문서가 바뀌면 저는 아무것도 안 해도 되고, 사용자가 표 단에 주소를 붙여 넣고
`주소 저장` 을 누르면 끝입니다. 순서는 **운영자 창 > 환경변수 > SOURCE.txt** 입니다.

> **다음 판에도 SOURCE.txt 를 고쳐야 하면**, 그건 이 기능을 안 쓰고 계신다는 뜻입니다.
> 그때는 파일을 고치기 전에 **표 단에서 바꾸는 길**을 먼저 안내하세요.

---

## 0.51 → 0.52 — `이름은 비울 수 없습니다` 는 시트 잘못이 아니었습니다

받은 것: 운영자 창 화면 두 장. `401` 은 사라졌고(0.51 이 올라갔다는 뜻), 대신:

```
'classes' 탭 — classes 시트 2번째 줄: 이름은 비울 수 없습니다.
```

### 어떻게 알아냈나

드라이브에서 통합문서를 받아 `classes` 탭 2번째 줄을 직접 봤습니다:

```
warrior, 이름, 용사, 화면에 나오는 직업 이름
```

**멀쩡했습니다.** 그러면 지운 것은 받아 오는 쪽입니다.

`gviz` 는 **열마다 자료형을 정하고, 안 맞는 칸을 빈칸으로 지웁니다.**
`classes` 의 `값` 열은 글자(`용사`·`예`)와 숫자(`48`·`11`)가 섞여 있고 숫자가 더 많습니다.
그래서 숫자 열로 보고 `용사` 를 지웠습니다.

> **같은 통로에서 두 번 다른 이유로 막혔습니다.**
> 0.50 — 제목 줄이 몇 줄인지 짐작(`직업 warrior warrior warrior`).
> 0.52 — 열의 자료형을 짐작(`용사` 를 지움).
> 두 번째부터는 그 통로를 고칠 것이 아니라 **버려야 합니다.**

### 고친 것

`export?format=xlsx` — 통합문서 한 장을 그대로 받습니다. 짐작하는 자리가 없습니다.
xlsx 읽기는 `server/xlsxread.js` 에 새로 썼습니다(의존성 0개 원칙 그대로, zlib 만 씁니다).
굽는 쪽(`tools/make-xlsx.js`)과 읽는 쪽이 서로를 시험합니다(`/tmp/batch37.js`).

덤: 요청이 **열 번에서 한 번**으로 줄었고, 탭을 반쯤 읽다 마는 상태가 없어졌습니다.

### 가져온 것 — 사람이 시트에서 고친 값

| 탭 | 무엇을 | 전 → 후 |
|---|---|---|
| `monsters` | 슬라임 체력 | 44 → **24** |
| `drops` | 슬라임의 약초 | 25% 1개 → **100% 5개** |

> 시트 연결이 되는지 보려고 넣으신 값 같습니다. 그래도 **규칙대로 가져왔습니다** —
> 시트가 저장소보다 셉니다. 시험용이었다면 시트에서 되돌리시라고 알려 드렸습니다.

---

## 0.52 → 0.53 — 시트 연결이 살아났습니다

받은 시트: `dragonfieldtables` (`19yz0jyl…` · 09-04 06:57 수정). **문서는 그대로**입니다.

### 가져온 것 — 사람이 시트에서 고친 값

| 탭 | 무엇을 | 전 → 후 |
|---|---|---|
| `monsters` | 슬라임 체력 | 24 → **44** (0.52 에 시험 삼아 내리셨던 것을 되돌림) |
| `drops` | 슬라임의 약초 | 100% 5개 → **25% 1~2개** |
| `items` | 낡은 몽둥이 공격력 | 5 → **7** |

**0.52 의 통합문서 통로가 실제로 도는 것이 이걸로 확인됐습니다** — 시트에서 고친 값이
그대로 넘어왔고, `classes` 탭도 더는 막지 않습니다.

> 슬라임 체력을 되돌리셨으므로 `/tmp/batch31.js` 의 잣대도 44 로 되돌렸습니다.
> **시트가 저장소보다 세다**는 규칙 탓에, 시험에 박아 둔 숫자는 시트를 따라 움직입니다.
> 시트에서 값을 고치실 때마다 그 값을 박아 둔 시험이 빨개질 수 있습니다 — 정상입니다.

---

## 0.53 → 0.54 — 시트에서 가져올 것이 없었습니다

받은 시트: `pull-0.54.xlsx` (09-04 07:44 받음). **문서는 그대로**입니다.

`node tools/sheets-diff.js` 로 견주었을 때 **시트에만 있는 값이 하나도 없었습니다** —
다른 곳은 전부 이번 판에서 저장소가 새로 만든 것이었습니다. 그래서 가져올 것이 없었고,
`npm run sheets` 로 저장소 쪽을 내보냈습니다.

### 이번 판에서 **칸 이름이 바뀐 곳** — 시트를 반드시 갈아 끼우세요

| 탭 | 옛 이름 | 새 이름 | 왜 |
|---|---|---|---|
| `classes` | `보호막 무적 시간` (밀리초) | **`보호막 막는 횟수`** (대) | 건너뛰기를 하면 타임라인 시간이 순식간에 흘러서, 같은 값인데도 판마다 막는 수가 달랐습니다 |
| `skills` | `보호막 지속` | **`보호막 막는 횟수`** | 같은 이유 |
| `skills` | `위기의 피해 증가` | **`위기의 치명타 피해`** | 이제 주는 피해가 아니라 **치명타 피해**를 올립니다 |

> 칸은 **자리(열)로 읽습니다.** 이름만 바뀐 것이라 옛 시트를 그대로 두어도 값은 읽히지만,
> 화면에 적히는 이름이 실제로 하는 일과 어긋납니다. `npm run sheets` 로 구운 새 xlsx 를
> **같은 문서에 덮어쓰기로 가져오기** 하세요.

### 이번 판에서 늘어난 줄

| 탭 | 무엇 |
|---|---|
| `items` | 투구 넷 — `cloth_hood` · `leather_cap` · `knight_helm` · `rune_helm` |
| `items` | `magic_helm` 의 `기타` 칸 — `{"mods":{"sightBonus":2,"dungeonStealth":1}}` |
| `drops` | `skeleton` 이 `knight_helm` 을 2% 로 |

> `기타` 칸은 **JSON 그대로** 읽습니다. 시트에서 이 칸을 지우면 매직 투구의
> 시야·은신이 통째로 사라집니다 — 값만 고치고 칸은 비우지 마세요.

---

## 0.55 → 0.56 — 저장소가 시트보다 한 칸 앞섭니다

이번 판에서 표를 딱 **한 칸** 고쳤습니다.

| 탭 | 무엇을 | 전 → 후 |
|---|---|---|
| `classes` | 마법사 · **끌어오기 범위** | 3 → **4** (칸) |
| `classes` | 마법사 · 전투설명 3 | "주변 3칸의…" → "주변 **4칸**의…" |

> **시트를 갈아 끼우기 전에는 이 값이 되돌아갑니다.** 서버가 시트를 읽는 순간
> 시트에 적힌 3 이 저장소의 4 를 덮습니다 — 이 저장소의 규칙이 "시트가 더 세다" 이기 때문입니다.
> `npm run sheets` 로 구운 `dragon-field-tables.xlsx` 를 **같은 문서에 덮어쓰기로 가져오기** 하세요.

### 표가 **아닌** 것들 (시트와 무관)

전투 자세 그림 · 화살 각도 · 화살통 위치 · 파이어볼 · 이펙트 크기 · 용사 얼굴은
전부 `tools/art-*.js` 와 `src/scenes` 에 있습니다. 시트에는 없습니다.

---

## 0.57 → 0.58 — 시트에서 손본 것을 가져왔습니다

받은 통합문서: `dragonfieldtables` (수정 시각 2026-09-04 08:00) → `sheets/inbox/pull-0.58.xlsx`

**가져온 것 — 사람이 시트에서 고친 값** (`--take drops,quests`)

| 탭 | 무엇 |
|---|---|
| `drops` | **`elite_wolf` 가 `gem_ruby` 를 1% 로** 떨군다 (시트에만 있던 줄) |
| `quests` | `hunt_sold` EXP 5000 → **3000**, 보상 화염검 1 → **마력석 50** |
| `quests` | `boss_gen` EXP 10000 → **5000**, 보상 용린갑 1 → **악마의 핵 30** |
| `quests` | `deep_open` EXP 20000 → **10000**, 수량 5 → **20** |
| `quests` | `deep_stone` 30000 → **15000** · `deep_cap` 50000 → **20000** |
| `quests` | `deep_core` 70000 → **30000** · `deep_gen` 100000 → **50000** |
| `quests` | `dragon_scrap` EXP 0 → **100000** |

**저장소를 지킨 것 — 지난 판에서 요청받아 바꾼 값**

| 탭 | 무엇 | 언제 |
|---|---|---|
| `classes` | 마법사 끌어오기 4칸 | 0.56 "마법사의 몬스터끌어오기는 4칸으로 올리고" |
| `items` | 투구 넷 + `magic_helm` 의 시야·은신 | 0.54 "모든 투구이미지를 만들고", "지하감옥에서만 몹이 인식을 못하는 효과" |
| `drops` | `skeleton` → `knight_helm` 2% | 0.54 (투구를 얻을 자리) |
| `skills`·`traits` | 위기의 치명타 피해 · 보호막 막는 횟수 · 마력 증폭 합연산 | 0.54 |

**가른 방법:** 한 판 전의 통합문서(`pull-0.54.xlsx`)와도 견주었습니다.
0.54 판에는 `quests` 차이도 `elite_wolf` 줄도 **없었습니다** — 그러니 그 둘은
0.54 이후 사람이 시트에서 새로 손본 것입니다. 나머지 차이는 0.54 작업 기록에 이유가 적혀 있습니다.

---

## ⚠ 받은 한 장짜리 html 은 시트를 **따라가지 않습니다** (구조입니다)

"다운받은 html 이 구글 시트와 다르다" — 맞습니다. 그리고 고칠 수 있는 종류가 아닙니다.

| 어떻게 돌리나 | 표를 어디서 읽나 | 시트를 고치면 |
|---|---|---|
| **한 장짜리 `dragon-field.html`** | **파일 안에 박혀 있다** | 안 바뀐다 — 다시 구워야 한다 |
| 서버 (`node server/index.js`) | `server/content/` | 반영된다 (서버가 시트에서 갱신) |
| 개발 (`npx serve` 등) | `src/data/*.json` | `sheets.js import` 하면 반영된다 |

`tools/build-single-file.js` 는 `items.json` · `quests.json` 같은 표를 **esbuild 로 통째로
끼워 넣어** 굽습니다(`loader: { '.json': 'json' }`). 그래서 그 파일은 **구운 순간의 사진**입니다.
인터넷도 서버도 없이 파일 하나로 돌아가는 것이 그 파일의 값어치이고, 그 대가입니다.

**0.58 부터는 그 사진을 언제 찍었는지 화면에 찍습니다.** 접속 화면 오른쪽 아래 —

```
표 2026-09-05 15:27      v0.58.0
```

시트를 고친 시각이 이 시각보다 **뒤**라면, 그 파일은 옛 표를 들고 있는 것입니다.
`npm run build` 로 다시 구우세요. (서버로 돌릴 때는 이 표시가 안 나옵니다 — 그쪽은 시트를 따라가므로
새길 시각이 없습니다.)
