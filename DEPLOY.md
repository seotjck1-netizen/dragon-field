# 인터넷에 올려서 같이 놀기

이 문서만 따라 하면 **주소 하나**가 생깁니다.
그 주소를 친구에게 보내면, 각자 자기 컴퓨터·폰에서 접속해 **같은 세계에서 함께** 놉니다.

돈은 들지 않습니다. 카드도 필요 없습니다.

> **이미 예전 버전을 올려 두었다면** — 처음부터 다시 할 필요 없습니다.
> **[UPGRADE.md](UPGRADE.md)** 를 보세요. 새 코드로 갈아 끼우고, Render 가 그걸
> 집어 가게 하고, 구글 시트를 붙이는 순서만 적혀 있습니다.

---

## 무엇을 만들게 되나

두 곳에 가입합니다. 각각 하는 일이 다릅니다.

| | 무엇 | 왜 |
|---|---|---|
| **Render** | 게임 서버 | 주소를 만들어 주고, 접속자끼리 위치를 주고받게 해 줍니다 |
| **Upstash** | 데이터베이스 | 캐릭터를 보관합니다 |

**왜 두 곳이나?** Render 무료 요금제는 15분간 아무도 접속하지 않으면 서버를 재웁니다.
깨어날 때 서버 안의 파일이 **전부 처음 상태로 돌아갑니다.** 캐릭터를 서버 파일에 두면
잠들 때마다 사라진다는 뜻입니다. 그래서 캐릭터만 바깥(Upstash)에 둡니다.

---

## 0단계 — GitHub 에 올리기 (10분)

Render 는 GitHub 저장소를 읽어서 배포합니다. 먼저 이 폴더를 올려야 합니다.

1. [github.com](https://github.com) 에 가입하고 로그인합니다.
2. 오른쪽 위 **+** → **New repository**.
3. 이름은 아무거나 (`dragon-field` 등). **Private 로 두어도 됩니다.**
4. **Create repository** 를 누르면 나오는 안내 중
   *"…or push an existing repository from the command line"* 부분을 씁니다.

이 폴더에서 터미널을 열고:

```bash
git init
git add .
git commit -m "드래곤 필드"
git branch -M main
git remote add origin https://github.com/내아이디/dragon-field.git
git push -u origin main
```

> `.gitignore` 가 이미 들어 있어서 **계정 파일(`server/data/`)은 올라가지 않습니다.**

---

## 1단계 — Upstash: 캐릭터 보관함 만들기 (5분)

1. [upstash.com](https://upstash.com) → **Sign Up** (GitHub 계정으로 바로 가입됩니다).
2. **Create Database** 를 누릅니다.
3. 이름은 아무거나. **Region 은 가까운 곳**(`ap-northeast-1` 도쿄 등)을 고릅니다.
4. 요금제는 **Free** 를 고릅니다.
5. 만들어진 데이터베이스를 열고 **REST API** 칸을 찾습니다.
   두 줄을 복사해 메모장에 붙여 둡니다.

```
UPSTASH_REDIS_REST_URL    https://xxxxx.upstash.io
UPSTASH_REDIS_REST_TOKEN  AXXXxxxx...(아주 긴 글자)
```

> 이 토큰은 데이터베이스의 열쇠입니다. 남에게 보여 주지 마세요.
> 저장소에 올리지도 마세요 — 다음 단계에서 Render 화면에 직접 붙여 넣습니다.

---

## 2단계 — Render: 서버 띄우기 (5분)

1. [render.com](https://render.com) → **Get Started**(GitHub 계정으로 가입).
2. 대시보드에서 **New +** → **Blueprint**.
3. 0단계에서 만든 저장소를 고릅니다.
   → 이 폴더의 `render.yaml` 을 읽어 설정을 알아서 잡아 줍니다.
4. **환경변수 두 개를 물어봅니다.** 1단계에서 복사해 둔 값을 붙여 넣습니다.
   - `UPSTASH_REDIS_REST_URL`
   - `UPSTASH_REDIS_REST_TOKEN`
5. **Apply** 를 누릅니다.

### 더 넣어 두면 좋은 것 (선택)

Render → **Environment** 에서 나중에 추가할 수 있습니다.

| 이름 | 무엇 | 안 넣으면 |
|---|---|---|
| `ADMIN_KEY` | 이벤트 우편을 보낼 때 쓰는 열쇠 | **이벤트 발송이 아예 막힙니다** |
| `REGISTER_LIMIT` | 한 IP 에서 시간당 만들 수 있는 계정 수 | 5개. 친구 여럿이 같은 공유기에서 한꺼번에 만들면 걸립니다 |
| `MAX_ACCOUNTS` | 이 서버의 계정 수 상한 | 500개 |
| `DRAGON_EVERY_MS` | 고룡이 오는 주기(ms) | 30분 |
| `DRAGON_STAY_MS` | 고룡이 머무는 시간(ms) | 25분 |
| `DRAGON_HP` | 고룡의 체력 | 700000 |
| `DRAGON_EXP` | 고룡을 눕혔을 때 나눠 줄 경험치(기여도 비율대로) | 400000 |
| `DRAGON_GOLD` | 같은 방식으로 나눠 줄 골드 | 300000 |

`ADMIN_KEY` 는 **아무에게도 알려 주지 마세요.** 그 값을 아는 사람은
전 유저에게 아이템을 뿌릴 수 있습니다. 그래서 비워 두면 발송 경로가 아예 닫힙니다.

이벤트 우편은 내 컴퓨터에서 서버 주소를 가리켜 보냅니다.

```bash
SERVER=https://내서버.onrender.com ADMIN_KEY=열쇠 \
  node tools/event.js "추석 선물" "즐거운 명절 보내세요!" potion:20
```

2~3분 뒤 위쪽에 주소가 나옵니다.

```
https://dragon-field-xxxx.onrender.com
```

**이 주소가 게임 주소입니다.** 친구에게 그대로 보내세요.

---

## 3단계 — 잘 올라갔는지 확인 (1분)

주소 뒤에 `/api/ping` 을 붙여 브라우저로 열어 봅니다.

```
https://내주소.onrender.com/api/ping
```

이렇게 나오면 성공입니다.

```json
{"ok":true,"name":"poino-server","version":2,"storage":"upstash","players":0}
```

**`"storage":"upstash"` 인지 꼭 확인하세요.**
`"file"` 이라고 나오면 Upstash 값이 안 들어간 것이고, **서버가 잠들 때마다 캐릭터가 사라집니다.**
Render 대시보드 → 그 서비스 → **Environment** 에서 두 값을 다시 확인하세요.

서버 로그(Render 의 **Logs** 탭)에도 상태가 찍힙니다.

```
계정 저장    : Upstash (바깥 데이터베이스)
세션 서명 키 : 환경변수에서 읽음
```

`⚠ 확인하세요` 가 떠 있으면 그 내용을 그대로 고치면 됩니다.

---

## 알아 두어야 할 것

### 처음 접속이 느립니다

15분간 아무도 안 들어오면 서버가 잠듭니다.
그 뒤 첫 사람은 **1분쯤** 하얀 화면을 봅니다. 두 번째 사람부터는 바로 들어옵니다.
누군가 놀고 있는 동안에는 잠들지 않습니다.

친구들과 시간을 정해 두고 들어가면 한 명만 기다리면 됩니다.

### 계정은 게임 전용 비밀번호로

이 게임의 아이디·비밀번호는 **세이브 슬롯을 구분하기 위한 것**이지 은행 계정이 아닙니다.
공개 배포를 견디도록 손봐 두긴 했습니다.

- 비밀번호는 브라우저에서 한 번, 서버에서 소금과 함께 또 한 번 굳혀서 보관합니다.
  받은 값을 그대로 저장하지 않습니다.
- 접속하면 비밀번호와 무관한 토큰을 따로 발급합니다.
- 가입·로그인·저장에 횟수 제한이 걸려 있어 기계로 찍어 볼 수 없습니다.
- `server/` 폴더는 바깥에서 절대 받아 갈 수 없습니다.

그래도 **다른 곳에서 쓰는 비밀번호를 재사용하지 마세요.** 접속 화면에도 그렇게 적혀 있습니다.

### 아무나 들어옵니다

주소를 아는 사람은 누구나 계정을 만들 수 있습니다. 막고 싶다면 두 가지 방법이 있습니다.

- **계정 수를 줄인다** — Render 의 `MAX_ACCOUNTS` 를 친구 수만큼(예: `8`) 낮춥니다.
  그 수가 차면 더 이상 가입되지 않습니다.
- **다 모인 뒤에 잠근다** — 다 같이 계정을 만든 다음 `MAX_ACCOUNTS` 를 `0` 으로 바꿉니다.
  기존 계정은 그대로 쓰고 새 가입만 막힙니다.

### 게임 내용을 고치고 싶을 때 — 두 가지 길

**① GitHub 에 올리기** — 코드까지 바뀐 경우.

```bash
node tools/sheets.js check     # 표에 문제가 없는지
git add . && git commit -m "밸런스 조정" && git push
```

Render 가 알아서 다시 배포합니다(2~3분). 접속해 있던 사람에게는
"새 콘텐츠가 배포되었습니다" 알림이 뜹니다.

**② 구글 시트에서 숫자만 고치기** — GitHub 을 거치지 않습니다. 이쪽이 훨씬 빠릅니다.

```bash
npm run sheets      # sheets/*.csv 아홉 개 + dragon-field-tables.xlsx
```

만들어진 `dragon-field-tables.xlsx` 를 **구글 드라이브에 끌어다 놓고 더블클릭**하면
탭 아홉 개짜리 시트가 됩니다. 그 시트를 "링크가 있는 모든 사용자 — 뷰어"로 공개하고,
Render 의 **Environment** 에 `SHEET_ID` 로 주소를 넣으면 끝입니다.

| 탭 | 무엇을 고치나 |
|---|---|
| `items` | 장비 · 소모품 · 재료 (능력치 · 가격 · 등급) |
| `monsters` | 몬스터 능력치 · 경험치 · 골드 |
| `drops` | 무엇이 몇 % 로 떨어지나 |
| `quests` | 목표와 보상 |
| `skills` | 직업별 스킬과 효과 |
| `classes` | 기본 능력치 · 성장 · 패시브 · **힘/민첩/지능 성장주기** |
| `traits` | 포인트로 찍는 특성 여섯 갈래 |
| `stats` | 힘 · 민첩 · 지능이 1점당 주는 것 |
| `config` | 포인트를 몇 레벨마다 주나 |

그 뒤로는 **시트에서 숫자만 고치면** 서버가 10분마다 읽어 갑니다.
자세한 것은 → **[SHEETS.md](SHEETS.md)**

> 시트를 쓰더라도 GitHub 저장소는 그대로 두세요. Render 는 코드를 거기서 읽어 옵니다.
> 시트는 **표(숫자)만** 덮어씁니다.

---

## 안 될 때

| 증상 | 원인과 해결 |
|---|---|
| 주소를 열면 계속 로딩만 함 | 서버가 깨어나는 중입니다. 1분 기다려 보세요. |
| `/api/ping` 이 `"storage":"file"` | Upstash 환경변수가 안 들어갔습니다. Render → Environment 확인. |
| 접속 화면에 "이 브라우저 안에만" 이라고 뜸 | 게임이 서버를 못 찾았습니다. 주소가 `https://` 로 시작하는지, `/api/ping` 이 열리는지 확인하세요. |
| 캐릭터가 사라짐 | `"storage":"file"` 인 채로 서버가 잠들었던 것입니다. 위 항목을 고치세요. |
| 서로가 안 보임 | 화면 위 표시를 보세요. **🌐 함께 접속 N명** 이면 연결된 것이고 같은 맵에 있어야 보입니다. **🖥 혼자 (서버 없음)** 이면 그 기기가 서버를 못 찾은 것입니다 — 아래 칸을 보세요. |
| 한쪽에 **🖥 혼자 (서버 없음)** | 그 기기는 서버가 잠들어 있는 동안 들어와서 자기 브라우저 안에 계정을 만든 것입니다. 서버가 깬 뒤 **새로고침**하면 됩니다. 그 기기에서 만든 캐릭터는 그 브라우저에만 남습니다. |
| **⚠ 연결 끊김** 이 오래 감 | 폰이 절전으로 들어갔거나 와이파이가 바뀐 것입니다. 잠시 두면 스스로 다시 붙습니다. 안 붙으면 새로고침하세요. |
| Render 로그에 `계정 저장소에 연결하지 못했습니다` | Upstash 주소나 토큰이 틀렸습니다. 복사할 때 앞뒤 공백이 섞이지 않았는지 보세요. |

---

## 인터넷에 올리지 않고 같이 놀기

같은 와이파이(집·사무실)에 있다면 가입할 필요가 없습니다.
내 컴퓨터에서 서버를 켜고 주소만 알려 주면 됩니다.

```bash
node server/server.js
```

켜지면 화면에 폰에서 칠 주소가 그대로 찍힙니다 (`http://192.168.0.x:8787`).
이 방식은 **내 컴퓨터가 켜져 있는 동안만** 접속됩니다.
캐릭터는 `server/data/accounts.json` 에 남습니다.
