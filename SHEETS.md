# 표를 스프레드시트에서 고치기

아이템 값 하나 바꾸자고 JSON 을 열 필요는 없습니다.
아래 **여덟 가지**는 엑셀이나 구글 시트에서 고칠 수 있습니다.

| 시트 | 무엇을 고치나 |
|---|---|
| `items` | 아이템 — 이름 · 능력치 · 가격 |
| `monsters` | 몬스터 — 레벨 · 능력치 · 경험치 |
| `drops` | 드랍표 — 어느 몬스터가 무엇을 얼마나 떨구나 |
| `quests` | 퀘스트 — 조건 · 보상 · 순서 |
| `skills` | 스킬 — 효과 · 최대 단계 · **어느 직업이 배우나** |
| `classes` | 직업 — 기본 능력치 · 성장 · **패시브와 전투 방식** |
| `traits` | 특성 — 힘/민첩/지능이 1점당 무엇을 올려 주나 |
| `config` | 특성·스킬 포인트를 몇 레벨에 몇 점 주나 |

방법은 두 가지입니다. 하나만 골라 쓰세요.

| | 어떻게 | 언제 |
|---|---|---|
| **A. 파일로 주고받기** | CSV 로 꺼내 → 고쳐 → 다시 넣는다 | 혼자 작업할 때. 준비할 게 없다 |
| **B. 구글 시트에 연결** | 시트에서 고치면 서버가 알아서 읽어 간다 | 여러 명이 고칠 때. 접속 중에도 반영된다 |

---

> **이미 Render 에 올려 둔 서버가 있다면** — 시트를 붙이는 순서만 따로 정리해 두었습니다.
> **[UPGRADE.md](UPGRADE.md)** 의 C 절을 보세요.

## A. 파일로 주고받기

### 1) 꺼내기

```bash
node tools/sheets.js export
```

`sheets/` 폴더에 아홉 개가 생깁니다.

```
sheets/items.csv      아이템          장비 · 소모품 · 재료
sheets/monsters.csv   몬스터          능력치 · 경험치 · 골드
sheets/drops.csv      드랍표          무엇이 몇 % 로 떨어지나
sheets/quests.csv     퀘스트          목표 · 보상
sheets/skills.csv     스킬            직업별 스킬과 효과
sheets/classes.csv    직업·패시브     기본 능력치 · 성장 · 패시브 · 스탯 성장주기
sheets/traits.csv     특성            포인트로 찍는 여섯 갈래
sheets/stats.csv      스탯            힘 · 민첩 · 지능 (레벨이 올려 주는 셋)
sheets/engrave.csv    각인            등급별 각인 범위 · 붙을 확률 · 초월 확률
sheets/config.csv     포인트 설정     포인트를 몇 레벨마다 주나
```

엑셀이나 구글 시트로 그대로 열립니다. (한글이 깨지지 않게 만들어 둡니다)

**한 번에 통합문서로 만들려면:**

```bash
npm run sheets        # export + 통합문서 굽기, 한 번에
# 또는
node tools/make-xlsx.js
```

`dragon-field-tables.xlsx` 하나에 **탭 열 개**가 들어갑니다.
구글 드라이브에 끌어다 놓고 더블클릭하면 시트로 열리고, **탭 이름도 그대로** 붙습니다.

### 2) 고치기

숫자를 고치거나, 맨 아래에 줄을 하나 더 붙이면 그게 새 아이템입니다.

### 3) 넣기 전에 — 전투가 어떻게 달라지는지 미리 본다

```bash
node tools/balance.js sheets          # 아직 안 넣은 시트로 미리 재 본다
node tools/balance.js sheets --diff   # 넣기 전후를 나란히
```

게임이 쓰는 진짜 전투 계산기를 그대로 돌리므로, 여기 나온 숫자가 곧 게임의 숫자입니다.

```
  상대                              레벨/강화     용사  사냥꾼  마법사   목표 대비
  5단계 보스 · 악마 정찰대장        Lv.15 +5       56%     54%     39%   목표 50% ✓
  20단계 보스 · 강화된 발가르       Lv.40 +9       24%     17%     24%   목표 20% ✓
```

각 줄의 **목표 승률**은 `tools/balance.js` 의 `MATCHES` 에 적혀 있습니다.
세 직업 평균이 목표에서 ±10%p 안이면 `✓`, 벗어나면 얼마나 벗어났는지 적힙니다.

| 승률 | 뜻 |
|---|---|
| 0% | 아무리 해도 못 이긴다 — 벽 |
| **20~70%** | 몇 번 도전하면 이긴다 — **보스로 알맞다** |
| 100% | 한 번도 안 진다 — 없는 것과 같다 |

직업끼리 **30%p** 넘게 벌어지면 한 직업이 정답이 되어 버립니다.

옵션:

```bash
node tools/balance.js --lv 40 --enh 7   # 그 구간만
node tools/balance.js --trash           # 보스 말고 잡몹
node tools/balance.js --n 200           # 표본을 늘린다(느리지만 정확)
node tools/balance.js --solve           # 목표 승률에 닿는 능력치를 거꾸로 찾아 준다
node tools/balance.js --solve --hp-only # 체력만 건드려서 찾는다 (0.35)
```

#### `--solve` — 목표만 정하고 숫자는 맡기기

"5단계 보스는 반쯤 이겼으면 좋겠다"처럼 **원하는 승률**만 정해 두면,
거기에 닿는 hp·공격·방어를 이분 탐색으로 찾아서 알려 줍니다.

```
  5단계 보스 · 악마 정찰대장        ×0.3583     57%     54%     39%   목표 50%
                                    hp 1626→583 · 공격 109→62 · 방어 48→34   (맵 보정 ×1.85 적용해서 잰 값)
```

체력만 늘리면 "오래 걸릴 뿐 절대 안 지는 자루"가 되고 셋을 똑같이 늘리면
"때려도 안 죽는 벽"이 되므로, hp 는 그대로 ×k, 공격은 ×k^0.55, 방어는 ×k^0.35 로
서로 다르게 밉니다. 나온 값을 `sheets/monsters.csv` 에 적고 `check` → `import` 하면 끝입니다.

#### `--hp-only` — 보스를 약하게 만들 때만 (0.35)

보스를 **약하게** 만드는 방향(k < 1)으로 여러 번 풀면 공격력이 `k^0.55` 로 계속 깎여,
어느 순간 **보스가 제 땅의 잡몹보다 약해집니다.** 실제로 10단계 보스의 전투력이
4,183 까지 내려가 같은 땅 잡몹 5,243 아래로 들어간 적이 있습니다 — 사람 눈에는
"보스방에 들어갔더니 밖보다 편하다"로 보입니다.

`--hp-only` 를 붙이면 **hp 만** 밀고 공격·방어는 그대로 둡니다. 보스가 때리는 힘은
지키면서 버티는 시간만 조절하는 셈이라, 이 함정에 빠지지 않습니다.
**보스를 다시 잡을 때는 이쪽을 기본으로 쓰세요.**

확인은 `node tools/zone-power.js` — 보스 전투력이 다음 땅 잡몹보다 낮은지 봅니다.

---

## 0.37 에서 늘어난 칸

| 탭 | 칸 | 뜻 |
|---|---|---|
| `quests` | **열림조건** | 비어 있으면 여느 때처럼 차례대로 이어지는 줄. 값이 있으면 **특별 의뢰**가 되어 차례에서 빠지고, 그 조건을 채운 사람에게만 열린다. 지금 쓸 수 있는 값: `met:<몬스터id>`(그 상대를 한 번이라도 만났으면) |
| `classes` | **마법 전환 배율** | 주는 피해를 전부 마법으로 바꾸면서 곱하는 값. 마법사가 `1.1`. 비우면 1(그대로) |

> ⚠ **칸이 늘었으므로 시트를 다시 올려야 합니다.** 서버는 칸을 **자리로** 읽습니다.
> `npm run sheets` 로 새 xlsx 를 구워 갈아 끼우세요.

---

## 두 번째 고룡 검사 — `tools/dragon2.js` (0.37)

```bash
node tools/dragon2.js [--n 30]
```

`tools/balance.js` 는 **평소 사냥**을 재는 도구라, 조건부 세트 효과
(용린 4세트 — 고룡과 싸울 때만 켜지는 몫)를 일부러 끕니다. 그런데 이 싸움은
**그 효과를 켜고 하는 싸움**입니다. 끈 채로 재면 0% 가 나오고 "잡을 수 없는 상대"
라는 잘못된 결론이 납니다.

이 도구는 용린 한 벌을 갖춘 몸으로, 그 효과를 켜고 잽니다. 그리고 묻는 것이 다릅니다 —
**"이기나"가 아니라 "몇 번 물어뜯으면 눕나"** 입니다(상처가 남는 상대이므로).

```
    warrior  한 판에  23.8%  →  5번쯤 물어뜯으면 눕는다
    ranger   한 판에  55.2%  →  2번쯤 물어뜯으면 눕는다
```

용린 없이 룬 한 벌로도 재서, **세트가 정말 필요한지**까지 함께 봅니다
(지금은 룬만으로는 25~48번이라 사실상 못 눕힙니다 — 의도한 대로입니다).

---

## 장비 사다리 검사 — `tools/item-audit.js`

```bash
node tools/item-audit.js            # 강화 +0 기준
node tools/item-audit.js --enh 9    # +9 기준(등급별 강화 배율이 적용된 뒤)
```

부위·계열마다 값싼 것부터 줄을 세우고, **뒤 단계가 앞 단계보다 약하면 ⚠** 로 잡습니다.
장비 하나만으로는 전투력이 나오지 않으므로(HP 0 이면 0), 표준 캐릭터에 그 장비만
갈아 끼우고 전투력이 얼마나 오르는지를 잽니다.

계열끼리의 비교(지팡이가 검보다 센가)는 이 도구가 답하지 않습니다 —
전투력 공식은 마법 학파도 직업별 치명타 확률도 모릅니다.
**계열 사이 균형은 `tools/balance.js` 가 정답입니다.**

---

## 구간 설계 — `tools/zone-power.js` · `tools/zone-solve.js`

보스가 있는 단계의 문을 지나면 새 구간이 시작됩니다. 그 첫 땅이 방금 잡은 보스보다
약하면 난이도가 푹 꺼지고, 힘들게 문을 연 보람이 없습니다.

```bash
node tools/zone-power.js            # 문턱이 살아 있는지 검사만
node tools/zone-solve.js            # 목표 전투력이 나오는 맵 배율을 계산
node tools/zone-solve.js --write    # tools/gen-maps.js 의 배율표를 실제로 고친다
npm run maps                        # 고친 배율을 maps.json 에 반영
```

규칙은 둘입니다.

1. **구간의 첫 땅 > 방금 넘어온 보스** — 문턱이 꺼지지 않게
2. **구간의 마지막 땅 < 그 구간의 보스** — 보스가 보스답게

목표는 그 땅에서 **가장 센 놈**에 맞춥니다. 한 단계에 종이 둘 섞여 있고 세기가 배까지
차이 나서, 평균에 맞추면 난이도가 톱니처럼 오르내리기 때문입니다.

> ⚠ **보스도 그 맵 위에 서 있습니다.** 맵 배율을 고치면 보스의 세기도 함께 움직이므로,
> `zone-solve --write` → `npm run maps` 뒤에는 반드시 `node tools/balance.js --solve` 로
> 보스 능력치를 다시 찾아 `sheets/monsters.csv` 에 적어야 합니다. 이 순서를 건너뛰면
> 목표 승률이 조용히 어긋납니다.
>
> 배율은 나눗셈으로 구할 수 없습니다 — 전투력은 배율에 정비례하지 않습니다
> (방어력 항이 체력과 함께 곱해져 배율보다 빠르게 자랍니다). 그래서 이분 탐색을 씁니다.

---

## 단계 사다리 — `tools/stage-solve.js` · `stage-curve.js` · `stage-color.js` (0.35)

`zone-*` 은 **구간과 구간 사이**를 봅니다. 이쪽 셋은 **구간 안의 한 칸 한 칸**을 봅니다.
"6단계가 5단계보다 쉽다" 같은 것은 여기서 잡힙니다.

```bash
node tools/stage-solve.js           # 얼마로 바꿔야 하는지 보여만 준다
node tools/stage-solve.js --write   # tools/gen-maps.js 의 배율표를 고친다
node tools/stage-solve.js --n 40    # 표본 수(기본 30)
npm run maps                        # 고친 배율을 maps.json 에 반영

node tools/stage-curve.js           # 장비를 고정한 몸으로 걸어가 본다 (성패로 끝난다)
node tools/stage-color.js           # 색과 실제 승률을 나란히 잰다 (성패로 끝난다)

node tools/dungeon-solve.js --rate [--write] [--n 24]   # 지하감옥 층 배율을 승률로 (0.36)
```

**얕은 땅(1~4단계)은 배율로 풀지 않습니다** — 배율이 경험치·골드도 함께 올려서
초반 성장 속도가 통째로 달라지기 때문입니다. 1단계를 95% 로 맞추려면 배율이 1.3 → 5.2 가
되고, 그러면 첫 들판 한 바퀴에 Lv.9 가 됩니다. 그 자리는 **몬스터 능력치**로 풉니다.

```bash
node tools/balance.js --trash --solve --n 60   # TRASH 표의 1·2·3단계 줄
```

⚠ **한 몬스터가 여러 땅에 삽니다.** 늑대는 3·4단계에 함께 있어서, 3단계 고비를 만들려고
늑대를 세게 했더니 4단계가 82% 에서 12% 로 무너졌습니다. 능력치를 고친 뒤에는 반드시
`node tools/stage-solve.js --write` → `npm run maps` 로 그 몬스터가 사는 **모든 땅**을 다시 풉니다.

**지하감옥은 0.36 부터 승률로 풉니다.** 예전에는 "20단계 보스 전투력 대비 몇 %" 로
풀었는데, 그건 몬스터끼리의 잣대라 기준 보스가 세지면 목표도 같이 올라가는 반면
사람의 몸은 그대로여서 실제 승률이 조용히 어긋납니다 —
보스 체력을 다시 잡은 뒤 2·3층이 목표보다 **+18%p·+28%p 쉬워져** 있었습니다.

**왜 전투력이 아니라 승률로 푸나** — `zone-solve` 는 "이 단계의 전투력이 얼마여야 하나"로
풉니다. 그건 몬스터끼리의 잣대라 사람이 겪는 것과 어긋날 수 있습니다.
`stage-solve` 는 **몸을 세워 두고 실제 전투를 돌려** 승률을 재고, 그 승률이 목표 곡선을
그리도록 배율을 찾습니다. 사람이 말하는 "쉽다/어렵다"는 곧 승률이기 때문입니다.

**단계마다 다른 몸으로 잽니다.** 한 구간을 통째로 같은 몸으로 재면, 앞에 맞추면 뒤가
불가능해지고 뒤에 맞추면 앞이 공짜가 됩니다. 실제로 그렇게 풀었더니 **1단계 슬라임의
배율이 1.3 에서 16 으로 튀었습니다** — Lv.8 이 버거워하는 슬라임은 Lv.3 에게는 벽입니다.

| 도구 | 몸 | 답하는 물음 |
|---|---|---|
| `stage-solve` | 단계마다 **알맞은 몸** | 배율을 얼마로 두어야 목표 승률이 나오나 |
| `stage-curve` | 구간마다 **고정한 몸** | 같은 장비로 걸어갔을 때 뒤 땅이 더 쉬워지는 곳이 없나 |
| `stage-color` | 단계마다 **알맞은 몸** | 이름표 색이 단계를 따라 짙어지나 |

> ⚠ 세 도구의 기준 몸은 **같아야 합니다.** `stage-solve.js` 의 `ZONES` 와
> `stage-color.js` 의 `REF`, 그리고 `balance.js` 의 `MATCHES`/`TRASH` 가 서로 다른 사람을
> 세워 두면 세 도구가 다른 말을 합니다. 실제로 한 번 그랬고, 그때 보스가 제 땅의
> 잡몹보다 약해졌습니다.

> ⚠ `stage-solve --write` → `npm run maps` 뒤에는 반드시
> `node tools/balance.js --solve --hp-only` 로 보스를 다시 잡아야 합니다.
>
> ⚠ 그리고 **보스를 다시 잡았으면 지하감옥도 다시 풉니다.**
> 지하감옥의 기준이 20단계 보스이기 때문입니다. 순서는 이렇습니다:
> `stage-solve --write` → `npm run maps` → `balance --solve --hp-only` → 표에 쓰기 →
> `dungeon-solve --rate --write` → `npm run maps` → `balance --solve --hp-only`(감옥의 주인만) → 표에 쓰기.

**한 구간 안에서는 강화 수치를 올리지 마세요.** 구간 중간에 +9 → +10 을 두었더니
그 자리에서 배율이 40% 뛰고, 강화를 아직 못 한 사람의 승률이 75% 에서 11% 로 떨어졌습니다.
사다리가 아니라 절벽입니다. 구간 안에서 달라지는 것은 **레벨뿐**이어야 합니다.

**배율은 몬스터가 같을 때만 뒤로 못 갑니다.** 더 센 놈이 새로 나오는 땅은 배율이 낮아도
실제로는 더 어렵습니다(8단계에 악마 병사가 처음 나옵니다). 그걸 모르고 무조건 올려 두면
승률이 목표를 한참 밑돌아 절벽이 됩니다 — `stage-solve` 는 `sameFoes` 일 때만 눌러 둡니다.

**한 땅 안의 두 몬스터는 4레벨 넘게 벌리지 마세요.** 예전에는 해골(17)과 악마 병사(24)가
한 땅에 있어 일곱 칸이 벌어졌고, 같은 땅인데 어떤 놈을 만나느냐로 승패가 갈렸습니다.
레벨은 `sheets/monsters.csv`, 배치는 `tools/gen-maps.js` 의 `STAGES` 에 있습니다.

#### 재는 방법에 대해 — 세 가지를 꼭 맞춰 둡니다

미리보기가 게임과 어긋나면 없는 문제를 고치게 됩니다. 실제로 그런 일이 있었습니다.

1. **맵의 단계 보정** — 몬스터 표의 숫자는 "맨몸" 값입니다. 게임에서는 그 위에
   맵의 `power` 가 곱해집니다(발가르가 서 있는 `field_10` 은 ×3.9). `MATCHES` 의
   마지막 칸이 그 값이며, 빠뜨리면 네 배 어긋납니다.
2. **회복약** — 가방에 약이 있다고 치고 잽니다. 빼놓으면 용사 패시브(회복량 2배)가
   통째로 없는 셈이 되어 용사만 유독 약하게 나옵니다.
3. **스킬 찍는 순서** — `PLAN` 에 적힌 순서대로 찍습니다. 순서 하나로 그 직업이
   두 배 세지기도 합니다(마법사는 '마력 증폭'이 아니라 '생명 흡수'부터가 정답입니다).
   "이렇게 찍으면 어떻게 되나"가 궁금하면 그 목록을 고치고 다시 돌리면 됩니다.

### 4) 넣기

```bash
node tools/sheets.js check     # 말이 되는지 검사만 (아무것도 안 바꿈)
node tools/sheets.js import    # src/data/*.json 에 반영
```

**검사에 걸리면 아무것도 바꾸지 않습니다.** 오타를 내도 게임이 깨지지 않습니다.

### 4) 서버에 올리기

```bash
node tools/sheets.js apply "물약값 조정"
```

`import` + 서버 배포를 한 번에 합니다.
서버가 켜져 있으면 접속해 있는 사람들에게 **"새 콘텐츠"** 알림이 바로 뜹니다.

---

## B. 구글 시트에 연결

시트에서 숫자를 고치면 서버가 알아서 읽어 갑니다. GitHub 에 올릴 필요가 없습니다.

> **가장 중요한 것 — 문서는 반드시 하나여야 합니다.**
> 표 열 개를 **문서 열 개**로 나눠 두면 서버가 못 읽습니다.
> **문서 하나 안에 탭 열 개**여야 합니다. 서버는 문서 아이디 하나만 알고,
> 그 안에서 탭 이름으로 찾아가기 때문입니다.

### 1) 시트 만들기 — 가장 쉬운 길

```bash
npm run sheets      # sheets/*.csv 열 개 + dragon-field-tables.xlsx
```

만들어진 **`dragon-field-tables.xlsx`** 를 구글 드라이브에 **끌어다 놓고**,
그 파일을 더블클릭해 엽니다. 구글이 알아서 시트로 바꿔 주고 탭 이름도 그대로 붙습니다.

> 표를 고칠 때마다 `npm run sheets` 를 다시 돌리면 통합문서도 함께 새로 구워집니다.
> 탭은 `server/sheets.js` 의 등록표에서 그대로 나오므로, 표를 새로 하나 만들면
> 통합문서에도 서버 동기화에도 자동으로 따라옵니다.

직접 만들고 싶다면 새 구글 시트 하나를 만든 뒤 열 번 반복합니다 —
**파일 → 가져오기 → 업로드 → (CSV 하나) → 새 시트 삽입**.

**탭 이름을 반드시 이렇게 맞추세요.** 이름으로 찾아갑니다.

```
items  monsters  drops  quests  skills  classes  traits  stats  engrave  config
```

### 2) 공개하기

오른쪽 위 **공유** → **일반 액세스** → **링크가 있는 모든 사용자** → 권한은 **뷰어**.

> 뷰어면 충분합니다. 아무도 시트를 고칠 수 없고, 서버는 읽기만 합니다.

### 3) 서버에 알려 주기 — Render 에서 할 일

Render 대시보드 → 그 서비스 → 왼쪽 **Environment** → **Add Environment Variable**.
아래 셋을 넣고 **Save Changes** 를 누르면 서버가 다시 뜹니다.

| 이름 | 값 | 뜻 |
|---|---|---|
| `SHEET_ID` | `https://docs.google.com/spreadsheets/d/…` (주소창 통째로) | 어느 시트를 볼지 |
| `SHEET_POLL_MIN` | `5` | 5분마다 확인. **안 넣으면 5분**, `0` 을 넣으면 확인 안 함 |
| `ADMIN_KEY` | 아무 긴 문자열 | "지금 당장" 반영할 때 쓰는 열쇠 |

`render.yaml` 을 쓰고 있다면 `SHEET_POLL_MIN` 과 `ADMIN_KEY` 는 이미 들어 있으므로
**`SHEET_ID` 하나만** 넣으면 됩니다.

> ### `SHEET_ID` 를 안 넣어도 됩니다 (0.45)
>
> 저장소의 **`sheets/SOURCE.txt`** 에 문서 아이디가 적혀 있으면 서버가 그것을 씁니다.
> `node tools/sheets.js pull "<시트 주소>"` 를 한 번 돌리면 그 파일이 만들어집니다
> (지금 저장소에는 이미 들어 있습니다).
>
> 환경변수를 넣으면 **그쪽이 이깁니다** — 배포한 서버만 다른 문서를 보게 하고 싶을 때 씁니다.
>
> 왜 이렇게 했나: 운영자 창에서 "시트 지금 읽기" 를 눌렀을 때
> `SHEET_ID 가 설정되어 있지 않습니다` 만 나오고 거기서 막히는 일이 있었습니다.
> **저장소는 이미 어느 문서인지 알고 있는데** 사람에게 다시 물을 이유가 없습니다.
>
> ⚠ 어느 쪽이든 그 문서는 **"링크가 있는 모든 사용자 — 뷰어"** 로 공개되어 있어야 합니다.
> 공개하고 싶지 않다면 `sheets/SOURCE.txt` 의 마지막 줄을 지우고 환경변수만 쓰세요.

### 3-1) 제대로 붙었는지 확인

Render 의 **Logs** 탭에 이 줄이 보이면 성공입니다.

```
구글 시트   : 연결됨 — 10분마다 확인
구글 시트   : 연결됨 (sheets/SOURCE.txt) — 5분마다 확인 (기본값)   ← 저장소가 기억한 문서를 쓸 때
```

`연결됨`이 안 보이면 둘 다 없는 것입니다. 그때는 이렇게 찍힙니다.

```
구글 시트   : 안 씀 — SHEET_ID 도 sheets/SOURCE.txt 도 없습니다
```

`자동 확인 꺼짐`이라고 나오면 `SHEET_POLL_MIN` 이 0 입니다.

넣고 저장하면 서버가 다시 뜨고, 로그에 이렇게 찍힙니다.

```
구글 시트   : 연결됨 — 10분마다 확인
```

### 3-2) "고쳤는데 반영이 안 됩니다" — 여기 셋을 봅니다

이 셋이 실제로 있었던 원인 전부입니다.

| 무엇 | 어떻게 알아보나 | 어떻게 고치나 |
|---|---|---|
| **서버가 아예 안 읽는다** | 로그에 `자동 확인 꺼짐(SHEET_POLL_MIN=0)` | `SHEET_POLL_MIN` 을 지우거나 `5` 로. **0.34 부터 안 넣으면 5분이 기본**입니다 |
| **구글이 옛 값을 준다** | 당겼는데 `시트와 서버 내용이 같습니다` | 0.34 부터 주소에 매번 다른 값을 붙여 캐시를 비껴갑니다. 그래도 그러면 1~2분 뒤 다시 |
| **화면이 안 바뀐다** | 서버 로그에는 `콘텐츠 v… 배포` 가 찍혔다 | 게임 화면 위에 뜨는 **`지금 적용`** 을 누릅니다. 표는 접속할 때 읽으므로 새로고침이 필요합니다 |

> **칸을 새로 넣은 판을 올렸다면** 시트도 새로 올려야 합니다.
> 0.34 의 `monsters` 탭 **분노** 칸이 그렇습니다 — 없으면 당기기가 그 자리에서 멈추고
> 어느 칸이 다른지 알려 줍니다. `SHEET-FIRST.md` 를 보세요.

### 3-3-3) `이름은 비울 수 없습니다` — 시트에는 적혀 있는데 (0.52 에서 고침)

```
'classes' 탭 — classes 시트 2번째 줄: 이름은 비울 수 없습니다.
```

시트에는 `용사` 라고 멀쩡히 적혀 있습니다. **지운 것은 구글입니다.**

서버가 쓰던 `gviz` 통로는 **열마다 자료형을 스스로 정합니다.** `classes` 탭의
`값` 열은 이렇게 생겼습니다:

```
값
용사          ← 글자
몽둥이 하나로…  ← 글자
예            ← 글자
48            ← 숫자
11            ← 숫자
…             ← 숫자가 훨씬 많다
```

구글은 이 열을 **숫자 열**로 보고, 숫자가 아닌 칸을 **빈칸으로 지워서** 줍니다.
그래서 `용사` 가 사라졌습니다. `classes` 만 걸린 이유도 이것입니다 —
다른 탭은 한 열에 한 가지만 들어 있습니다.

**고친 법 — 통로를 바꿨습니다.** 이제 탭마다 CSV 를 받지 않고,
**통합문서 한 장**을 그대로 받습니다:

```
https://docs.google.com/spreadsheets/d/<문서아이디>/export?format=xlsx
```

이 통로는 아무것도 짐작하지 않습니다. 칸에 적힌 것이 그대로 옵니다.
덤으로 요청이 **열 번에서 한 번**으로 줄고, 탭을 반쯤 읽다 마는 일도 없어졌습니다.
서버를 0.52 이상으로 올리면 끝이고, **시트에서는 아무것도 안 하셔도 됩니다.**

### 3-3) `'직업 warrior warrior warrior'` 라고 나올 때 (0.50 에서 고침, 0.52 에서 뿌리째)

운영자 창에 이렇게 뜨는 일이 있었습니다:

```
'classes' 탭 — classes 시트의 첫 줄이 제목 줄이 아닙니다.
첫 칸은 '직업' 이어야 합니다. 지금: '직업 warrior warrior warrior'
```

**시트는 멀쩡합니다.** 첫 줄은 `직업`이 맞습니다. 구글이 잘못 읽은 것입니다.

서버는 구글의 `gviz` 라는 통로로 탭을 CSV 로 받아 옵니다. 이 통로는
**제목 줄이 몇 줄인지 스스로 짐작합니다.** 첫 칸이 죽 글자인 탭
(`classes` 는 `직업` / `warrior` / `warrior` / `warrior` …)에서는
"아, 이건 네 줄짜리 제목이구나" 하고 네 줄을 **한 줄로 이어 붙여** 돌려줍니다.
그래서 첫 칸이 `직업 warrior warrior warrior` 가 됩니다.

0.50 에서는 주소에 `headers=1` 을 붙여 "제목은 한 줄" 이라고 못 박아 막았습니다.
그런데 같은 통로가 **이번에는 열의 자료형을 짐작해서** 또 막았습니다(위 3-3-3).
짐작하는 통로를 계속 달래는 대신 **0.52 에서 통째로 갈아 치웠습니다.**

### 3-3-2) `401` — 문서를 새로 만들어 올리셨습니까 (0.51 에서 고침)

```
'items' 탭을 받지 못했습니다 (401).
```

`401` 과 `404` 는 거의 언제나 **서버가 다른 문서를 보고 있다**는 뜻입니다.
시트를 새로 만들어 올리면 **문서 아이디가 바뀌는데**, 서버는 옛 아이디를 들고
없어진 문서를 계속 두드립니다. 시트의 공개 설정은 아무 잘못이 없습니다.

**가장 쉬운 길 — 운영자 창에서 바꿉니다. 다시 배포하지 않아도 됩니다.**

1. 운영자 창 → **표** 단을 엽니다
2. 맨 위 **지금 보는 문서** 에 적힌 아이디를 봅니다 — 내 시트 주소의 `/d/` 뒤와
   다르면 그것이 원인입니다
3. 시트 주소를 그대로 복사해 **시트 주소** 칸에 붙여 넣고 **주소 저장**
4. **시트 지금 읽기**

`원래대로` 를 누르면 저장한 값을 지우고 환경변수 → `sheets/SOURCE.txt` 순서로 돌아갑니다.

| 어디서 온 값인가 | 센 순서 | 어떻게 바꾸나 |
|---|---|---|
| 운영자 창에서 정한 문서 | **①** | 표 단에서 주소 저장 (서버 재배포 없음) |
| 서버 환경변수 `SHEET_ID` | ② | Render 설정에서 고치고 재배포 |
| 저장소 `sheets/SOURCE.txt` | ③ | 파일을 고치고 다시 올리기 |

> **문서를 매번 새로 만들지 마세요.** 같은 문서에 **파일 → 가져오기 →
> 현재 시트 바꾸기(덮어쓰기로 가져오기)** 를 쓰면 아이디가 안 바뀝니다.
> 그러면 이 일 자체가 안 생깁니다.

### 3-4) 시트가 자꾸 말썽이면 — 시트를 안 쓰는 길

시트는 **여러 명이 값을 고칠 때** 값을 합니다. 혼자 고치실 거라면
`A. 파일로 주고받기` 가 훨씬 단단합니다. 공개 설정도, 문서 아이디도,
제목 줄 짐작도 끼어들지 않습니다.

```bash
npm run sheets          # sheets/*.csv 와 xlsx 를 꺼낸다
# 엑셀이나 구글 시트에서 고친 뒤 sheets/*.csv 로 저장
npm run sheets:import   # src/data 에 넣는다
npm run sheets:apply    # 서버에 배포한다
```

### 4) 이제부터

시트에서 숫자를 고치고 최대 5분 기다리면 끝입니다.
서버가 읽고 → 검사하고 → 새 버전을 배포하고 → 접속자에게 알립니다.

기다리기 싫으면 바로 부릅니다.

```bash
curl -X POST https://내주소.onrender.com/api/content/pull \
     -H "Content-Type: application/json" \
     -d '{"key":"내가-정한-ADMIN_KEY","note":"물약값 조정"}'
```

내려받아 확인만 하고 싶다면 내 컴퓨터에서도 됩니다.

```bash
node tools/sheets.js pull "구글시트주소"           # 받아만 온다
node tools/sheets.js pull "구글시트주소" --apply   # 받아서 반영·배포까지
```

---

## 칸 설명

### items (아이템)

| 칸 | 뜻 |
|---|---|
| `id` | 영문 아이디. 다른 표에서 이걸로 부른다. **바꾸면 안 된다** |
| `이름` | 화면에 나오는 이름 |
| `종류` | `weapon` `armor` `accessory` `consumable` `material` |
| `부위` | 장비만. `weapon` `armor` `shoulder` `gloves` `boots` `belt` `necklace` `ring` |
| `아이콘` | 그림 이름 (manifest.json 에 있어야 한다) |
| `등급` | `common` `uncommon` `rare` `epic` |
| `겹침` | 인벤에서 겹쳐 쌓이는가 (`예`/`아니오`) |
| `강화` | 강화할 수 있는가 |
| `가격` | 상점 가격. 파는 값은 이것의 일부 |
| `atk def hp spd crit` | 장비가 올려 주는 값. 빈 칸이면 없음 |
| `회복HP` | 물약이 채워 주는 체력 |
| `마법` | 룬 장비의 속성 이름 |
| `설명` | 아이템 설명. 줄바꿈은 `\n` 두 글자로 |
| `기타` | **손대지 마세요.** 표로 못 펴는 값이 JSON 으로 들어 있습니다 |

### monsters (몬스터)

`레벨` `hp` `atk` `def` `spd` `crit` `exp` `골드` 가 핵심입니다.
`보스`/`정예` 는 `예`/`아니오`, `전투크기` 는 전투 화면에서의 크기 배수입니다.

`분노` 는 **한 번 휘두를 때마다 공격력에 더 붙는 비율**입니다(`0.1` = 매번 +10%, 누적).
비워 두거나 `0` 이면 예전과 똑같습니다. 지금은 고룡 카르나크에만 `0.1` 이 붙어 있습니다.
오래 끌수록 무서워지는 상대를 만드는 칸이라, 아무 몬스터에나 붙이면 사냥이 갑자기
"오래 걸리면 죽는" 놀이가 됩니다 — 보스에만 쓰세요.

> **여기 적는 숫자는 "맨몸" 값입니다.**
> 게임에서는 그 몬스터가 서 있는 맵의 단계 보정(`maps.json` 의 `power`)이 곱해집니다.
> 그래서 표만 보면 10단계 보스(발가르, hp 406)가 5단계 보스(정찰대장, hp 583)보다
> 약해 보이지만, 실제로는 발가르가 ×3.9 인 맵에 서 있어서 1,583 대 1,078 로 더 셉니다.
> 진짜 세기는 `node tools/balance.js` 로 확인하세요 — 그 도구는 맵 보정까지 넣고 잽니다.

> **같은 상대라도 판마다 세기가 다릅니다(기세).**
> 전투를 시작할 때 상대의 체력·공격력이 ±35% 안에서 무작위로 흔들립니다
> (`formulas.js` 의 `MONSTER_MOOD`). 크게 치우친 판은 전투 기록에
> "잔뜩 벼르고 있다 / 지쳐 보인다" 로 뜹니다.
> 이게 없으면 능력치가 조금만 넘쳐도 100%, 조금만 모자라도 0% 여서
> "다섯 번 도전해 한 번 이기는 보스" 같은 자리를 만들 수가 없습니다.

### drops (드랍표)

**한 줄이 드랍 하나**입니다. 몬스터 하나가 여러 줄을 가집니다.

```
몬스터    아이템          확률   최소  최대
slime    slime_jelly    0.75   1    2
slime    herb           0.25   1    1
```

확률은 **0~1** 입니다. `0.25` = 25%, `1` = 무조건.
표 순서대로 전부 굴리므로 한 마리에서 여러 개가 나올 수 있습니다.

### quests (퀘스트)

**위에서 아래로 순서대로** 이어집니다. 하나를 끝내면 바로 다음 줄이 열립니다.

| 칸 | 뜻 |
|---|---|
| `조건` | `collect`(모아오기) `hunt`(처치) `reach`(단계 도달) |
| `대상` | collect 면 아이템 id, hunt 면 몬스터 id, reach 면 맵 id |
| `필요레벨` | 이 레벨이 되어야 완료된다. 비우면 제한 없음 |
| `선택보상` | `|` 로 나눠 여러 개. 완료할 때 하나를 고른다 |
| `설명` | 줄바꿈은 `\n` 두 글자로 |

### skills (스킬)

| 칸 | 뜻 |
|---|---|
| `id` | 영문 아이디. **바꾸면 안 된다** (이미 찍어 둔 사람의 포인트가 날아간다) |
| `이름` `아이콘` | 화면에 나오는 것 |
| `직업` | **어느 직업이 이 스킬을 배우나.** `용사` `사냥꾼` `마법사`. `용사 \| 마법사` 처럼 여럿도 된다 |
| `최대` | 몇 단계까지 찍을 수 있나 |
| `효과1` `값1` … | 1단계당 얼마씩 오르나. 이름은 아래 목록에서 고른다 |
| `설명` | 스킬 칸에 한 줄로 나오는 글 |
| `자세한설명` | 골랐을 때 아래에 나오는 긴 글. 비워도 된다 |

`직업` 칸이 이 시트의 핵심입니다. 여기를 고치면 **정말로** 그 직업이 배울 수 있게 됩니다.
비워 두면 아무도 못 배우고, 들여올 때 그렇다고 알려 줍니다.

줄 하나를 새로 추가하면 그게 새 스킬입니다. 효과가 하나도 없으면 막힙니다
(찍어도 아무 일이 없는 스킬이 배포되는 걸 막기 위해서입니다).

### classes (직업 · 패시브)

**패시브를 고치는 곳입니다.** 다른 시트와 달리 세로로 눕혀 있습니다 —
한 줄이 "어느 직업의 · 무슨 항목이 · 얼마" 하나이고, 줄마다 설명이 붙어 있습니다.

```
직업       항목        값     설명
warrior   물약 배율    2      물약 회복량 배율. 2 = 두 배
warrior   반격 확률    0.35   맞았을 때 되받아칠 확률. 0.35 = 35%
```

#### ⚠ 먼저 알아야 할 것 — 패시브는 두 겹입니다

| | 무엇 | 고치면 |
|---|---|---|
| **글** | `패시브 이름` `패시브 설명` `패시브 자세히` `전투설명 1~3` | 화면에 보이는 글만 바뀐다. **전투는 그대로** |
| **알맹이** | `반격 확률` `물약 배율` `보호막 무적 시간` 같은 숫자들 | **전투가 실제로 바뀐다** |

설명이 `★` 로 시작하는 줄이 "글일 뿐인" 항목입니다.

그래서 패시브를 고칠 때는 **둘 다** 고쳐야 합니다.
숫자만 고치면 화면에는 옛 설명이 남고, 글만 고치면 게임은 그대로입니다.

**예 — 용사의 "무쇠 위장(회복약 2배)" 을 3배로:**

```
warrior   물약 배율     2 → 3
warrior   패시브 설명   회복약 효과 2배 → 회복약 효과 3배
```

#### 지금 직업별 패시브의 알맹이

| 직업 | 패시브 | 이 줄들을 고친다 |
|---|---|---|
| 용사 | 무쇠 위장 (회복약 2배) | `물약 배율` |
| 용사 | (반격·치명상 버티기) | `반격 확률` `반격 위력` `치명상 버티기` |
| 사냥꾼 | 반사 신경 (맞은 뒤 한 대 확정 회피) | `맞은 뒤 확정 회피` |
| 사냥꾼 | (선제 사격·회피) | `선제 공격 횟수` `선제 공격 위력` `회피 확률` |
| 마법사 | 마력 보호막 (쓰러질 피해를 막고 3초 무적) | `보호막` `보호막 무적 시간` |
| 마법사 | (광역·충전 폭발) | `광역 여파` `충전 주기` `충전 위력` |

#### 없던 능력을 켤 수도 있습니다

빈 칸에 숫자를 넣으면 그 직업이 그 능력을 갖게 됩니다.
사냥꾼의 `반격 확률` 에 `0.2` 를 넣으면 사냥꾼도 반격하기 시작합니다.
반대로 **칸을 비우면 그 능력이 꺼집니다.**

#### 고칠 수 있는 항목 전부

| 항목 | 값의 뜻 |
|---|---|
| `이름` `한줄소개` | 화면에 나오는 글 |
| `선택가능` | `아니오` 면 접속 화면에서 "준비 중" 회색 처리 |
| `기본 HP/공격력/방어력/속도/치명타` | 1레벨 능력치 |
| `성장 HP/공격력/방어력/속도/치명타` | 레벨 1당 오르는 양 |
| `반격 확률` | 맞았을 때 되받아칠 확률. `0.35` = 35% |
| `반격 위력` | 반격 피해 배율. `0.55` = 평소의 55% |
| `치명상 버티기` | 쓰러질 피해를 HP 1 로 버틸 확률. 전투당 1회 |
| `물약 배율` | 물약 회복량 배율. `2` = 두 배 |
| `선제 공격 횟수` | 전투 시작하자마자 공짜로 때리는 횟수 |
| `선제 공격 위력` | 그 공격의 피해 배율 |
| `회피 확률` | `0.18` = 18% |
| `맞은 뒤 확정 회피` | `1` 이면 켬, 비우면 끔 |
| `광역 여파` | 주 대상에게 준 피해의 몇 %가 나머지에게. `0.45` = 45% |
| `충전 주기` | 몇 번째 공격마다 대폭발이 터지나 |
| `충전 위력` | 그 폭발의 피해 배율 |
| `보호막` | `1` 이면 켬 |
| `보호막 무적 시간` | 밀리초. `3000` = 3초 |
| `끌어오기 범위` | 필드에서 몇 칸 안까지 함께 끌고 들어오나. `0` = 한 마리 |
| `한 전투 최대 마리` | 끌고 올 수 있는 최대 수 |
| `공격 속성` | `magic` 이면 마법 공격으로 친다. 비우면 물리 |

**가장 흔한 실수 —** 확률을 `35` 라고 적는 것입니다. `0.35` 여야 합니다.
`35` 로 적으면 들여올 때 막고 그렇다고 알려 줍니다.

> **새 직업은 여기서 못 만듭니다.** 그림(스프라이트)이 있어야 하고
> `manifest.json` 에도 등록해야 해서 시트만으로는 부족합니다.
> 없는 직업 아이디를 적으면 막힙니다. 시작 아이템 · 스킬 목록 · 그림 이름은
> 이 시트가 건드리지 않으니 안심하고 고쳐도 됩니다.
> (스킬 목록은 `skills` 시트의 `직업` 칸이 담당합니다.)

### traits (특성)

**포인트로 찍는 여섯 갈래**입니다. **1점당** 무엇이 얼마나 오르는지를 적습니다.

| 칸 | 뜻 |
|---|---|
| `최대` | 몇 점까지 넣을 수 있나 (지금은 전부 5) |
| `hp` `atk` `def` `spd` `crit` | 1점당 더해지는 **순수 값** |
| `효과1` `값1` … | 1점당 더해지는 **비율**. `0.01` 이 1% |
| `설명` | 특성 창에 나오는 요약. **숫자를 고치면 이 글도 같이 고쳐야 합니다** (자동으로 안 바뀝니다) |

줄을 새로 추가하면 일곱 번째 특성이 생깁니다.
포인트를 몇 레벨마다 주는지는 `config` 탭에, 어느 보스 퀘스트가 한 점을 더 주는지는
`src/data/traits.json` 의 `questPoints` 에 있습니다.

### stats (힘 · 민첩 · 지능)

**포인트로 찍지 않는 셋**입니다. 레벨이 오르면 직업이 알아서 올려 줍니다.
그래서 `최대` 칸이 없습니다 — 위가 없으니까요.

| 칸 | 뜻 |
|---|---|
| `hp` `atk` `def` `spd` `crit` | 1점당 더해지는 **순수 값**. `atk 3` 이면 1점에 공격력 +3 |
| `효과1` `값1` … | 1점당 더해지는 **비율**. `0.01` 이 1% |
| `설명` | 스탯 창에 나오는 요약 |

**몇 레벨마다 오르는지는 여기가 아니라 `classes` 탭**에 있습니다 —
`힘 성장주기` · `민첩 성장주기` · `지능 성장주기` 세 줄이고, 직업마다 다릅니다.
`1` 이면 매 레벨, `2` 면 두 레벨에 한 번입니다. 오를 때 20% 확률로 두 점이 오릅니다.

| 직업 | 힘 | 민첩 | 지능 |
|---|---|---|---|
| 용사 | 1 | 2 | 3 |
| 사냥꾼 | 3 | 1 | 2 |
| 마법사 | 2 | 3 | 1 |

### engrave (각인)

주울 때·살 때 **열에 하나꼴로** 장비에 붙는 덤 한 줄입니다. 강화(+7~+9)로 붙는
옵션과는 **다른 자리**라 서로 뺏지 않습니다. 다섯 줄, 등급마다 한 줄입니다.

| 칸 | 뜻 |
|---|---|
| `등급` | `common` `uncommon` `rare` `epic` `legendary` — 아이템의 등급과 같은 말 |
| `이름` | 사람이 읽는 등급 이름. 여기서 고쳐도 게임은 안 봅니다(눈으로 찾기 위한 칸) |
| `최소` `최대` | 각인 수치가 굴러가는 범위. **퍼센트 옵션이면 %, 힘·민첩·지능이면 점수** |
| `붙을 확률 %` | 그 등급 장비에 각인이 붙을 확률. 마법사 패시브가 여기에 더 얹습니다 |
| `초월 확률 %` | 붙은 각인이 **초월**이 될 확률 |

**초월**이 붙은 물건은 각인도, 강화로 붙는 옵션도, 다시 굴린 값도 전부 **최대치**로만
나옵니다. 화면에서는 붉은 글씨와 ✦✦ 로 보입니다.

등급마다 확률을 다르게 줄 수 있습니다 — 전설만 초월이 잘 뜨게 하거나,
일반은 아예 안 붙게 하는 식입니다. 다섯 줄에 같은 값을 적어 두면 지금과 같습니다.

> 이 표는 `src/data/affixes.json` 의 `각인` 칸으로 들어갑니다.
> 같은 파일의 옵션 통(무기·방어구·장신구 `min~max`)은 **여기가 아닙니다** —
> 각인은 그 통의 범위를 쓰지 않고 이 표의 범위를 씁니다.

### config (포인트 설정)

네 줄뿐입니다. 숫자만 고치세요.

| 항목 | 뜻 | 지금 |
|---|---|---|
| `traits.json / everyLevels` | 몇 레벨마다 특성 포인트를 주나 | 10 |
| `traits.json / pointsPerGrant` | 그때 한 번에 주는 특성 포인트 | 1 |
| `skills.json / everyLevels` | 몇 레벨마다 스킬 포인트를 주나 | 3 |
| `skills.json / pointsPerGrant` | 그때 한 번에 주는 스킬 포인트 | 1 |

줄을 추가해도 무시되지 않고 **막힙니다** — 여기서 고칠 수 있는 건 이 네 가지뿐입니다.

---

## 스킬·특성에 쓸 수 있는 효과 이름

시트에는 한글 이름으로 적혀 나옵니다. 영문 키로 적어도 받아 줍니다.
**여기 없는 이름은 들여올 때 막힙니다** (게임이 모르는 효과는 조용히 무시되기 때문입니다).

| 한글 이름 | 영문 키 | 값의 뜻 |
|---|---|---|
| 공격력 | `atkPct` | 0.06 = +6% |
| 방어력 | `defPct` | 0.08 = +8% |
| 최대 HP | `hpPct` | 비율 |
| 최대 HP 배율 | `hpMult` | 0.25 = +25%p (4단계면 두 배) |
| 치명타 확률 | `crit` | 0.025 = +2.5% |
| 치명타 피해 | `critMult` | 0.12 = +12% |
| 2회 공격 확률 | `doubleHit` | 0.06 = +6% |
| 흡혈 | `lifesteal` | 준 피해의 비율만큼 회복 |
| 방어 관통 | `pierce` | 0.05 = 상대 방어 5% 무시 |
| 받는 피해 감소 | `dmgReduction` | 0.035 = -3.5% |
| 마법 피해 | `magicPower` | 마법 공격 증가 |
| 받는 마법 피해 감소 | `magicResist` | 마법 피해 경감 |
| 가시 반사(방어력 기준) | `thorns` | 방어력의 이 비율만큼 되돌려 줌 |
| 방어력 → 공격력 전환 | `defToAtk` | 방어력의 이 비율이 공격력에 더해짐 |
| 위기의 피해 증가 | `lowHpDamage` | HP 가 낮을 때 주는 피해 증가 |
| 위기 기준 HP | `lowHpThreshold` | 그 '낮을 때'의 기준. 0.3 = 30% 이하 |
| 보호막 지속 | `shieldBonusMs` | 밀리초. 1000 = 1초 |
| 회피 확률 | `evadeBonus` | 0.03 = +3% |
| 선제 사격 횟수 | `openerBonus` | 1 = 화살 한 발 더 |
| 광역 여파 | `cleaveBonus` | 0.08 = +8%p |
| 충전 폭발 위력 | `chargeBonus` | 0.3 = +0.3배 |

특성의 `hp` `atk` `def` `spd` `crit` 칸은 비율이 아니라 **그대로 더해지는 값**입니다.

---

## 검사에서 걸리는 것들

넣기 전에 표끼리 앞뒤가 맞는지 봅니다. 하나라도 걸리면 **아무것도 바뀌지 않습니다.**

- 없는 아이템을 떨구게 했을 때
- 없는 몬스터의 드랍표를 만들었을 때
- 퀘스트가 없는 아이템·몬스터를 가리킬 때
- 확률이 0~1 밖일 때
- 아이디가 두 번 나올 때
- 이름이 비었을 때
- 상점이 없는 아이템을 팔 때
- 스킬·특성에 **게임이 모르는 효과 이름**을 적었을 때
- 스킬의 `직업` 칸에 **없는 직업 이름**을 적었을 때
- 스킬에 효과가 하나도 없을 때 (찍어도 아무 일이 없는 스킬)
- 특성이 아무것도 안 올려 줄 때
- `최대` 나 포인트 설정이 0 이하일 때
- 직업의 확률 값이 0~1 밖일 때 (`0.35` 를 `35` 로 적는 실수)
- 직업의 숫자 칸에 글자가 들어갔을 때
- 없는 직업 · 없는 항목을 적었을 때

무엇이 몇 번째 줄에서 걸렸는지 그대로 알려 줍니다.

---

## 주의할 것 두 가지

**`id` 는 바꾸지 마세요.** 다른 표와 이미 저장된 캐릭터가 그 아이디로 아이템을 기억합니다.
아이디를 바꾸면 사람들 가방에서 그 아이템이 사라집니다. 이름만 바꾸는 건 안전합니다.

**`기타` 칸은 건드리지 마세요.** 속도 물약의 버프처럼 표로 펼 수 없는 값이 JSON 으로 실려 있습니다.
그대로 두면 그대로 돌아옵니다.

---

**스킬 아이디를 바꾸지 마세요.** 이미 그 스킬을 찍어 둔 사람의 포인트가 사라집니다.
이름과 효과는 얼마든지 바꿔도 됩니다 — 그건 다음 접속 때 바로 반영됩니다.

**설명 글은 자동으로 안 바뀝니다.** 숫자를 고쳤으면 설명도 같이 고치세요.
특성 창에 "공격력 +3" 이라고 적혀 있는데 실제로는 +5 인 일이 생깁니다.
패시브는 특히 그렇습니다 — `패시브 설명` 과 `전투설명 1~3` 은 전부 글일 뿐입니다.

---

## 여기서 다루지 않는 것

맵 · NPC · 상점 재고 · 직업의 시작 장비와 그림은 표로 펴기에 구조가 복잡해서 뺐습니다.
새 직업을 만드는 것도 그림이 필요해서 여기서는 안 됩니다.
그것들은 `src/data/*.json` 을 직접 고치고 평소대로 배포하세요.

```bash
node tools/print-tables.js      # 오타 검사
node tools/content.js release "무엇을 고쳤는지"
```
