# 운영 손질 — 랭킹 초기화 · 이벤트 우편

> ## 0.34 부터는 게임 안에서 합니다
>
> **`admin` 계정으로 접속하면** HUD 오른쪽에 🛡 버튼이 생기고, 그 창에서
> **랭킹 초기화 · 전 유저 우편 · 시트 지금 읽기**를 전부 할 수 있습니다.
> 아래의 명령줄 방법도 그대로 살아 있습니다 — 서버에 들어갈 수 있을 때 쓰면 됩니다.
>
> 운영자 계정은 서버가 켜질 때 `ADMIN_KEY` 가 있으면 알아서 만듭니다
> (비밀번호 기본값 `@2222`, `ADMIN_PW` 로 바꿉니다 — **공개 서버라면 꼭 바꾸세요**).
>
> **랭킹 초기화는 이제 시즌 넘기기입니다.** 지금 표가 '지난 시즌'으로 옮겨지고
> 시즌 번호가 하나 오릅니다. 지워지는 것이 아니라 옮겨지므로, 사람들이
> 랭킹 창의 '지난 시즌' 단에서 옛 기록을 계속 볼 수 있습니다.
> 보스 하나만 지우는 것은 시즌을 넘기지 않습니다.
>
> **우편에는 이제 유효기간이 있습니다** — 기본 7일. 보낼 때 날수를 정할 수 있고,
> `0` 을 주면 안 사라집니다. 기한이 지난 우편은 그 사람이 우편함을 열 때 치워집니다.
>
> **0.35 — 받을 사람을 고를 수 있습니다.** 우편 단 맨 위의 **`전체 유저` / `고른 사람에게만`**
> 에서 고릅니다. **기본은 전체 유저**라, 예전처럼 쓰면 예전처럼 됩니다.
> `고른 사람에게만` 을 누르면 계정 목록이 펼쳐지고 검색으로 걸러 담을 수 있습니다
> (`전부` · `비우기` 버튼도 있습니다). 보내기 버튼에 **"3명에게 보내기"** 처럼
> 몇 명인지가 적혀서, 누르기 전에 전체로 갈 참인지 아닌지가 보입니다.
> 한 번에 **200명**까지이고, 없는 아이디가 섞여 있으면 나머지는 보낸 뒤
> **못 찾은 아이디를 돌려줍니다** — 조용히 빠뜨리면 "보냈는데 안 왔다"가 됩니다.
>
> 사과 우편·보상 우편처럼 **몇 사람에게만 갈 것**은 이쪽을 쓰세요.
> 전체 우편은 아직 접속 안 한 사람도 다음에 들어올 때 받아 가므로 되돌리기가 어렵습니다.


> ## 0.36 — 새 아이템이 든 판을 올렸다면 시트부터
>
> 이번 판에서 `items` 탭에 줄이 둘 늘었습니다(쓸모없는 검 · 용린 기사검).
> **시트를 안 올리면 그때부터 시트 반영이 통째로 멈춥니다** — 서버가 당기기를
> `npcs.json: 'gate_merchant' 의 판매 목록에 없는 아이템 'useless_sword'` 로 거절합니다.
> 게임은 이전 표로 그대로 돌아가지만, 시트에서 무엇을 고쳐도 안 들어갑니다.
> `npm run sheets` 로 새 xlsx 를 구워 드라이브의 시트를 갈아 끼우면 풀립니다.

서버를 돌리면서 손으로 해야 하는 두 가지입니다.
둘 다 **같은 열쇠**(`ADMIN_KEY`)를 씁니다.

> **열쇠가 없으면 두 경로 모두 잠겨 있습니다.** 일부러 그렇게 두었습니다 —
> 열쇠 없이 열려 있으면 누구든 전 유저에게 아이템을 뿌리거나 기록을 지울 수 있습니다.
>
> Render → 그 서비스 → **Environment** → `ADMIN_KEY` 에 아무 긴 문자열.
> 로컬이면 `ADMIN_KEY=아무거나 npm start`.

---

## 1. 랭킹 초기화 — 새 시즌 열기

### 시즌 일정 — 끝나는 날을 미리 정해 두기 (0.70.5)

게임 → 운영자 창 → **랭킹** → **시즌 일정**.

| 칸 | 뜻 |
|---|---|
| **끝나는 시각** | 이 시각이 지나면 서버가 **스스로** 시즌을 닫습니다. 비우면 끝을 안 정한 것입니다 |
| **다음 시즌 시작** | `0` = 닫자마자 곧바로 · `n` = n일 쉰 뒤 |

닫히면 **표가 얼어붙습니다** — 그 뒤로는 아무도 기록을 못 올립니다
(마감 1분 전에 눕힌 사람과 한 시간 뒤에 눕힌 사람이 같은 표에 서면 안 되니까요).
쉬는 날수가 지나면 서버가 스스로 다음 시즌을 엽니다 — **전체 초기화와 같은 일**이
일어납니다(표가 지난 시즌으로 옮겨지고 모두가 갓 만든 상태가 됩니다).

* 시각은 **여러분 컴퓨터의 시계**로 적고, 서버로는 그 순간(ms)으로 보냅니다 —
  시간대가 다른 곳에서 열어도 같은 순간을 가리킵니다.
* 판단은 **서버 시계**가 합니다. 손님 시계를 뒤로 돌려도 마감은 안 밀립니다.
* 서버는 30초에 한 번 시계를 봅니다. 아무도 접속해 있지 않아도 끝납니다.
* 새 시즌이 열리면 **끝은 다시 비워집니다.** 다음 마감은 다시 정하세요.
* 사람들에게는 랭킹 창 탭 위 오른쪽에 `3일 20시간 59분 후 종료` 로 보입니다.

### 그냥 하기

저장소 폴더에서:

```bash
# 전부 지운다
SERVER=https://내서버.onrender.com ADMIN_KEY=내열쇠 node tools/rank-reset.js

# 보스 하나만 지운다
SERVER=https://내서버.onrender.com ADMIN_KEY=내열쇠 node tools/rank-reset.js imp_captain
```

지우기 전에 **지금 표를 먼저 보여 줍니다.** 무엇이 사라지는지 보고 나서 지워집니다.

```
  서버: https://내서버.onrender.com
  지금 남아 있는 표:
    imp_captain                3명  1위 철수 42.5분
    demon_general              1명  1위 영희 88.1분

  ✓ 지웠습니다 — 표 2개 · 계정 4개의 기록
    imp_captain, demon_general
    이제 모두 처음부터 다시 도전할 수 있습니다.
```

### 터미널 없이 (curl 로)

```bash
curl -X POST https://내서버.onrender.com/api/rank/reset \
  -H "Content-Type: application/json" \
  -d '{"key":"내ADMIN_KEY"}'
```

보스 하나만: `-d '{"key":"내ADMIN_KEY","boss":"imp_captain"}'`

### 무엇이 지워지고 무엇이 남나

| | |
|---|---|
| **지워지는 것** | 랭킹 표, 그리고 계정마다 적힌 '이 보스를 처음 잡은 시각' |
| **그대로인 것** | 캐릭터 · 레벨 · 골드 · 소지품 · 장비 · 강화 수치 · 우편함 |

> **왜 계정 기록까지 지우나** — 랭킹은 "처음 잡은 것만" 셉니다. 다 키운 캐릭터로 다시 잡아
> 기록을 갈아 치우면 "얼마 만에 여기까지 왔나"라는 물음이 사라지기 때문입니다.
> 그래서 계정마다 '처음 잡은 시각'이 적혀 있는데, **표만 지우면 그게 남아** 이미 잡아 본
> 사람은 다시는 기록을 올릴 수 없습니다. 표는 비었는데 아무도 못 채우는 상태가 됩니다.
> 그래서 둘을 함께 지웁니다.

> ⚠ **되돌릴 수 없습니다.** 지운 기록은 돌아오지 않습니다.

### 랭킹 시간은 무엇을 재나

**첫 캐릭터를 만든 순간부터 그 보스를 처음 잡기까지**입니다. 시간은 **서버가 잽니다** —
게임이 보낸 숫자는 아예 쓰지 않습니다. 브라우저 콘솔에서 "1초 만에 잡았다"고 보내면
그만인 값이면 표 자체가 뜻을 잃기 때문입니다.

보스 아이디는 `imp_captain`(5단계) · `demon_general`(10단계) ·
`elite_imp_captain`(15단계) · `elite_demon_general`(20단계) · `dungeon_lord`(지하감옥) ·
`great_dragon`(고룡) 입니다.

### 사람이 스스로 다시 도전할 수 있습니다 (0.68)

운영자가 시즌을 넘기지 않아도, **각자** 설정 → **처음부터 다시 도전** 으로
캐릭터만 1레벨로 되돌리고 시계를 0초부터 다시 시작할 수 있습니다.
계정은 그대로 남으므로 아이디가 늘어나지 않습니다.

운영자가 알아 둘 것은 셋입니다.

- **같은 보스로 여러 번 올릴 수 있습니다.** 표에는 **가장 빠른 기록**만 남습니다.
  느리게 끝낸 도전이 잘 나온 기록을 지우지 않으므로, 다시 도전하는 것이 손해가 아닙니다.
- **공지는 기록을 실제로 갈아 치웠을 때만** 나갑니다. 안 그러면 아무 일도 없는데
  모두의 화면에 "1위에 랭킹등록!" 이 뜹니다.
- **한 시간에 여섯 번**으로 묶여 있습니다. 실수로 두 번 누르는 것과,
  남의 열쇠를 주워 장난치는 것을 함께 막습니다. 서버 기록에
  `✓ 다시 도전 — <아이디> (n번째)` 로 남습니다.

되돌리는 것은 세이브·시계(`playMs`·`bornAt`)·처음 잡은 기록(`firstKill`)·우편함·이벤트 표시입니다.
0.70.2 부터 타임어택 시계는 **접속해 있는 동안만** 흐릅니다(`server/playclock.js`) — 실제로 재는 값은 `playMs` 입니다.
**이미 랭킹에 올라간 줄은 지우지 않습니다** — 그 사람이 낸 기록이기 때문입니다.

---

## 2. 이벤트 우편으로 아이템 주기

### 그냥 하기

```bash
SERVER=https://내서버.onrender.com ADMIN_KEY=내열쇠 \
  node tools/event.js "추석 선물" "즐거운 명절 보내세요!" potion:20 dragon_token:2
```

형식은 `제목` `내용` 그다음에 `아이템id:개수` 를 원하는 만큼 붙입니다.

```
  ✓ 이벤트 우편을 걸어 두었습니다
    제목  추석 선물
    내용  즐거운 명절 보내세요!
    선물  potion ×20, dragon_token ×2

  접속해 있는 사람은 우편함을 열면 바로, 나머지는 다음 접속 때 받습니다.
```

보내기 전에 **그런 아이템이 있는지 표에서 먼저 확인**합니다. 오타가 있으면 보내지 않고
멈춥니다 — 받을 수 없는 선물이 우편함에 쌓이는 일이 없습니다.

### 누가 받나

계정마다 한 통씩 넣지 않습니다. **한 곳에 적어 두고**, 각자 접속할 때 "내가 마지막으로
가져간 시각" 뒤의 것만 우편함으로 옮겨 갑니다.

그래서 — **아직 계정을 안 만든 사람도 나중에 만들면 받아 갑니다.** 미리 걸어 두고
천천히 모으는 식으로 써도 됩니다.

### 자주 쓰는 아이템 아이디

| 무엇 | id |
|---|---|
| 회복약 · 고급 회복약 | `potion` · `greater_potion` |
| 용의 징표 (초월 강화 재료) | `dragon_token` |
| 보석 | `gem_ruby` `gem_sapphire` `gem_emerald` `gem_topaz` `gem_amethyst` `gem_onyx` `gem_diamond` |
| 재료 | `herb` `mana_stone` `slime_jelly` `bat_fang` `demon_core` |

전체 목록은 `dragon-field-tables.xlsx` 의 `items` 탭 첫 칸에 있습니다.

### curl 로

```bash
curl -X POST https://내서버.onrender.com/api/event/send \
  -H "Content-Type: application/json" \
  -d '{"key":"내ADMIN_KEY","subject":"점검 보상","body":"기다려 주셔서 고맙습니다.",
       "items":[{"id":"greater_potion","count":5}]}'
```

### 무엇이 걸려 있는지 보기

```bash
curl -X POST https://내서버.onrender.com/api/event/list \
  -H "Content-Type: application/json" -d '{}'
```

이건 열쇠가 필요 없습니다(읽기만 하므로).

---

## 계정을 시트에 올리고, 시트에서 지우기 (0.64)

### 올리기 — 복사해서 붙이면 끝

게임 안 **운영자 창 → 계정** 맨 위에 **「시트에 붙여 넣을 표 복사」** 가 있습니다.
누른 다음 구글 시트의 **계정** 탭 `A1` 에 붙여 넣으세요. 만든 순서로 정렬돼 있습니다.

터미널을 쓰고 싶으면 예전 길도 그대로 있습니다.

```bash
node tools/accounts.js --server https://내서버.onrender.com --pw 운영자비밀번호
npm run sheets     # sheets/accounts.csv → 통합문서의 '계정' 탭
```

> 서버가 시트에 **직접 쓰지는 못합니다.** 읽는 길("링크가 있는 모든 사용자 — 뷰어")만
> 열어 두었기 때문입니다. 쓰려면 구글 자격증명을 서버에 두어야 하고, 그건 지켜야 할
> 비밀이 하나 더 느는 일입니다.

### 지우기 — 켜야 켜지고, 켜도 24시간을 기다립니다

**기본은 꺼져 있습니다.** `SHEET_ACCOUNT_DELETE` 를 안 넣었다면 시트 **계정** 탭은
보기만 하는 사진이고, 거기서 줄을 지워도 계정은 **안 지워집니다.**
급히 지울 때 쓰는 것은 운영자 창 → 계정 → 「고른 계정 지우기」이고, 그쪽은 즉시입니다
(다만 0.64.3부터 그것도 휴지통을 거칩니다 — 30일 안에 되살릴 수 있습니다).

켜려면 서버 환경변수에:

| 이름 | 값 | 뜻 |
|---|---|---|
| `SHEET_ACCOUNT_DELETE` | `1` | 켠다 |
| `ADMIN_KEY` | (이미 있는 것) | 없으면 켜도 안 돈다 |
| `SHEET_ACCOUNT_GRACE_HOURS` | (없어도 됨) | 기다리는 **시간**. 기본 24시간, 최소 1시간 |

> ⚠ **`SHEET_ACCOUNT_GRACE_MS` 는 밀리초입니다.** 거기에 `24` 를 적으면 24시간이 아니라
> **24밀리초**입니다 — 0.64.1 에서 그 때문에 계정이 곧바로 지워지는 일이 있었습니다.
> 0.64.2 부터는 어느 이름을 쓰든 1시간 밑으로 안 내려가고, 너무 짧게 적으면
> 서버 로그와 운영자 창이 크게 알려 줍니다. 새로 적으실 때는 `..._HOURS` 를 쓰세요.

켠 뒤의 흐름:

1. 시트 **계정** 탭에서 줄을 지운다
2. 서버가 **삭제 예약**만 건다 (아직 안 지운다)
3. **그 계정에 우편으로 알린다** — "며칠 뒤에 지워집니다"
4. 24시간 뒤 확인에서 **실제로 지운다** — 계정·세이브·우편함·랭킹까지
5. 그 전에 **줄을 되돌려 놓거나**, 운영자 창 → 계정 의 예약 목록에서
   **되돌리기**를 누르면 취소된다
6. 지운 뒤에도 **30일 동안 휴지통**에 남는다 — 운영자 창 → 계정 아래쪽에서
   **되살리기**. 아이디·비밀번호·캐릭터·소지품·골드가 그대로 돌아온다
   (랭킹 기록만 안 돌아온다)

> **지우려면 두 가지가 다 지나야 합니다** — 기한, 그리고 **경고 우편이 나간 지 유예만큼.**
> 기한 하나만 보면 그 기한을 잘못 만든 어떤 사고든 곧바로 삭제가 됩니다.
> 우편을 못 보냈으면 시간이 아무리 지나도 안 지웁니다.

서버 로그에 전부 남습니다.

```
✓ 계정 탭 (자동) — 삭제 예약 2개: test01, test02
  2026-09-06T15:00:00.000Z 이후에 지웁니다. 그 전에 되돌릴 수 있습니다.
✓ 계정 탭 (자동) — 삭제 완료 2개: test01, test02
```

기다리기 싫으면 운영자 창 → 계정 에서 **「시트 계정 탭 지금 보기」**를 누릅니다
(예약을 앞당기지는 않습니다 — 시트를 지금 한 번 더 볼 뿐입니다).
급히 지워야 하면 예전처럼 **계정을 골라 지우기**를 쓰세요. 그쪽은 즉시입니다.

> ⚠ **시트를 고칠 수 있는 사람이 곧 계정을 지울 수 있는 사람입니다.**
> 켜기 전에 그 문서의 편집 권한을 확인하세요. 서버가 읽는 데는 **뷰어**면 충분합니다.
> 켜 두면 서버가 시작할 때마다 이 사실을 크게 알립니다.

---

## 안 될 때

| 증상 | 원인 |
|---|---|
| `이 서버는 … 꺼져 있습니다` | `ADMIN_KEY` 가 서버에 없습니다. Render 의 Environment 에 넣고 저장하세요 |
| `열쇠가 다릅니다` | 보낼 때 준 `ADMIN_KEY` 가 서버 것과 다릅니다 |
| `그런 아이템이 없습니다` | 아이디 오타입니다. `items` 탭 첫 칸에서 확인하세요 |
| 우편이 안 온다 | 게임에서 **우편함을 한 번 열어야** 서버에서 가져옵니다(접속 1.5초 뒤에도 한 번 자동으로 확인합니다) |
| 랭킹을 지웠는데 표가 그대로 | 화면이 옛 표를 들고 있습니다. 랭킹 창을 닫았다 열면 다시 받아 옵니다 |
| 계정 단이 '받아 오는 중…' 에서 멈춘다 | 0.64.1 에서 고쳤습니다. 그 전 판이면 **전체 우편 → 고른 사람만** 을 한 번 누른 뒤 계정 단으로 가면 열립니다 |
| `'…' 탭 — N번째 칸 제목이 다릅니다` | 앞 칸이 어긋났습니다(칸을 옮기거나 지웠습니다). 통합문서를 새로 올리세요. **뒤쪽 칸이 모자란 것만이라면** 0.64.1부터는 안 막고 알림만 뜹니다 |
| `'quests' 탭에 재료유지 칸이 없습니다` | 시트가 한 판 뒤처졌습니다. 없는 칸 값은 지켜 주지만 **새로 생긴 줄은 안 보입니다** — 통합문서를 갈아 끼우세요 |
| 시트에서 줄을 지웠는데 계정이 그대로 | ① `SHEET_ACCOUNT_DELETE=1` 을 안 넣었거나 ② 아직 24시간이 안 됐습니다. 운영자 창 → 계정 의 예약 목록을 보세요 |
| 운영자 창에서 지운 계정을 되돌리고 싶다 | 운영자 창 → 계정 → **휴지통** 에서 30일 안에 되살립니다. 0.64.3부터 **어느 길로 지우든** 휴지통을 거칩니다 |
| 계정이 **너무 빨리** 지워졌다 | 유예를 밀리초 칸(`..._MS`)에 `24` 처럼 적으면 24밀리초가 됩니다. 0.64.2 부터는 1시간으로 올리고 알려 줍니다. **지워진 계정은 운영자 창 → 계정 → 휴지통 에서 30일 안에 되살릴 수 있습니다** |
| `기한을 못 읽어 다시 잡았습니다` | 저장된 예약이 깨져 있었습니다. 지우지 않고 기한만 새로 잡은 것이니 그대로 두시면 됩니다 |
| `시트에 서버 계정 N개 중 M개만 있습니다` | 계정 탭이 낡았습니다. **「시트에 붙여 넣을 표 복사」**로 새로 올린 뒤 다시 보세요(그 사이 아무것도 안 지워집니다) |
| `한 번에 N개가 사라졌습니다` | 스무 개 넘게 한꺼번에 지웠습니다. 사고로 보고 멈춘 것입니다 — 나눠서 지우거나 운영자 창에서 지우세요 |
