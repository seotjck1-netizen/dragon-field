# 0.15 → 0.57 올리기

GitHub 에 **0.15.0** 이 올라가 있고 Render 가 그것을 보고 있는 상태에서,
지금 버전으로 갈아 끼우는 순서입니다.

세 부분입니다. 위에서부터 차례로 하면 됩니다.

| | 무엇 | 걸리는 시간 |
|---|---|---|
| **A** | GitHub 에 새 코드 올리기 | 10분 |
| **B** | Render 가 그걸 집어 가게 하기 | 5분 (대부분 기다리는 시간) |
| **C** | 구글 시트로 표 연결하기 | 10분 |

> **먼저 알아 둘 것 — 계정은 사라지지 않습니다.**
> 캐릭터는 Upstash(바깥 데이터베이스)에 있고, 코드를 올려도 거기는 건드리지 않습니다.
> 다만 **특성과 스킬은 전부 되돌려집니다** — 0.15 시절의 특성·스킬이 지금은 다른 것이라
> 점수를 그대로 옮길 수가 없습니다. 레벨에 맞는 포인트를 그대로 쥐고 다시 고르게 됩니다.
> 레벨·골드·소지품·강화 수치·붙은 옵션은 전부 그대로입니다.

---

## A. GitHub 에 올리기

### A-1. 지금 폴더가 그 저장소인지 확인

터미널을 열고 **0.15 를 올렸던 폴더**로 갑니다.

```bash
git remote -v
```

`origin  https://github.com/내아이디/…` 이 나오면 맞는 폴더입니다.
아무것도 안 나오면 그 폴더가 아닙니다 — 0.15 를 올릴 때 쓴 폴더를 찾으세요.

### A-2. 새 파일로 갈아 끼우기

받은 zip 을 풀면 `rpg-game` 폴더가 나옵니다. 그 **안의 내용물**을 저장소 폴더에 덮어씁니다.

> ⚠ **`.git` 폴더는 절대 지우지 마세요.** 그게 지워지면 GitHub 과의 연결이 끊깁니다.
> zip 안에는 `.git` 이 없으므로, "덮어쓰기"만 하면 안전합니다. **폴더를 통째로 지우고
> 새로 만들지 마세요.**

맥/리눅스라면 이렇게 하면 확실합니다(마지막 슬래시가 중요합니다):

```bash
# 압축을 푼 자리에서
rsync -a --exclude '.git' rpg-game/ /저장소/폴더/
```

윈도우 탐색기라면 zip 안의 `rpg-game` 폴더를 열고 **안에 있는 것 전부**를 복사해
저장소 폴더에 붙여 넣은 뒤 "덮어쓰기"를 고르면 됩니다.

### A-3. 올리기 전에 한 번 검사

```bash
cd /저장소/폴더
node tools/sheets.js check
```

`✓ 표에 문제가 없습니다.` 가 나와야 합니다.

### A-4. 올리기

```bash
git add -A
git status          # server/data/ 가 목록에 없어야 합니다
git commit -m "0.58.0 — 투구가 정수리를 덮는다 · 몬스터도 다가온다 · 용사 앞머리 · 표 기준 시각"
git push
```

`git status` 에 `server/data/` 가 보이면 **멈추세요.** 그건 계정 파일입니다.
`.gitignore` 에 이미 막아 두었으므로 보통은 안 나옵니다. 혹시 나오면:

```bash
git rm -r --cached server/data
```

---

## B. Render 쪽에서 할 일

### B-1. 기다리기 (대개 이것만으로 끝납니다)

Render 는 GitHub 에 새 커밋이 올라오면 **알아서 다시 배포합니다.**
대시보드 → 그 서비스 → **Logs** 를 보면 배포가 도는 게 보이고, 2~3분이면 끝납니다.

자동 배포가 꺼져 있다면 오른쪽 위 **Manual Deploy → Deploy latest commit** 을 누릅니다.

### B-2. 올라갔는지 확인

브라우저에서 서버 주소를 엽니다. 접속 화면 아래에 **`v0.58.0`** 이 보이면 성공입니다.
`v0.15.0` 그대로면 배포가 안 된 것이니 B-1 의 Manual Deploy 를 누르세요.

### B-2-1. 0.34 에서 새로 생긴 환경 변수

| 이름 | 안 넣으면 | 뜻 |
|---|---|---|
| `ADMIN_ID` | `admin` | 운영자 계정의 아이디 |
| `ADMIN_PW` | `@2222` | 운영자 계정을 **처음 만들 때만** 쓰는 비밀번호 |

`ADMIN_KEY` 가 있으면 서버가 켜질 때 운영자 계정을 알아서 만듭니다(이미 있으면 손대지 않습니다).

> ⚠ **공개 서버라면 `ADMIN_PW` 를 꼭 정하세요.** `@2222` 는 짧고 문서에 적혀 있습니다.
> 이 계정은 전 유저에게 우편을 뿌리고 랭킹을 지울 수 있습니다.
> 이미 기본값으로 만들어졌다면, `ADMIN_PW` 를 정한 뒤 그 계정을 지우고 서버를 다시 켜면 됩니다.

### B-2-000000000000. 0.57 에서 달라지는 것

> **표는 안 바뀌었습니다.** 시트를 다시 올릴 필요가 없습니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **투구·모자** | 때리는 순간 몸을 따라 같이 기웁니다(예전에는 머리에서 떨어졌습니다) |
| **용사** | 상대와의 중간까지 달려나갔다 돌아오며 벱니다 |
| **화살통** | 34° → 22° |
| **소리** | 칼바람 · 시위 · 날아가는 불덩이가 새로 납니다 |
| **접속 화면** | 오른쪽 위 스피커 단추로 배경 음악을 켜고 끕니다 |

### B-2-00000000000. 0.56 에서 달라지는 것

> ⚠ **표가 한 칸 바뀌었습니다** — `classes` 의 마법사 **끌어오기 범위** 가 3 → **4**.
> `npm run sheets` 로 구운 새 xlsx 를 **같은 문서에** 덮어써 주세요.
> (안 바꾸면 다음 시트 읽기 때 3칸으로 되돌아갑니다 — 시트가 저장소보다 셉니다)

| 무엇 | 어떻게 되나 |
|---|---|
| **전투 자세** | 세 직업 모두 전투에서 싸울 자세로 서 있습니다(새 그림 셋이 늘었습니다) |
| **첫 화살 각도** | 쏘는 화살과 날아가는 화살이 같은 쪽(2시)을 봅니다 |
| **화살통** | 사냥꾼 얼굴을 안 가립니다 |
| **파이어볼** | 마법사의 마법이 불덩이로 바뀌고 터지는 것이 커졌습니다 |
| **용사 얼굴** | 다시 그렸습니다 |
| **마법사 끌어오기** | 3칸 → 4칸 (최대 4마리는 그대로) |
| **몬스터 세기** | **하나도 안 바뀌었습니다.** 밸런스 수치가 0.55 와 같습니다 |

### B-2-0000000000. 0.55 에서 달라지는 것

> **표가 조금 바뀌었습니다** — `classes` 의 마법사 시작 아이템에 '천 두건' 이 들어갔습니다.
> 시작 아이템은 **시트에 없는 칸**이라 시트를 다시 올리지 않아도 됩니다.
> 이미 만들어 둔 마법사 캐릭터는 두건을 갖고 있지 않으므로 **맨머리로 나옵니다** —
> 마을 상점에서 30골드에 살 수 있습니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **마법사 모자** | 그림에서 빼고 '천 두건' 아이템으로 옮겼습니다. 투구를 끼면 바뀝니다 |
| **사냥꾼 얼굴** | 용사와 닮았던 것을 통째로 다시 그렸습니다 |
| **전투 GIF** | `npm run gif` 로 전투 동작을 녹화합니다(개발용 도구) |

### B-2-000000000. 0.54 에서 달라지는 것

> ⚠ **표가 바뀌었습니다 — 새 xlsx 를 구워 시트에 덮어써 주세요.**
> `npm run sheets` 로 구운 `dragon-field-tables.xlsx` 를 **같은 문서에** 가져오기 하세요.
> 바뀐 곳:
> * `items` — 투구 넷이 늘었습니다(천 두건 · 가죽 모자 · 기사 투구 · 룬 투구).
>   매직 투구의 `기타` 칸에 `{"mods":{"sightBonus":2,"dungeonStealth":1}}` 이 들어갑니다.
> * `drops` — 해골이 기사 투구를 2% 로 떨굽니다.
> * `classes` — 마법사의 **`보호막 무적 시간`(밀리초)** 칸이 **`보호막 막는 횟수`(대)** 로 바뀌었습니다.
> * `skills` — **`위기의 피해 증가`** 가 **`위기의 치명타 피해`** 로, **`보호막 지속`** 이
>   **`보호막 막는 횟수`** 로 바뀌었습니다.
>
> 칸 이름이 바뀐 자리는 **옛 시트를 그대로 두면 서버가 그 줄을 못 찾습니다.**
> 반드시 새 xlsx 로 갈아 끼우세요.

| 무엇 | 어떻게 되나 |
|---|---|
| **직업 얼굴** | 마법사로 다시 접속하면 용사(남자) 얼굴이 나오던 것을 고쳤습니다. 얼굴은 이제 언제나 직업에서 나옵니다 |
| **투구 겉모습** | 투구를 끼면 머리에 쓴 모습이 그려집니다. 투구가 넷 늘었습니다 |
| **매직 투구** | 지하감옥에서만 몹이 먼저 못 알아채고, 시야가 두 칸 넓어집니다 |
| **보석** | 두 번 눌러야 박힙니다(오폭 방지). 무엇이 오르는지 이름으로 적힙니다 |
| **관통** | 전투력에 들어갑니다. `node tools/pierce-check.js` 가 값어치를 재고 그림을 그립니다 |
| **보호막** | 시간이 아니라 **횟수**로 셉니다(기본 1대, 연장 2점마다 +1대 · 최소 한 번은 반격) |
| **위기의 치명타 피해** | HP 가 임계 아래일 때 **치명타 피해**가 오릅니다(예전에는 그냥 피해였습니다) |
| **전투 동작** | 검을 휘두르고, 마법과 화살이 날아가고, 회피·부활·오로라 효과가 붙습니다 |
| **지하감옥 5층** | 주인을 눕히면 웨이포인트가 열립니다 |
| **용린 4세트** | 고룡 앞의 스탯 덤이 100 → **50**. 두 번째 용은 그대로입니다 |
| **운영자 창** | 목록에서 분류를 골라도 스크롤이 안 튑니다 |

### B-2-00000000. 0.53 에서 달라지는 것

> ### `1 시즌` 을 칠 수 있게 됐습니다
>
> 운영자 창에 확인 글을 적을 수가 없었습니다. 원인은 둘이었습니다.
>
> **① 게임이 키를 먼저 집어 갔다.** `1`·`2`·`3`·`4` 는 단축칸, 스페이스는 확인,
> `W`·`A`·`S`·`D` 는 이동입니다. 예전에는 **어디에 포커스가 있든** 이 키들을
> 막아 버렸습니다. 이제 글자 칸에 있으면 게임은 손을 뗍니다.
>
> **② 한 글자마다 창을 다시 그렸다.** 영어는 그럭저럭 됐지만 한글은 조합이
> 깨집니다 — 'ㅅ' 을 친 순간 칸이 새로 만들어져 '시' 로 못 이어집니다.
> 확인 글 칸은 이제 **단추 잠금만** 그 자리에서 풀고, 찾기 칸은 **조합이 끝난 뒤**
> 다시 그립니다. 접속 화면도 다시 그려질 때 적던 아이디를 지킵니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **글자 입력** | 칸 안에서는 게임이 키를 안 가져갑니다. 한글 조합도 안 끊깁니다 |
| **전투 화면** | 화면을 통째로 덮습니다 — 아래에 필드가 안 비칩니다 |
| **전투 글** | `23 데미지를 주었다 (치명타!)` 처럼 한 줄. 세로에서는 단축칸 위로 올립니다 |
| **여덟 갈래 이동** | 화살표 둘을 같이 누르면 대각선. 화면 패드도 여덟 칸입니다 |

> ### 대각선 규칙 셋
>
> - **√2 배 오래 걸립니다** — 안 그러면 대각선으로만 다니는 게 41% 빠릅니다
> - **모서리는 못 뚫습니다** — 옆 두 칸 중 하나라도 벽이면 대각선으로 못 갑니다
> - **바라보는 쪽은 좌우가 이깁니다** — 대각선 그림이 없습니다

### B-2-0000000. 0.52 에서 달라지는 것

> ### 시트 읽기를 **뿌리째** 고쳤습니다 — 서버만 올리면 끝입니다
>
> ```
> 'classes' 탭 — classes 시트 2번째 줄: 이름은 비울 수 없습니다.
> ```
>
> 시트에는 `용사` 라고 멀쩡히 적혀 있습니다. **지운 것은 구글이었습니다.**
>
> 서버가 쓰던 `gviz` 통로는 **열마다 자료형을 스스로 정합니다.** `classes` 탭의
> `값` 열은 `용사`·`예` 같은 글자와 `48`·`11` 같은 숫자가 섞여 있는데,
> 숫자가 더 많으니 숫자 열로 보고 **글자 칸을 빈칸으로 지워서** 줬습니다.
> `classes` 만 걸린 이유도 그것입니다 — 다른 탭은 한 열에 한 가지만 들어 있습니다.
>
> 0.50 에서는 같은 통로가 **제목 줄을 몇 줄로 볼지** 짐작해서 막혔습니다
> (`직업 warrior warrior warrior`). 짐작하는 통로를 계속 달래는 대신
> **통로를 갈아 치웠습니다.**
>
> | | 0.51 까지 | 0.52 |
> |---|---|---|
> | 무엇을 받나 | 탭마다 CSV **열 번** | 통합문서 **한 번** (`export?format=xlsx`) |
> | 구글이 짐작하는 것 | 제목 줄 수 · 열의 자료형 | **없음** — 칸에 적힌 그대로 |
> | 반쯤 읽다 마는 일 | 있었다 | 없다(한 번에 다 받는다) |
>
> **시트에서는 아무것도 안 하셔도 됩니다.** 서버를 올리고 `시트 지금 읽기` 를 누르세요.

| 무엇 | 어떻게 되나 |
|---|---|
| **시트 읽기** | 통합문서 한 장을 그대로 읽습니다. 짐작하는 자리가 사라졌습니다 |
| **탭 이름이 다를 때** | "'items' 탭이 문서에 없습니다. 탭 이름을 그대로 두세요" 라고 짚어 줍니다 |
| **시트에서 고치신 값** | 슬라임 체력 44 → **24**, 슬라임 약초 25% 1개 → **100% 5개** 를 가져왔습니다 |

> ### 슬라임 값 — 시험 삼아 고치신 것이면 되돌려 주세요
>
> 시트에 슬라임 체력이 24, 약초가 100% 5개로 되어 있어 **그대로 가져왔습니다**
> (시트에서 고치신 값은 언제나 저장소보다 셉니다). 시트 연결이 되는지 보시려고
> 넣으신 값이라면, 시트에서 원래 값(44 · 0.25 · 1 · 1)으로 되돌리고
> `시트 지금 읽기` 를 누르시면 됩니다.

### B-2-000000. 0.51 에서 달라지는 것

> ### 시트 주소를 **운영자 창에서** 바꿉니다 — 재배포 없이
>
> 문서 아이디가 **판마다 바뀌고 있었습니다.** 시트를 새로 만들어 올리면 아이디가
> 달라지는데, 그때마다 저장소의 `sheets/SOURCE.txt` 를 고치고 다시 배포해야
> 서버가 새 문서를 봤습니다. 그 사이 서버는 없어진 문서를 두드리며 `401` 만 받았고,
> 화면에는 "공개했는지 보세요" 만 떠서 **멀쩡한 시트의 공개 설정**을 붙들게 했습니다.
> 0.45 부터 0.51 까지 여섯 판 연속으로 같은 일이 났습니다.
>
> 이제 운영자 창 **표** 단이 이렇게 생겼습니다:
>
> ```
>   지금 보는 문서
>   19yz0jylJE3PcA5A7Py8pnk6focMzJYCdzepM09KpmzM
>   저장소의 sheets/SOURCE.txt
>
>   시트 주소  [ https://docs.google.com/spreadsheets/d/…        ]
>   [ 주소 저장 ]  [ 원래대로 ]
>   [       시트 지금 읽기       ]
> ```
>
> 주소를 붙여 넣고 **주소 저장** 하면 그때부터 그 문서를 봅니다.
> 여기서 정한 값이 **환경변수보다 셉니다** — 그래야 환경변수가 낡았을 때도
> 재배포 말고 다른 길이 생깁니다. `원래대로` 를 누르면 지워지고 되돌아갑니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **표 단 맨 위** | 지금 보고 있는 문서 아이디와 그 값이 어디서 왔는지 |
| **시트 주소 칸** | 주소를 붙여 넣고 저장 — 서버 재배포 없음 |
| **401 · 404 오류 글** | "그 문서가 없어졌거나 공개가 풀렸습니다. 표 단에서 주소를 넣어 보세요" |
| **`upload.bat`** | 깃허브에 올리는 스크립트를 폴더에 넣었습니다(아래) |

> ### `upload.bat` — 더블클릭으로 깃허브에 올리기
>
> 폴더 안에 `upload.bat` 이 함께 들어갑니다. 더블클릭하면 됩니다.
> 예전에 쓰시던 것과 셋이 다릅니다:
>
> - `cd rpg-game` 을 **안 합니다.** 이 파일이 이미 그 폴더 안에 있어서, 그 줄이
>   있으면 "폴더를 찾을 수 없습니다" 로 끝났습니다
> - 커밋 글의 판 번호를 `package.json` 에서 **읽습니다.** 손으로 적으면 언젠가
>   `Update0.44.0` 인 채로 0.51 을 올리게 됩니다
> - `.gitignore` 가 없으면 만들어 둡니다 — **`server/data/`(계정과 비밀번호)를
>   절대 안 올리기 위해서**입니다

### B-2-00000. 0.50 에서 달라지는 것

> ### 시트 읽기가 다시 됩니다 — 서버만 올리면 끝입니다
>
> 운영자 창에 이렇게 뜨던 것:
>
> ```
> 'classes' 탭 — classes 시트의 첫 줄이 제목 줄이 아닙니다.
> 첫 칸은 '직업' 이어야 합니다. 지금: '직업 warrior warrior warrior'
> ```
>
> **시트는 멀쩡했습니다.** 구글의 `gviz` 통로가 제목 줄이 몇 줄인지 스스로 짐작하는데,
> `classes` 탭은 첫 칸이 죽 글자라(`직업` / `warrior` / `warrior` …) 앞의 네 줄을
> 통째로 제목으로 보고 **한 줄로 이어 붙여** 돌려줬습니다.
>
> 주소에 `headers=1` 을 붙여 "제목은 한 줄" 이라고 못 박았습니다.
> **시트에서는 아무것도 안 하셔도 됩니다.** 서버를 올리고 `시트 지금 읽기` 를 누르세요.

| 무엇 | 어떻게 되나 |
|---|---|
| **시트 읽기** | `headers=1` 로 고정. 같은 일이 또 나면 오류 글이 "제목과 아랫줄이 붙어 왔습니다" 라고 짚어 줍니다 |
| **밸런스 기준 몸** | 10단계 Lv.14·+7·매직 세 점 / 15단계 Lv.20·+7·희귀 여섯 점 / 20단계 Lv.27·+7·룬 한 벌 |
| **`몸 견주기` 줄** | `node tools/balance.js` 가 사람과 상대의 체력·공격·방어를 나란히 찍습니다 |

> ### ⚠ 새 기준으로 재면 10·15·20단계가 **0%** 입니다
>
> 게임 값을 바꾼 것이 아니라 **자를 바꾼 것**입니다. 예전 자는 10단계를
> Lv.24 · +5 · 여덟 점으로 상상하고 있었고, 그 상상에 맞춰 맵 배율을 풀어 두었습니다.
>
> ```
> 10단계 보스 · 발가르   사람 hp 435 · atk 129     상대 hp 4418 · atk 904
> ```
>
> 실제 몸으로 사다리를 다시 풀면 배율이 9.5 → **2.8**(10단계), 4.8 → **1.0**(15단계),
> 9.1 → **2.8**(20단계) 로 내려가야 합니다. **아직 안 고쳤습니다** — 배율은
> 경험치·골드도 같은 배수로 올리기 때문에, 3분의 1로 내리면 레벨 오르는 속도가
> 통째로 바뀝니다. 난이도와 성장 속도를 함께 정하고 나서 손대는 것이 맞습니다.

### B-2-0000. 0.49 에서 달라지는 것

> **표가 바뀝니다** — `monsters` 두 줄(보스 체력)과 `drops` 여러 줄(보석).
> 시트를 쓰고 계시면 `npm run sheets` 로 구운 새 xlsx 를 **같은 문서에
> 덮어쓰기로 가져오기** 하세요. 칸 생김새는 0.48 그대로라 당기기는 멈추지 않지만,
> 옛 시트를 그대로 두고 당기면 **이번에 고친 값이 옛 값으로 되돌아갑니다.**

| 무엇 | 어떻게 되나 |
|---|---|
| **퀘스트 의뢰 목록이 제자리로** | 목록이 왼쪽 아래로 내려가 있었습니다. 다시 오른쪽 칸입니다 |
| **퀘스트 보상 = 상점 물건** | 퀘스트로 받는 장비도 **각인을 굴립니다**. 그동안 퀘스트 것만 안 굴려서, 같은 용린 어깨갑옷인데 상점 것과 달라 보였습니다 |
| **동쪽 필드에 보석 없음** | 1~20단계 잡몹·보스의 보석 드랍을 전부 뺐습니다. **늑대의 사파이어 1% 만** 이벤트로 남습니다 |
| **용린 4세트 글** | "용에게 특별한 힘이 발휘한다" 한 줄로 줄였습니다(두 줄이던 것을 하나로) |
| **5단계 보스 체력 103 → 150** | 같은 땅의 꼬마 악마(118)보다 얇았습니다 |
| **10단계 보스 체력 232 → 464** | 같은 땅의 악마 병사(260)보다 얇았습니다. 두 배로 올렸습니다 |

> ### ⚠ 10단계 보스가 **많이** 어려워졌습니다
>
> 체력을 두 배로 올리면 승률이 **36% → 4%** 로 떨어집니다(Lv.24 · +5 · 10단계 장비 기준).
> 20단계 보스(40%)나 지하감옥 주인(36%)보다 어려워져, 지금은 10단계가
> 게임에서 가장 높은 벽입니다. 너무 세다 싶으면 `monsters` 시트의
> 발가르 hp 를 348(1.5배) 쯤으로 내려 보세요 — 그 자리는 약 18% 입니다.

> ### 보스가 제 땅 잡몹보다 센지 재는 자를 만들었습니다
>
> `node tools/boss-check.js` — 어떤 땅의 보스는 그 땅에 서는 잡몹 누구보다도
> 체력과 공격력이 높아야 한다는 규칙 하나만 봅니다.
> 숫자가 `monsters` 와 `maps` 두 곳에 나뉘어 있어 눈으로는 잘 안 보입니다.
> 회귀에도 넣어 두었습니다.

### B-2-000. 0.48 에서 달라지는 것

> ### ⚠ `monsters` 시트에 **칸이 둘 늘었습니다** — 시트를 꼭 다시 올리세요
>
> `등장대사` 와 `쓰러질때` 입니다(`전투그림` 뒤, `기타` 앞).
> 서버는 칸을 **자리로** 읽으므로, 옛 시트를 그대로 두면 당기기가 그 자리에서
> 멈추고 어느 칸이 다른지 알려 줍니다. 게임은 이전 표로 계속 돌지만
> **그때부터 시트에서 무엇을 고쳐도 반영되지 않습니다.**
>
> `npm run sheets` 로 새 xlsx 를 구워 **같은 문서에 덮어쓰기로 가져오기** 하세요.

| 무엇 | 어떻게 되나 |
|---|---|
| **마법사가 세졌습니다** | 시트에서 고치신 값입니다 — 마력 증폭 6%→**20%**, 보호막 300→**500**(최대 4), 메테오 0.14→**0.25**(최대 4) |
| **보스 대사** | 등장·쓰러짐에 한 줄씩. 여덟 보스에 미리 적어 두었습니다. 시트에서 고치시면 됩니다 |
| **고룡 등장이 커졌습니다** | 그림이 아래에서 솟아오르고 화면을 통째로 씁니다(2.8초). 다른 보스는 그대로 |
| **쓰러짐 연출** | 보스를 눕히면 결과창 **앞에** 한 컷. 푸른빛으로 가라앉습니다 |
| **마지막 접속 시각** | 계정 목록에 `접속 오늘 17:17 · 만듦 8/20` 처럼 붙습니다 |

> ### 마법사가 제자리를 찾았습니다
>
> 다섯 자리 중 **넷이 목표 안**에 들어왔습니다(5단계 45→60%, 10단계 15→28%,
> 15단계 18→30%, 20단계 23→40%, 지하감옥 30→43%).
> 이제 뒤처지는 쪽은 **사냥꾼**입니다 — 지하감옥 14%, 20단계 23%.

### B-2-00. 0.47 에서 달라지는 것

> ⚠ **표가 한 줄 바뀌었습니다** — `skills` 의 철벽(방어력 20% → **15%**).
> 시트에서 고치신 값을 가져온 것이라 **시트를 쓰고 있다면 이미 그 값입니다.**

| 무엇 | 어떻게 되나 |
|---|---|
| **철벽 20% → 15%** | 용사가 또 한 단 내려옵니다(20단계 65%→56%, 지하감옥 63%→51%). 직업 격차 49%p → **37%p** |
| **보스 등장 연출** | 보스와 붙으면 이름표가 1.4초 가로질러 들어왔다 나갑니다. 그동안 전투는 멈춥니다 |
| **계정 목록이 만든 날짜순** | 새로 만든 계정이 위에. 줄마다 `오늘 09:18` 처럼 언제 만든 것인지 붙습니다 |
| 손 안 댄 것 | 용사 초반 · 마법사 · 5~9단계 — 요청대로 그대로입니다 |

### B-2-0. 0.46 에서 달라지는 것

> ⚠ **표가 바뀌었습니다** — `monsters` 의 1~4단계 넷과 `skills` 의 전투 본능.
> 시트에서 고친 값(슬라임·박쥐·몽둥이)을 가져온 뒤 이어서 맞춘 것이라,
> **`npm run sheets` 로 새 xlsx 를 구워 시트에 덮어써 주세요.**

| 무엇 | 어떻게 되나 |
|---|---|
| **1~4단계가 순해졌습니다** | 슬라임 hp 155→44 · 박쥐 115→51 · 늑대 163→99 · 버섯 62→75(공격은 낮춤). 경험치·골드는 **그대로**입니다 |
| **전투 본능 10% → 8%** | 용사가 조금 내려옵니다(20단계 80%→65%, 지하감옥 76%→63%) |
| **5단계 보스는 그대로** | 요청대로 손대지 않았습니다 — 여전히 목표보다 어렵습니다 |
| **계정 지우기** | 운영자 창에 **계정** 단이 생겼습니다. 고른 사람을 통째로 지웁니다(랭킹 기록까지) |
| **보스 전용 BGM** | 보스와 붙으면 곡이 바뀌고, 결과창을 닫으면 그 땅 곡으로 돌아옵니다 |

> ### 첫 슬라임이 벽이었습니다
>
> 밸런스 도구가 1단계를 **Lv.3** 으로 재고 있었습니다. 그런데 슬라임은 **Lv.1 에**
> 만납니다 — 마을 문을 나서면 바로 거기입니다. 그래서 "1단계 95%" 를 맞춰 놓고도
> 실제 첫 싸움은 **33%** 였습니다. 목표는 맞았는데 사람은 첫 슬라임에게 세 번 중
> 두 번을 졌습니다.
>
> 틀린 것은 몬스터가 아니라 **자에 적힌 레벨**이었습니다. 이제 1~4단계를
> **그 땅에 처음 발을 들이는 레벨**(Lv.1 · 3 · 5 · 8)로 잽니다.

### B-2-1-0. 0.45 에서 달라지는 것

**표는 안 바뀌었습니다.** 시트를 다시 올릴 필요가 없습니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **시즌 고정** | 운영자 창 랭킹 단. 켜 두면 어떤 초기화도 시즌을 안 넘깁니다 — **시험 중인 서버용** |
| **1 시즌으로 되돌리기** | 이미 올라간 번호를 1 로 내립니다. 기록도 세이브도 안 지웁니다 |
| **여태 보낸 우편 모두 지우기** | 운영자 창 전체 우편 단 맨 아래. 배달 대기 중인 전체 발송까지 함께 지웁니다 |
| **시즌 번호 한 살 밀림** | 초기화 알림이 랭킹 창보다 하나 컸던 것을 고쳤습니다 |
| **표 새로고침이 그냥 됩니다** | `SHEET_ID` 환경변수를 안 넣어도 저장소의 `sheets/SOURCE.txt` 에 적힌 문서를 읽습니다 |

> **"SHEET_ID 가 설정되어 있지 않습니다" 가 뜨던 자리입니다.** 이제 그 환경변수가 없어도
> 저장소가 기억하고 있는 문서를 씁니다. 환경변수를 넣어 두었다면 그쪽이 계속 이깁니다.
> 그 문서는 여전히 **"링크가 있는 모든 사용자 — 뷰어"** 로 공개되어 있어야 합니다.

> **시험 중이라면 지금 바로 할 일은 하나입니다** — 운영자 창 → 랭킹 → **시즌 고정을 켜세요.**
> 그러면 몇 번을 초기화해도 1 시즌 그대로입니다. 이미 번호가 올라가 있다면
> 그 아래 **1 시즌으로 되돌리기** 로 먼저 내린 뒤 고정을 켜면 됩니다.

### B-2-1-1. 0.44 에서 달라지는 것

> ⚠ **표가 바뀌었습니다** — `skills` 의 용사 스킬 셋과 `stats` 의 힘.
> 시트에서 고친 값을 가져온 것이라, **시트를 쓰고 있다면 이미 그 값입니다.**
> `npm run sheets` 로 새 xlsx 를 구워 두면 다음 판에서 헷갈리지 않습니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **용사가 방어로 때립니다** | 전투 본능 6% → **10%**, 철벽 최대 4 → **5**, 가시 갑옷 25%(최대 4). 힘의 공격력은 3 → **1** |
| **찍어 둔 포인트** | 그대로입니다. 최대 랭크가 줄어든 가시 갑옷만 4 로 맞춰집니다 |
| **몬스터가 덤벼듭니다** | 열한 종류 모두 공격 그림이 생겼습니다. 늑대는 발톱, 고룡은 숨 |
| **소리가 곳곳에** | 마을·들판·지하감옥·고룡 BGM 넷과 효과음 열다섯. **배경 음악 기본값이 켬으로 바뀝니다** |
| **치명타 글씨** | 크고 굵고 금빛으로 튀어오릅니다. 세게 맞을수록 글씨도 이펙트도 커집니다 |

> ### ⚠ 용사가 많이 세졌습니다
>
> 위 다섯 줄로 **15단계 보스 13% → 51%, 20단계 3% → 80%, 지하감옥 5층 0% → 76%** 가
> 되었습니다. 세 판째 "용사가 처진다" 던 자리는 풀렸지만, 이번에는 용사가 혼자
> 앞서 나갑니다(직업 격차 최대 62%p).
>
> **값은 하나도 되돌리지 않았습니다** — 시트에서 고친 것을 말 없이 되돌리지 않는 것이
> 규칙입니다. 어느 쪽으로 맞출지는 `SHEET-FIRST.md` 의 0.43 → 0.44 절에 적어 두었습니다.

### B-2-2. 0.42 에서 달라지는 것

> ⚠ **표가 바뀌었습니다.** `items` 의 송곳 이름·설명, `drops` 의 감옥의 주인 보석 개수,
> 그리고 `classes` 의 **'힘 성장주기' 칸이 '힘 성장 차례'로 이름이 바뀌었습니다.**
> `npm run sheets` 로 새 xlsx 를 구워 갈아 끼우세요.

| 무엇 | 어떻게 되나 |
|---|---|
| **스탯이 정말 3:2:1 로 자랍니다** | 표의 1·2·3 을 '몇 레벨마다'가 아니라 **차례**로 읽습니다. 한 레벨 총량(11/6점)은 그대로라 **다 키운 총점은 안 바뀝니다.** 이미 키운 캐릭터의 스탯은 **깎지 않습니다** |
| **예측 공격이 화면에 보입니다** | 선제 사격 로그에 `예측 +210%`, 피해 숫자는 초록 🎯 |
| **보스 리젠은 1분이 규칙** | 값이 바뀐 것은 없습니다 — 이미 그랬고, 이번에 문서로 못 박았습니다. 고룡만 예외(30분 주기 · 아그라모스는 죽은 뒤 1시간) |
| **감옥의 주인 보석 원복** | 0.41 에서 두 배로 했던 개수를 되돌립니다(1~2개). 에메랄드·오닉스는 그대로 나옵니다 |
| **홈 뚫는 송곳 → 악마의 파편** | 이름과 설명만 바뀝니다. 갖고 있던 것은 그대로 쓰입니다(`socket_drill` id 는 안 바뀜) |
| **초기화 + 선물 한 번에** | 운영자 창의 확인 칸 아래 체크가 하나 늘었습니다. 기본은 켜짐 |

### B-2-3. 0.41 에서 달라지는 것

> ⚠ **표가 바뀌었습니다.** `skills` 에서 '연발 사격'이 빠지고 '예측 공격'이 들어왔으며,
> `monsters`·`drops` 의 감옥의 주인 줄이 바뀌었습니다. 구글 시트를 쓰고 있다면
> `npm run sheets` 로 새 xlsx 를 구워 갈아 끼우세요.

| 무엇 | 어떻게 되나 |
|---|---|
| **연발 사격 → 예측 공격** | 화살 수를 늘리는 대신 **한 발의 위력**을 올립니다(1포인트 +70%, 3포인트 +210%). **찍어 둔 포인트는 접속할 때 자동으로 돌아옵니다** — 잃는 것은 없습니다 |
| **죽음의 일격 30% → 20%** | 사냥꾼이 조금 약해집니다. 이미 찍은 포인트는 그대로이고 값만 바뀝니다 |
| **감옥의 주인 보상** | 경험치 +50% · 골드 +50% · 보석 두 배. 에메랄드와 오닉스가 새로 나옵니다 |
| **시즌 시작 선물** | 운영자 창 랭킹 단, 초기화 단추 옆. 한 번 누르면 전 유저에게 시작 꾸러미가 갑니다 |

**밸런스 재는 도구의 버그도 하나 고쳤습니다.** `tools/balance.js` 가 맵 배율을 미리 숫자로
바꿔 넘기는 바람에 **그 땅의 마법 피해 몫을 0 으로 재고 있었습니다.** 지하감옥은 절반이
마법이라, 그 탓에 감옥의 주인이 16%(너무 어려움)로 보였습니다 — 실제로는 28% 였습니다.
값을 고친 것이 아니라 **자를 고친 것**이라, 게임 안에서 달라지는 것은 없습니다.

### B-2-4. 0.40 에서 달라지는 것

> ⚠ **표가 바뀌었습니다.** `monsters` 에 새 줄(`dungeon_general`)이 하나 늘고,
> `drops` 에 스무 줄 남짓 늘었습니다. 구글 시트를 쓰고 있다면 `npm run sheets` 로
> 새 xlsx 를 구워 갈아 끼우세요. 안 그러면 시트가 새 몬스터를 모르는 상태로
> 당기기가 걸립니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **초월 강화가 성으로 옮겨졌습니다** | 마녀 → **성 안 왕실 대장장이 도르한**. 값은 용의 징표 → **보석 한 개**(무기 루비 · 방어구 에메랄드 · 장신구 오닉스). 확률 90·80·70·60·50%, **실패해도 안 내려갑니다.** 이미 +11 이상인 장비는 그대로입니다 |
| **보스가 세게 때립니다** | 감옥의 주인 공격 208 → 350(대신 체력 43,960 → 10,000), 고룡 1,050 → **7,000**, 아그라모스 7,406 → 9,500. 목표 승률은 그대로입니다 |
| **4층에 새 몬스터** | '봉인된 장군 발가르'. 20단계 들판의 '강화된 발가르'와 값을 나누지 않으려고 따로 세웠습니다 — **들판 난이도는 그대로입니다** |
| **1~5단계 보석** | 루비·에메랄드·오닉스가 2%씩 나옵니다(없던 줄은 새로 넣었습니다). 악마의 핵은 세 배 |
| **지하감옥 계단** | 위로 나가는 자리에 계단 그림이 생깁니다. 지도만 다시 그리면 됩니다(`npm run maps`) |
| **전체 유저 초기화** | 운영자 창 랭킹 단 맨 아래. 누르면 **모두가 처음으로** 돌아갑니다 — 뒤의 설명을 꼭 읽으세요 |
| **피통 · 소지품** | 체력 숫자가 안 잘리고, 소지품이 소제목으로 나뉩니다. 값은 안 바뀝니다 |

**옛 세이브는 그대로 들어옵니다.** 초월 강화 규칙이 바뀌었을 뿐, 이미 올려 둔 +11~+15 는
건드리지 않습니다. 마녀에게 걸러 갔다가 "이제 안 한다"는 말을 듣게 될 뿐입니다.

### B-2-5. 0.39 에서 달라지는 것

> ⚠ **시트에 탭이 하나 늘었습니다 — `engrave`.**
> 구글 시트를 쓰고 있다면 `npm run sheets` 로 새 xlsx 를 구워 갈아 끼우세요.
> 탭이 없으면 그 탭에서 당기기가 멈춥니다(게임은 이전 표로 그대로 돕니다).
> 시트를 안 쓴다면 아무것도 안 해도 됩니다 — 값은 `src/data/affixes.json` 에 그대로 있습니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **초월 각인 알림** | 붉은 띠 알림이 조금 더 오래 머물고, 전투 결과 카드에도 한 줄이 남습니다 |
| **장비 칸 · 판매 목록** | 초월은 ✦✦ 와 붉은 글씨로 보입니다 — 모르고 파는 일을 막습니다 |
| **상점 장비 비교** | 사기 전에 지금 낀 것과의 차이가 ▲▼ 로 붙습니다. 값이 달라지는 것은 없습니다 |
| **각인 확률이 등급별로** | 표에 칸이 둘 늘었습니다(붙을 확률 · 초월 확률). **비워 두면 지금까지와 같습니다** — 다섯 등급 모두 10% |

**이미 붙어 있던 각인·초월은 그대로입니다.** 이번 판은 보여 주는 방법과 표를 두는 자리를
바꾼 것이지, 이미 만들어진 물건을 다시 굴리지 않습니다.

### B-2-6. 0.38 에서 달라지는 것

**표에는 손댈 것이 없습니다** — 이번 판은 코드와 `affixes.json`(시트 밖 파일)만 바뀌었습니다.
0.37 시트를 이미 올렸다면 그대로 두면 됩니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **각인이 세졌습니다** | 등급을 탑니다(일반 3~5 · 희귀 5~10 · 영웅 10~15 · 전설 20~30). **이미 붙어 있던 각인은 그대로**이고, 앞으로 줍는 것부터 달라집니다 |
| **초월 각인** | 각인 열에 하나는 모든 수치가 최대로 굳습니다. 붉은 ✦✦ 로 보입니다 |
| **소지품 창** | 입고 있는 것은 아이템 목록에서 빠지고 장비 띠에만 남습니다. 장비 띠를 눌러도 이제 안 벗겨집니다 |
| **장비 비교** | 안 입은 장비를 고르면 지금 낀 것과의 차이가 초록 ▲ / 빨강 ▼ 로 뜹니다 |
| **촌장** | 두 칸 아래로 옮겼고, 연달아 두 번 말을 걸면 다른 이야기를 합니다 |
| **고룡2** | 용린 4세트가 아그라모스 앞에서 훨씬 크게 붙습니다 — 한두 판이면 눕습니다 |

### B-2-7. 0.37 에서 달라지는 것

**환경 변수는 늘지 않았습니다.** 대신 이런 것들이 달라집니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **두 번째 고룡** | 서쪽 절벽 서쪽 끝에 잠긴 문이 생깁니다. 마녀에게 징표 10개로 열쇠를 사면 열립니다 |
| **직업별 기사무기** | 쓸모없는 검이 **그 사람 직업에 맞는** 검·활·지팡이가 됩니다. 이미 만든 기사검은 그대로입니다 |
| **특별 의뢰** | 고룡을 만나 본 사람에게 `쓸모를 잃은 무기`(10만 골드)가 열립니다. 옛 세이브는 다시 만나면 그때 열립니다 |
| **마법 피해** | 11단계부터 공격의 일부가 마법으로 들어옵니다. 지능을 올린 사람이 그만큼 덜 맞습니다 |
| **마법사 1.1배** | 마법사가 주는 피해가 10% 늘어납니다 |
| **매직 투구** | 성 안 무기상 진열에 한 줄 늘어납니다 |

> ⚠ **표에 칸이 둘 늘었습니다** — `quests` 탭의 `열림조건`, `classes` 탭의 `마법 전환 배율`.
> 서버는 칸을 **자리로** 읽으므로 시트를 안 올리면 당기기가 그 자리에서 멈춥니다.
> `items`·`monsters`·`drops` 에도 줄이 늘었습니다(용의 열쇠 · 아그라모스 · 그 드랍).
> `npm run sheets` 로 새 xlsx 를 구워 갈아 끼우세요.

### B-2-8. 0.36 에서 달라진 것

**환경 변수는 늘지 않았습니다.** 대신 이런 것들이 달라집니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **용린 세트가 4조각** | 용린 기사검이 늘었습니다. 이미 세 조각을 입고 있던 사람은 그대로 쓰고, 세트가 3/4 로 잡힙니다 |
| **상점이 옮겨졌습니다** | 용린 세 조각을 **성 밖 마을의 마르타**가 팝니다. 성 안 무기상은 대신 **쓸모없는 검**을 팝니다 |
| **용사 스킬 '강타' 삭제** | 찍어 두었던 점수는 **접속할 때 자동으로 돌아옵니다.** 잃는 것은 없고, 다시 고르면 됩니다 |
| **민첩 2회 공격 0.6% → 0.5%** | 민첩에 기댄 캐릭터가 아주 조금 약해집니다 |
| **1~4단계 난이도** | 슬라임·박쥐·늑대가 세졌습니다. **경험치·골드는 그대로**입니다 |
| **이름표 색** | 경계값을 다시 잡았습니다. 예전에 온통 주황이던 땅들이 초록·노랑으로 보입니다 — 세기가 바뀐 게 아니라 색이 정직해진 것입니다 |
| **몬스터 표** | 잡몹과 보스의 체력이 바뀌었습니다. 시트를 쓰고 있다면 **꼭 다시 올리세요**(아래) |

> ⚠ **`items` 탭에 줄이 둘 늘었습니다**(쓸모없는 검 · 용린 기사검).
> `skills` 탭에서는 한 줄이 빠졌습니다(강타).
> 시트를 안 올리면 새 검 둘이 사라지고 강타가 되살아납니다.

### B-2-9. 0.35 에서 달라진 것 — 넣을 값은 없지만 알아 둘 것

**환경 변수는 하나도 늘지 않았습니다.** 대신 올리고 나면 이런 것들이 달라집니다.

| 무엇 | 어떻게 되나 |
|---|---|
| **장비 칸이 10칸** | **투구** 칸이 새로 생깁니다. 옛 세이브는 그 칸이 빈 채로 열립니다 — 잃어버리는 것은 없습니다 |
| **필드 난이도** | 1~20단계 몬스터 레벨과 맵 배율이 다시 깔렸습니다. **이미 지나온 사람의 캐릭터는 그대로**이고, 땅만 바뀝니다 |
| **몬스터 표** | `sheets/monsters.csv` 의 레벨·hp 가 바뀌었습니다. 시트를 쓰고 있다면 **A-3 뒤에 시트에도 반영**하세요(아래) |
| **용린 세 조각** | 성문 무기상 진열에 세 줄이 늘어납니다. 옛 `용린 갑옷`을 이미 가진 사람은 그대로 쓰고, 세트 2/3 로 잡힙니다 |

> ⚠ **시트를 쓰고 있다면 순서가 중요합니다.** 서버는 시트를 원본으로 봅니다.
> 새 코드만 올리고 시트를 그대로 두면, 서버가 켜지자마자 **옛 몬스터 표를 다시 덮어씁니다** —
> 그러면 이번에 고친 난이도가 통째로 사라집니다.
> `SHEETS.md` 의 "코드에서 시트로 되돌리기"를 먼저 하거나, 아래 두 줄로 올릴 CSV 를 뽑으세요.
>
> ```bash
> node tools/sheets.js export     # sheets/*.csv 를 지금 코드 값으로 맞춘다
> npm run sheets                  # 구글 시트에 올릴 xlsx 한 벌을 만든다
> ```
>
> 만들어진 xlsx 를 구글 시트에 **덮어쓰기로 가져오기** 하면 됩니다.

### B-3. 환경 변수 — 새로 넣을 것은 없습니다

0.15 때 넣어 둔 값이 그대로 쓰입니다. 다만 그동안 늘어난 것이 있어서,
**안 넣어 두었다면 지금 넣어 두면 좋은 것**들입니다.
Render → 그 서비스 → 왼쪽 **Environment** → **Add Environment Variable**.

| 이름 | 값 | 없으면 |
|---|---|---|
| `ADMIN_KEY` | 아무 긴 문자열 | 이벤트 우편을 아예 못 보냅니다 |
| `REGISTER_LIMIT` | `5` (친구끼리면 `50`) | 5. 같은 공유기에서 여럿이 만들면 걸립니다 |
| `SHEET_ID` | 아래 C 에서 만듭니다 | 구글 시트 연결이 안 됩니다 |
| `SHEET_POLL_MIN` | `5` | **안 넣어도 5분마다 봅니다.** `0` 을 넣으면 아예 안 봅니다 |

`render.yaml` 을 쓰고 있다면 `ADMIN_KEY` · `REGISTER_LIMIT` · `SHEET_POLL_MIN` 은
이미 들어 있습니다. **`SHEET_ID` 하나만** 손으로 넣으면 됩니다.

### B-4. 접속해 있던 사람들

배포가 끝나면 접속 중이던 사람에게 "새 콘텐츠가 배포되었습니다" 알림이 뜹니다.
**한 번 새로고침**하면 새 버전으로 넘어갑니다.

---

## C. 구글 시트로 표 연결하기

한 번 붙여 두면 **GitHub 을 거치지 않고** 시트에서 숫자만 고쳐도 서버가 읽어 갑니다.
밸런스를 만지는 동안에는 이쪽이 훨씬 빠릅니다.

> ### ⚠ 이미 시트를 붙여 두었다면 — 이번 판은 **다시 올려야 합니다**
>
> 0.33 에서 `monsters` 탭에 **`분노`** 칸이 하나 늘었습니다(`전투크기` 와 `필드그림` 사이).
> 서버는 칸을 **자리로** 읽으므로, 칸이 없는 옛 시트를 그대로 두면 그 뒤의 값이
> 한 칸씩 밀려 들어갑니다.
>
> 이제는 밀린 채로 들어가지 않고 **당기기가 그 자리에서 멈추고** 어느 칸이 다른지
> 알려 줍니다(`monsters 시트의 16번째 칸 제목이 다릅니다...`). 그 말이 보이면
> 아래 C-1 로 표를 새로 뽑아 올리면 됩니다.
>
> 시트에서 직접 고쳐 둔 숫자가 있다면 **먼저 `npm run sheets` 로 지금 값을 뽑아
> 견줘 보고** 올리세요 — 새로 뽑은 표는 저장소의 지금 값이라, 시트에만 있던
> 수정은 덮입니다.

### C-1. 통합문서 굽기

저장소 폴더에서:

```bash
npm run sheets
```

`dragon-field-tables.xlsx` 가 만들어집니다. **탭 아홉 개**가 들어 있습니다.

```
items  monsters  drops  quests  skills  classes  traits  stats  config
```

| 탭 | 무엇을 고치나 |
|---|---|
| `items` | 장비·소모품·재료 — 능력치, 가격, 등급 |
| `monsters` | 몬스터 능력치·경험치·골드 |
| `drops` | 무엇이 몇 % 로 떨어지나 |
| `quests` | 목표와 보상 |
| `skills` | 직업별 스킬과 효과 |
| `classes` | 기본 능력치·성장·패시브·**힘/민첩/지능 성장주기** |
| `traits` | 포인트로 찍는 특성 여섯 갈래 |
| `stats` | 힘·민첩·지능이 1점당 주는 것 |
| `config` | 포인트를 몇 레벨마다 주나 |

### C-2. 드라이브에 올리기

1. `dragon-field-tables.xlsx` 를 **구글 드라이브에 끌어다 놓습니다.**
2. 올라간 파일을 **더블클릭**합니다. 구글이 시트로 바꿔 주고 **탭 이름도 그대로** 붙습니다.

> **문서는 반드시 하나여야 합니다.** 표 아홉 개를 문서 아홉 개로 나누면 서버가 못 읽습니다.
> 서버는 문서 아이디 하나만 알고, 그 안에서 **탭 이름으로** 찾아갑니다.
> 그래서 **탭 이름을 바꾸면 그 표만 조용히 안 읽힙니다.**

### C-3. 공개하기 — 폴더가 아니라 그 시트를

여기서 헷갈리기 쉽습니다. **공유 폴더에 넣는 것만으로는 안 됩니다.**
서버는 로그인하지 않은 손님으로 읽으므로, **그 시트 문서 자체**가
"링크가 있는 모든 사용자" 로 열려 있어야 합니다.

시트를 연 상태에서 오른쪽 위 **공유** →
**일반 액세스** → **링크가 있는 모든 사용자** → 권한은 **뷰어**.

> **뷰어면 충분합니다.** 아무도 시트를 고칠 수 없고, 서버는 읽기만 합니다.
> 편집은 본인 계정으로 들어가서 하면 됩니다.
>
> 폴더째 공유해 두었다면 그 안의 파일이 대개 함께 열리지만, **시트 자체의 공유 설정을
> 직접 확인해 두는 편이 확실합니다.** 폴더 권한만 믿었다가 안 읽히는 일이 흔합니다.

### C-4. 주소 복사

시트 주소창을 통째로 복사합니다. 이렇게 생겼습니다.

```
https://docs.google.com/spreadsheets/d/1wzK84cOD……/edit#gid=0
```

### C-5. Render 에 알려 주기

Render → 그 서비스 → **Environment** → **Add Environment Variable**

| 이름 | 값 |
|---|---|
| `SHEET_ID` | 위에서 복사한 주소 **통째로** |
| `SHEET_POLL_MIN` | `5` |

지금 올려 둔 시트(`dragonfieldtables`)라면 `SHEET_ID` 에 이것을 그대로 넣으면 됩니다.

```
1-uaFpDrhAADYfBx27FlSlhoah56-MQUZ1qqORa9sK0k
```

**Save Changes** 를 누르면 서버가 다시 뜹니다.

> ### 시트 이름은 마음대로 바꿔도 됩니다 — **문서는 바꾸지 마세요**
>
> `SHEET_ID` 는 문서 **아이디**입니다. 시트 이름을 `…0_43_0` 으로 바꾸든 뭐로 바꾸든
> 아이디는 그대로라 서버가 계속 같은 문서를 읽습니다.
>
> 위험한 것은 **새 문서를 만드는 것**입니다. 새 판의 xlsx 를 드라이브에 올릴 때는
> **같은 문서에 "덮어쓰기로 가져오기"** 를 쓰세요. 새 문서로 만들면 아이디가 바뀌어
> 서버는 옛 문서를 계속 읽고, 새 문서에 아무리 고쳐도 반영되지 않습니다.
> (0.42 때 드라이브에 옛 문서와 새 문서가 둘 다 남아 실제로 헷갈렸던 자리입니다.)

### C-6. 붙었는지 확인

Render 의 **Logs** 에 이 줄이 보이면 성공입니다.

```
구글 시트   : 연결됨 — 10분마다 확인
```

안 보이면 `SHEET_ID` 가 안 들어간 것이고,
`받지 못했습니다 (403)` 이 보이면 **C-3 의 공개 설정**이 안 된 것입니다.

### C-7. 이제부터

시트에서 숫자를 고치면 **10분 안에** 서버가 읽어 갑니다. 기다리기 싫으면:

```bash
curl -X POST https://내서버.onrender.com/api/content/pull \
  -H "Content-Type: application/json" \
  -d '{"key":"내ADMIN_KEY","note":"밸런스 조정"}'
```

시트에 말이 안 되는 값을 넣으면 **서버가 거부하고 이전 표를 그대로 씁니다.**
게임이 깨지지는 않습니다. 무엇이 잘못됐는지는 Logs 에 나옵니다.

막아 주는 것들(0.29.1 에서 몬스터 쪽을 더 촘촘히 했습니다):

| 적어 넣은 값 | 어떻게 되나 |
|---|---|
| 몬스터 hp 를 0 이나 음수로 | 거부 — `hp 가 0 보다 커야 합니다. 지금: -5` |
| 몬스터 속도를 0 으로 | 거부 — 차례가 영영 안 돌아옵니다 |
| 치명타 확률을 `3` 으로 (3% 를 3 으로) | 거부 — `0~1 이 아닙니다. 3% 는 0.03 입니다` |
| 없는 아이템을 드랍표에 | 거부 |
| 탭 이름을 바꿈 | 거부 — 어느 탭인지 알려 줍니다 |

### C-7½. ⚠ 코드를 올릴 때마다 통합문서를 다시 구우세요

**표에 칸이 늘어나는 판올림이 있습니다.** 0.30 에서 `classes` 탭에
`회피 확률/민첩` 과 `마력 흡수/지능`, 0.31 에서 `1:1 피해 감소` 가 새로 생겼습니다.

옛 시트에는 그 줄이 없습니다. 그대로 두면 서버가 시트를 읽어 갈 때
**그 줄이 비어 있는 것으로 알고 새 패시브를 지워 버립니다.** 배포한 지 10분 만에
사냥꾼의 회피와 마법사의 마력 흡수가 조용히 사라집니다.

그러니 코드를 올린 뒤에는 **반드시** 이 순서를 밟으세요.

```bash
npm run sheets            # dragon-field-tables.xlsx 를 새로 굽는다
```

그리고 구글 드라이브의 **같은 문서를 새 파일로 갈아 끼웁니다.**
드라이브에서 기존 `dragonfieldtables` 를 열고 **파일 → 가져오기 →
업로드** 로 새 xlsx 를 올린 뒤 **"스프레드시트 바꾸기"** 를 고르면
**주소와 공개 설정이 그대로 유지됩니다.** (새로 끌어다 놓으면 주소가 바뀌어
`SHEET_ID` 를 다시 넣어야 합니다.)

> 시트에서 손으로 고쳐 둔 값이 있으면 갈아 끼우기 전에 적어 두세요 —
> 새로 구운 문서에는 코드에 든 값이 들어 있습니다.

### C-8. 고친 것이 사람들 화면에 닿기까지

1. 시트에서 값을 고칩니다.
2. **10분 안에** (또는 위 `curl` 로 즉시) 서버가 읽어 가고 콘텐츠 판이 하나 올라갑니다.
3. 접속해 있던 사람 화면 위에 **"새 콘텐츠 v○ 가 배포되었습니다 · 지금 적용"** 띠가 뜹니다.
4. **지금 적용**을 누르면 저장하고 새로고침합니다.

> ⚠ **새로고침하면 접속 화면으로 돌아갑니다.** 아이디·비밀번호를 다시 넣어야 합니다.
> 진행한 것은 누르는 순간 저장되므로 잃는 것은 없지만, 다시 들어가는 수고는 있습니다.

> **배포(git push)를 하면 기준 표가 코드에 든 것으로 새로 깔립니다.** 그 직후 잠깐은
> 시트에서 고쳐 둔 값이 사라진 것처럼 보이는데, 다음 확인 때(최대 10분) 시트가
> 다시 덮어씁니다. 급하면 위 `curl` 을 한 번 치세요.

---

## 두 갈래를 언제 쓰나

| | GitHub (`git push`) | 구글 시트 |
|---|---|---|
| 코드가 바뀌었다 | **이쪽** | 안 됩니다 |
| 새 아이템·몬스터를 넣는다 | 됩니다 | **이쪽** (줄을 하나 더 붙이면 됩니다) |
| 숫자만 만진다 | 됩니다 | **이쪽** (훨씬 빠릅니다) |
| 맵·그림을 바꿨다 | **이쪽** | 안 됩니다 |

시트를 쓰더라도 **GitHub 저장소는 그대로 두세요.** Render 는 코드를 거기서 읽어 옵니다.
시트는 표(숫자)만 덮어씁니다.

---

## 안 될 때

| 증상 | 원인과 해결 |
|---|---|
| 접속 화면에 `v0.15.0` 그대로 | Render 가 아직 배포를 안 했습니다. Manual Deploy → Deploy latest commit |
| 배포가 실패한다 | Logs 를 보세요. `NODE_VERSION` 이 `20` 인지 확인 (Environment) |
| 로그인은 되는데 캐릭터가 없다 | `UPSTASH_REDIS_REST_URL`·`TOKEN` 이 빠졌습니다. 없으면 서버가 잠들 때마다 지워집니다 |
| 특성·스킬이 0 으로 돌아갔다 | **정상입니다.** 0.15 의 특성·스킬은 지금 것과 다른 물건이라 되돌려 준 것이고, 포인트는 그대로 쥐고 있습니다. 캐릭터 창에서 다시 고르세요 |
| 시트를 고쳤는데 안 바뀐다 | ① 10분을 기다렸는가 ② 탭 이름이 그대로인가 ③ 시트가 "링크가 있는 모든 사용자"인가 |
| Logs 에 `403` | C-3 의 공개 설정이 안 됐습니다. 폴더가 아니라 **시트 문서**를 공개해야 합니다 |
